# Lola LMS — ТЗ. Часть 1. Роли, права, доступ

## 1.1 Модель прав

Права выдаются не ролью напрямую, а **скоупами**. Роль — именованный набор скоупов.
Роль назначается пользователю **в области видимости**: вся сеть, конкретная точка,
конкретное подразделение. Один пользователь может иметь несколько назначений ролей.

```
user ──< user_role >── role ──< role_scope >── scope
              │
              └── scope_type: tenant | org_unit | location
                  scope_id:   uuid | null (null = вся сеть)
```

Проверка доступа: `can(user, 'course.edit', { locationId })` → true, если у пользователя есть
роль со скоупом `course.edit`, назначенная на этот тенант, это подразделение или эту точку.

## 1.2 Системные роли (создаются при заведении тенанта)

| Роль | Код | Кому | Область по умолчанию |
| --- | --- | --- | --- |
| Сотрудник | `employee` | все | своя точка |
| Наставник | `mentor` | шеф, старший смены | своя точка |
| Руководитель точки | `manager` | управляющий | своя точка |
| Методист | `author` | HR, тренер | сеть |
| Администратор | `admin` | COO | сеть |
| Власник | `owner` | собственник, подписант договора | сеть |
| Оператор платформы | `platform_admin` | мы | вне тенанта |

> [исправлено, `docs/v2/35-billing-limits.md` §2 ссылается на `owner` как на существующую
> системную роль тенанта («Суммы видит только он, `admin` — потребление и даты»), а §1.2
> перечисляла пять ролей без него: суммы и смену тарифа было некому отдать] Ранее: «| Администратор | `admin` | COO, владелец | сеть |» и пять системных ролей без `owner`.

**Власник — один на тенант.** Это не «администратор посильнее»: администратор управляет
системой, владелец — тем, что нельзя отдать наёмному администратору (деньги, владение,
назначение самих администраторов). Инвариант «ровно один» держится схемой — частичный
уникальный индекс `user_roles_single_owner_uq` по `(tenant_id) where is_owner`
(миграция `0070_owner_role`), а не проверкой в сервисе. Подробности — §1.9.4.

Тенант может создавать свои роли из набора скоупов. Системные роли не удаляются,
но их набор скоупов можно менять, кроме `admin` и `owner`.

> [исправлено, у `owner` набор прав обязан быть неизменяемым по той же причине, что и у
> `admin`: иначе владение «переписывают» правкой роли вместо передачи — достаточно снять
> `tenant.transfer` или дописать себе `billing.manage`] Ранее: «кроме `admin` (иначе тенант заблокирует сам себя)».

## 1.3 Скоупы

**Обучение (потребление)**
- `learn.view` — видеть назначенное, проходить уроки
- `learn.catalog` — видеть каталог и записываться самостоятельно
- `learn.attempt` — проходить тесты и аттестации

**Контент**
- `course.view`, `course.create`, `course.edit`, `course.publish`, `course.archive`
- `question.manage` — банк вопросов
- `media.upload`, `media.delete`
- `knowledge.manage` — база знаний
- `survey.manage` — опросы

**Люди и назначения**
- `people.view`, `people.invite`, `people.edit`, `people.deactivate`, `people.import`, `people.password` (смена пароля другому человеку — отдельный скоуп, `04` §4.11)
- `assignment.create`, `assignment.cancel`
- `role.assign`

**Проверка**
- `review.queue` — очередь проверки развёрнутых ответов
- `review.grade` — выставлять зачёт/незачёт
- `certification.confirm` — подтверждать аттестацию очно

**Отчёты**
- `report.own` — свои результаты
- `report.team` — по своей точке/подразделению
- `report.tenant` — по всей сети
- `report.export` — выгрузка xlsx/csv

**Настройки**
- `settings.tenant` — бренд, языки, домены
- `settings.notifications` — шаблоны и расписания
- `settings.integrations` — Telegram, SMS, S3, вебхуки
- `audit.view` — журнал действий

**Мотивація** (бонуси і магазин подарунків, `21` §2, Г-21.1, §17)
- `shop.manage` — каталог магазина: подарки, категории, цены, остатки, публикация; все заказы сети
- `shop.issue` — очередь «До видачі» своей точки: «Готово до видачі», «Видано», отмена с причиной
- `bonus.grant` — «Нарахувати вручну»: бонусы людям своей области, со знаком и причиной; журнал и реестр бонусов этой области

Заказать подарок себе и отменить свой ещё не подготовленный заказ особого права не требует —
это `learn.view`.

> [исправлено, `gamification` (24.09.2026): скоупов магазина не было, модуль был R3] Ранее: группы
> «Мотивація» не было, а `21` §2 давал одной строкой «Управлять магазином» руководителю точки и
> администратору. Разведено на два скоупа: каталог и цена в бонусах — одни на сеть (`shop.manage`,
> только admin), выдача — дело точки (`shop.issue`, manager своей точки). `bonus.grant` — по
> строке `21` §2 «Начислять баллы вручную» (mentor, manager, admin), но только бонусы.

**Власність**
- `tenant.transfer` — передать владение пространством другому человеку (§1.9.4)

> [добавлено вместе с ролью `owner`: «передать владение» нельзя выразить ни одним из
> существующих скоупов — `settings.tenant` есть у каждого администратора, а проверка по коду
> роли (`if (role.code === 'owner')`) нарушила бы принцип «поведение — через права»] Ранее: группы «Власність» не было.

Скоупы денег (`billing.view`, `billing.usage.view`, `billing.payments.view`, `billing.manage`)
и хранилища (`storage.*`) перечислены в §1.10 — они пришли из пакета `docs/v2`. Здесь важно
одно: `billing.payments.view` и `billing.manage` есть **только** у `owner` (`docs/v2/35` §2).

## 1.4 Матрица (системные роли × скоупы)

| Скоуп | employee | mentor | manager | author | admin | owner |
| --- | :-: | :-: | :-: | :-: | :-: | :-: |
| learn.view / learn.attempt | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| learn.catalog | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| course.view | — | ✓ | ✓ | ✓ | ✓ | — |
| course.create / edit | — | — | — | ✓ | ✓ | — |
| course.publish | — | — | — | ✓ | ✓ | — |
| question.manage | — | — | — | ✓ | ✓ | — |
| media.upload | — | ✓ | ✓ | ✓ | ✓ | — |
| knowledge.manage | — | — | — | ✓ | ✓ | — |
| survey.manage | — | — | ✓ | ✓ | ✓ | — |
| people.view | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| people.invite / edit | — | — | ✓ | ✓ | ✓ | ✓ |
| people.import | — | — | — | — | ✓ | — |
| people.deactivate | — | — | ✓ | — | ✓ | ✓ |
| assignment.create / cancel | — | ✓ | ✓ | ✓ | ✓ | — |
| role.assign | — | — | — | — | ✓ | ✓ |
| review.queue / review.grade | — | ✓ | ✓ | ✓ | ✓ | — |
| certification.confirm | — | ✓ | ✓ | — | ✓ | — |
| report.own | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| report.team | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| report.tenant | — | — | — | ✓ | ✓ | ✓ |
| report.export | — | — | ✓ | ✓ | ✓ | ✓ |
| settings.tenant | — | — | — | — | ✓ | ✓ |
| settings.notifications / integrations | — | — | — | — | ✓ | — |
| audit.view | — | — | — | — | ✓ | ✓ |
| billing.view / billing.usage.view | — | — | — | — | ✓ | ✓ |
| billing.payments.view / billing.manage | — | — | — | — | — | ✓ |
| storage.view / storage.policy / storage.addon | — | — | — | — | ✓ | ✓ |
| org.structure.view | — | — | — | — | ✓ | ✓ |
| tenant.transfer | — | — | — | — | — | ✓ |
| shop.manage | — | — | — | — | ✓ | — |
| shop.issue | — | — | ✓ | — | ✓ | — |
| bonus.grant | — | ✓ | ✓ | — | ✓ | — |

> [исправлено, добавлена колонка `owner` и разведены строки денег: по `docs/v2/35` §2 суммы
> платежей и смена тарифа принадлежат только владельцу, а строка «settings.*» скрывала, что
> владельцу нужны бренд и домены, но не шаблоны рассылок и не интеграции] Ранее: колонок было пять, а строки читались как «settings.* — только admin».

**Как читать колонку `owner`.** Она не «admin плюс ещё немного»: у владельца нет контента,
назначений, проверки, рассылок и интеграций — и это сознательно. Владелец малой сети, который
ведёт обучение сам, берёт **вторую роль** `admin` и ходит между ними переключателем активной
роли (§1.9.2). Так у роли «Власник» остаются ровно те права, которые никому не передают.

## 1.5 Вход и идентификация

**Основной способ — телефон + одноразовый код.**

1. Пользователь вводит номер в формате `+380XXXXXXXXX` (маска, нормализация до E.164).
2. Сервер создаёт `otp_code`: 6 цифр, срок 5 минут, хранится как хеш (argon2id).
3. Доставка: Telegram, если номер связан с ботом, иначе SMS. Канал выбирается автоматически,
   пользователю пишется, куда ушёл код.
3.1. **Другой канал — e-mail** (рішення замовника 20.09.2026, `docs/28` «Вхід: код на e-mail»):
   якщо у людини в картці є пошта, вона може натиснути «Надіслати код на пошту» на екрані входу —
   той самий код піде листом (шаблон `otp_code`, SMTP тенанта → платформенний). Автоматичний фолбек
   на пошту (без явного вибору) — коли тенант не налаштував SMS-провайдера і дозволив це політикою
   `session.otpFallbackToEmail`. Телефон лишається єдиним ідентифікатором для входу — пошта тут лише
   спосіб доставки, а не альтернативний логін (нижче цей документ як і раніше не описує вхід за e-mail
   замість телефону; резервний спосіб «e-mail + пароль» — окремий механізм, див. нижче).
4. Ограничения: не более 3 отправок на номер за 15 минут, не более 5 попыток ввода на код,
   после 5 — блокировка номера на 30 минут. Счётчики — в БД, не в памяти процесса.
5. Успешный ввод → сессия: `session` в БД + httpOnly, Secure, SameSite=Lax cookie,
   срок 30 дней с продлением, привязка к user-agent и подсети (мягкая: смена → ре-аутентификация).
6. Если номер найден в нескольких тенантах — экран выбора пространства.

**Резервные способы**
- Вход по e-mail + паролю (argon2id) — для методистов и администраторов; включается тенантом.
- Ссылка-приглашение с одноразовым токеном (48 часов) — для первого входа.
- Вход «от имени» (impersonation) для `platform_admin`: только с явной причиной,
  фиксируется в `audit_log`, сессия помечена, в интерфейсе — постоянная плашка.

**Второй фактор** (`24` §3.4.2, PR-39): код приложения-аутентификатора (TOTP) после любого из
способов входа — если человек подключил его сам или политика «Двухфакторность для админов»
требует его от носителей `settings.tenant`. До кода сессия промежуточная: открыты только
`/auth/two-factor/*` и выход. Сессия «от имени» второй фактор человека не спрашивает.

**API-токен интеграции** (Bearer, `04` §4.1) действует скоупами токена на весь тенант. Скоуп с
флагом `sessionOnly` (`shared/domain/roles.ts`, `docs/v2/44` В-20) — право человека, а не
интеграции: заметки о людях, решения по кандидатам, прослушивание собеседования, пароль и второй
фактор другого человека, передача владения. Токену такой скоуп не выдаётся, у выданного раньше —
не действует.

**Выход**: удаление сессии; «выйти на всех устройствах» — удаление всех сессий пользователя.

## 1.6 Жизненный цикл сотрудника

| Статус | Что означает | Что можно |
| --- | --- | --- |
| `invited` | создан, не входил | получить приглашение повторно |
| `active` | входил, работает | всё по роли |
| `suspended` | временно отключён | вход запрещён, данные сохранены |
| `archived` | уволен | вход запрещён, результаты хранятся, из отчётов по умолчанию скрыт |

Удаление человека — только `archived` + анонимизация по запросу (см. `06-infra.md`, GDPR-раздел).
Результаты обучения при анонимизации сохраняются обезличенно: нужны для статистики курса.

## 1.7 Структура организации

```
tenant
└── org_unit (дерево: сеть → бренд → направление)
     └── location (точка: адрес, часовой пояс, руководитель)
          └── position (позиция: повар, кассир, курьер)
```

Пользователь имеет одно основное назначение `location + position` и может иметь
дополнительные (совмещение). Назначения обучения умеют адресоваться на:
человека, позицию, точку, подразделение, роль или динамический сегмент
(см. `03-modules.md`, «Назначения»).

## 1.8 Крайние случаи

- Человек сменил точку → прошлые результаты остаются за ним, новые назначения приходят
  по новой точке; в отчёте старой точки он виден до даты перевода.
- Человек уволился и вернулся → тот же `user`, статус `active`, история цела.
- Один телефон на двоих (редко, но бывает на точках) → запрещено: телефон уникален в тенанте.
  Для таких случаев — вход по e-mail или личный номер.
- Наставник проверяет сам себя → запрещено: в очередь проверки не попадают свои попытки.

## 1.9 Сверено с эталоном

Проверено 19.09.2026 (`/users/:id/roles`, меню пользователя, `/site/policies`).

### 1.9.1 Роли фиксированы поставщиком

В снятой установке четыре роли: **«Адміністратор Т.Т.»**, **«Адміністратор.»**,
**«Користувач.»**, **«Тьютор.»**. Человеку можно выдать несколько.
Экрана создания или правки роли у администратора тенанта **нет** — набор приходит
от вендора (точки в названиях выдают ручное заведение на его стороне).

Политика «Ролі» (`24` §3.4.1) позволяет только выбрать **роль по умолчанию**, включить
«Не використовувати глобальні ролі» и задать **«Режим призначення ролей»**:
«Зіставлення посад» \| «На основі даних імпорту». То есть роль в эталоне чаще выводится
из должности, а не назначается руками. То же и с подчинёнными: «Режим визначення
підлеглих» — «Зіставлення посад» \| «На основі даних імпорту».

**Для Lola.** Правило «должность → роль» берём обязательно: в сети из 120 человек
руками роли не раздают. Редактор ролей со скоупами — наше `[решение]`, обоснование
в `16` §14.5.

### 1.9.2 Переключение активной роли

В меню пользователя (по клику на ПІБ) рядом с «Профіль · Безпека · Сповіщення» и «Вийти»
перечислены **все роли человека, и одна из них активна**. Интерфейс меняется под активную
роль, а не показывает объединение прав.

**Берём это решение.** Наставник, который сам ещё учится, иначе видит кашу из двух меню.
В сессии есть `active_role_id`, переключение — без выхода, текущая роль видна в шапке
(в эталоне она подписана прямо под именем). Права считаются по активной роли, но аудит
пишет и её, и полный набор ролей — иначе при разборе не видно, кто на самом деле
мог совершить действие.

### 1.9.3 Что это меняет в `01`

1. `user_roles` — связь многие-ко-многим, а не одна роль на человека (§1.2 поправлен).
2. Добавляется `position_role_map` — правило «должность → роль», применяемое при импорте
   и при смене должности.
3. `sessions.active_role_id` и переключатель в шапке.

### 1.9.4 Владение пространством

Владелец (`owner`, §1.2) — **один на тенант**, и это не «роль, которую выдают», а факт: с этим
человеком договор, он платит и он решает, кто здесь администратор.

**Ровно один — гарантия схемы.** Признак владения лежит на назначении роли
(`user_roles.is_owner`, пишется триггером из кода роли), поверх него — частичный уникальный
индекс `user_roles_single_owner_uq` по `(tenant_id) where is_owner`. Две одновременные передачи
владения кончаются ошибкой уникальности, а не двумя владельцами. Сервис
(`server/services/owner.ts`) проверяет то же самое заранее — ради понятной ошибки, не ради
гарантии.

**Снять владение с себя нельзя — только передать.** Отдельной операции «перестать быть
владельцем» не существует:

| Попытка | Что происходит |
| --- | --- |
| `DELETE /people/:id/roles/owner` | `409 owner_role` — «володіння не можна зняти» |
| `POST /people/:id/roles` с `owner` | `409 owner_role` — владение не раздают через карточку человека |
| Заблокировать, архивировать, уволить владельца | `409 last_owner` — сначала передать владение |
| Правка набора прав роли `owner` | `409 owner_role` — набор фиксирован (§1.2) |
| `POST /settings/owner/transfer` | единственный путь: владение уходит целиком, в один шаг |

Принять владение может действующий сотрудник (`kind = 'employee'`, не заблокирован, не в
архиве) — не кандидат: владелец пространства не может быть человеком, который в нём ещё
не работает.

**Тенанты, заведённые до этой роли.** `[решение]` Миграция `0070_owner_role` заводит роль, но
**никому её не выдаёт**. Назначить владельцем «самого старого администратора» — значит молча
выбрать подписанта договора за людей; в тенанте с тремя администраторами этот выбор некому
обосновать. Вместо этого на экране «Ролі та права» администратор видит карточку «У простору
ще немає власника» с одноразовой кнопкой **«Стати власником»** (`POST /settings/owner/claim`,
скоуп `settings.tenant`): кто нажал — тот и владелец, второй уже видит имя первого, а гонку
между ними разрешает тот же уникальный индекс. Новые тенанты приходят с владельцем сразу:
создатель тенанта получает обе роли — `admin` и `owner` (`24` §4.3).

Оба события пишутся в `audit_log` (`tenant.owner_claimed`, `tenant.owner_transferred`) и в
журнал безопасности (`roles.changed`) с `request_context`.

## 1.10 Скоупы пакета `docs/v2`

Патч `docs/v2/39-patches.md` П-01. Одиннадцать модулей пакета (рекрутинг, ИИ-собеседование,
библиотека контента, оргструктура, жизненный цикл и офбординг, хранилище, тариф, обратная
связь по контенту, делегирование проверки, расширения карточки человека) добавляют скоупы в
`SCOPES` (`shared/domain/roles.ts`) и группы редактора ролей `SCOPE_GROUPS`
(`server/services/roles.ts`). Состав взят из §2 «Роли и скоупы» **каждого модульного
документа** — при расхождении с обзорной таблицей патча побеждает модульный документ (правило
самого П-01).

Скоупы только объявлены: под ними ещё нет эндпоинтов (`docs/v2/45-plan.md`, дорожка PR-03 —
эндпоинты появятся в PR своего модуля). Ни один не назначен ролям `employee`, `mentor`,
`manager`, `author` по умолчанию — их получает только `admin` (в силу `[...SCOPES]` в матрице
системных ролей); распределение по новым ролям (`recruiter`, `hr`) — решение того PR, который
добавит соответствующие эндпоинты и экраны.

> [исправлено, PR-21: абзац отстал от PR, выдававших скоупы вместе со своими эндпоинтами]
> Ранее абзац выше был правдой целиком. Сейчас системным ролям выдано: `lifecycle.view` —
> `mentor`, `manager`, `author` (PR-05); `offboarding.start` — `manager` (PR-07);
> `content_issue.report` — `employee`, `mentor`, `manager`, `author`, `content_issue.view` —
> `author` (PR-23); `org.structure.view` — всем системным ролям, `org.structure.edit` —
> `manager` в своей ветке (PR-30); `time.metrics.view` — `mentor`, `manager` (PR-21: `GET /learning/time/totals`,
> чужое время — в области своей роли, `docs/v2/37` §2). Автор поимённого времени не получает —
> ему обезличенный отчёт PR-22. Источник истины — `SYSTEM_ROLES` в `shared/domain/roles.ts`.
>
> [уточнено, PR-22] Нормы времени и обезличенный отчёт «План і факт часу» нового скоупа не
> заводят: читать — `time.metrics.view` (наставник и руководитель, агрегаты по своей области) или
> `course.edit` (методист и администратор, весь тенант), менять норму — `course.edit`
> (`docs/v2/37` §2, решение Р-22.5).

| Группа | Скоупы | Документ |
| --- | --- | --- |
| Кандидати | `candidate.view`, `candidate.edit`, `candidate.assign`, `candidate.decide`, `candidate.hire`, `candidate.delete`, `candidate.status.manage` | `docs/v2/28` |
| Вакансії | `vacancy.view`, `vacancy.edit`, `vacancy.publish`, `vacancy.close`, `vacancy.template.manage`, `vacancy.criteria.manage`, `vacancy.ai.use`, `jobboard.connect`, `jobboard.publish` | `docs/v2/29` |
| ІІ-співбесіда | `interview.configure`, `interview.view`, `interview.listen`, `interview.override`, `summary.view`, `summary.edit`, `summary.send`, `ai.review.use`, `ai.audit` | `docs/v2/30` |
| Бібліотека модулів | `library.view`, `library.use`, `library.publish`, `library.manage` | `docs/v2/31` |
| Оргструктура | `org.structure.view`, `org.structure.edit`, `org.structure.import` | `docs/v2/32` |
| Етапи життєвого циклу | `lifecycle.view`, `lifecycle.manage`, `offboarding.start`, `offboarding.complete` | `docs/v2/33` |
| Сховище | `storage.view`, `storage.delete`, `storage.policy`, `storage.addon` | `docs/v2/34` |
| Тариф і ліміти | `billing.view`, `billing.usage.view`, `billing.payments.view`, `billing.manage` | `docs/v2/35` |
| Звіт про помилки | `content_issue.report`, `content_issue.view`, `content_issue.triage`, `content_issue.assign`, `content_issue.rescore`, `content_issue.mute` | `docs/v2/36` |
| Делегування перевірки | `review.delegate`, `review.delegate.any`, `review.routing.manage`, `review.workload.view`, `review.absence.manage`, `time.metrics.view` | `docs/v2/37` |
| Картка людини | `person.activity.view_others`, `person.note.read`, `person.note.write`, `person.document.view_others`, `person.document.manage`, `person.absence.manage`, `person.rating.view_others` | `docs/v2/38` |

> [исправлено, PR-25: у библиотеки модулей появились эндпоинты (`/library/*`), и скоупы
> распределены по `docs/v2/31` §2: `library.view` + `library.use` — `mentor` и `manager`
> (видят библиотеку и предлагают свой урок; вставлять не могут — нет `course.edit`), плюс
> `library.publish` — `author` (кладёт модули, правит свои, принимает предложения);
> `library.manage` (чужие модули, физическое удаление) — только `admin`. Уже заведённым
> тенантам скоупы выдаёт миграция PR-25, новым — матрица `SYSTEM_ROLES`. Так же раньше
> поступили PR-05 (`lifecycle.view`), PR-07 (`offboarding.start`) и PR-23 (`content_issue.*`) —
> перечень исключений ведётся в шапке `shared/domain/roles.ts`] Ранее: «Ни один не назначен
> ролям `employee`, `mentor`, `manager`, `author` по умолчанию».

**Отдельно.** Скоупы `platform.plans.manage`, `platform.limits.override`, `platform.ai.grant`,
`platform.payments.manage` из `docs/v2/35-billing-limits.md` §2 в `SCOPES` не включены: доступ
оператора платформы — отдельный механизм (`platform_admin`, BYPASSRLS-подключение,
`server/services/platform.ts`), не скоупы тенантной роли. Их реализация — решение того PR,
который заведёт панель оператора для тарифов и лимитов.
