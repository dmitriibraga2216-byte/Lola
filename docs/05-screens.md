# Lola LMS — ТЗ. Часть 5. Экраны

Визуальные правила — в бренд-буке Lola (беж `#F0E7D7` фон, солнце `#FFC933` — действие,
бирюза `#2BBFAE` — прогресс и результат, коралл `#FF6B4A` — ошибка, чернила `#0C0F14` — текст;
шрифт Nunito, заголовки 900; радиусы 16–36; теней нет). Здесь — состав и поведение.

## 5.0 Навигация

Пять разделов вместо восьми у эталона. Внутри раздела группировка по задаче пользователя:
«Головне» → «Управління» → «Я перевіряю» → «Результати» → «Налаштування».
Видимость пункта определяется скоупом; счётчики (сколько на проверке, сколько новых) —
в бейджах пункта меню.

> [исправлено пакетом v2, `docs/v2/39-patches.md` П-05] Ранее (без пакета): «Черга перевірки»
> лежала пунктом внутри «Навчання», отдельного раздела «Найм» не было, «Люди» не содержали
> кандидатов. Пакет `docs/v2/` выносит очередь проверки в отдельный раздел (делегирование,
> `docs/v2/37-review-delegation.md`), добавляет раздел «Найм» (рекрутинг, `docs/v2/28`,
> `docs/v2/29`, `docs/v2/30`) и пополняет «Люди», «Звіти», «Налаштування» пунктами пакета.

| Раздел | Пункты |
| --- | --- |
| **Навчання** | Мої завдання · Мої траєкторії · Каталог · Бібліотека модулів (`docs/v2/31`) · Завдання (призначення) · Траєкторії · Запити (на навчання, на спроби) · Коментарі · Звіт про помилки (`docs/v2/36`, `/admin/content-issues`: черга, картка скарги, правила маршрутизації) · Моя історія · Сертифікати · Бейджі |
| **Контент** | Ресурси · Курси · Програми · Тести · Банк питань · Комплексні тести · Практикуми · Опитування · Чек-листи · Оцінки · Очні заняття · Вебінари · Категорії |
| **Люди** | Користувачі · Кандидати (`docs/v2/28`) · Групи · Оргструктура · Функціональні керівники · Імпорт · Офбординг (`docs/v2/33`) · Довідники (міста, посади, рівні, підрозділи, мітки) |
| **Найм** *(виден при включённому етапі `recruiting`, `docs/v2/33`)* | Вакансії (`docs/v2/29`) · Кандидати (`docs/v2/28`) · Співбесіди (`docs/v2/30`) |
| **Перевірка** | Моя черга · Делеговані мною (`docs/v2/37`) · Делеговані мені (`docs/v2/37`) |
| **Звіти** | Готовність змін · По типах контенту · Зведений · Траєкторії · Оцінка · Чек-листи · Воронка найму (`docs/v2/28`) · Етапи (`docs/v2/33`) · Якість контенту (`docs/v2/36`) · Журнали (статуси, звернення, безпека, сесії, сповіщення, імпорт, бонуси) |
| **Налаштування** | Простір · Політики · Ролі · Шаблони сповіщень · Час відправлення · Переклади · Правила автоматизації · Шкали · Компетенції · Інтеграції · API · Статистика · Етапи (`docs/v2/33`) · Статуси кандидатів (`docs/v2/28`) · Сховище (`docs/v2/34`) · Тариф (`docs/v2/35`) |

Раздел «Найм» показывается только тенантам, у которых включён этап `recruiting`
(`docs/v2/33-lifecycle.md`) — тенант, который взял Lola ради обучения, не должен видеть
рекрутинг в меню.

> [исправлено, «Навчання» в боковом меню админки разрослась до 22 пунктов подряд — владелец
> продукта потребовал разделить] Ранее: «Раздел «Інформація» эталона (база знаний, новости,
> объявления, магазин) в Lola не выносится в меню отдельно: база знаний и новости живут в
> «Навчанні», объявления — там же как назначаемый тип, магазин — в профиле. Причина: у эталона
> восемь разделов, и в снятой установке семь из них наполовину пустые (`21` §14.6)».
>
> Причина больше не действует: к 23.09.2026 боковое меню админки (`app/layouts/admin.vue`)
> выросло до 74 пунктов, и «Навчання» одна несла 22 из них подряд, без разделения — не «наполовину
> пустой» раздел, а перегруженный. Меню админки (не мобильная нижняя панель и не `/learn` —
> они не тронуты) переразбито на восемь разделов; там, где в разделе больше 8–10 пунктов, внутри
> есть подзаголовки (не третий уровень меню, просто визуальная подпись над частью списка):
>
> | Раздел | Подзаголовки | Пункты (было в «Навчанні»/«Людях»/«Звітах») |
> | --- | --- | --- |
> | **Навчання** (14) | Призначення · Перевірка · Доступ до каталогу · Оцінювання | завдання, траєкторії, профілі навчання, правила автоматизації, черга перевірки, відповіді на перевірку, запити на спроби, коментарі, заявки на навчання, доступ до каталогу, анкети/цикли оцінки, чек-листи |
> | **Контент** (10) | — | без змін |
> | **Люди** (10) | — | довідник людей, кандидати, вакансії, групи, оргструктура, довідники, імпорт, конфлікти оргструктури, офбординг |
> | **Розвиток** (10, новый) | Профілі та плани · Цілі та заявки | компетенції, профілі посад, плани розвитку, налаштування розвитку, цілі співробітників, матриця, дерево цілей, стратегічні плани, заявки, статуси цілей — раніше тонули в «Людях» |
> | **Інформація** (6) | — | новини, оголошення, прості оголошення, події, дні народження, контакти — були в «Навчанні» |
> | **База знань** (2, новый) | — | база знань, доступ до бази знань — були в «Навчанні» |
> | **Звіти** (11) | Основні звіти · Звіти по модулях | без змін по составу, добавлены подзаголовки |
> | **Налаштування** (12) | Сповіщення та інтеграції · Платформа | без змін по составу, добавлены подзаголовки |
>
> Изначально — 74 пункта, столько же, сколько было; ни один `to:` не потерян, ни одно `show`
> (право доступа) не изменилось. После ребейза на `main` (#104 «Власник», #106 «Вакансія»,
> 23.09.2026) добавился пункт «Вакансії» (`/admin/vacancies`, той же скоуп рекрутингу, что й у
> «Кандидати») — лёг рядом з кандидатами в «Людях» (стало 10), і скоуп `/admin/settings/usage`
> (у «Налаштуванні») змінився з `settings.tenant` на `billing.usage.view` (#104) — склад меню
> актуалізовано, разбивка та ліміти груп не порушені. Разом — 75 пунктів. Таблица выше (было
> записано до правки) оставлена для истории состава по смыслу «эталона»; фактический состав
> бокового меню админки — в этом блоке и в коде.
>
> [дополнено, `gamification` (24.09.2026)] В «Навчанні» появилась подгруппа «Мотивація» (мокапы
> ShopAdmin/Bonuses: крошки «Навчання · Мотивація»): «Магазин подарунків», «Бонуси», «Правила
> нарахування» — 78 пунктов; лимит «не больше 10 в пачке» не нарушен.

**Переключатель активной роли** — в меню профиля, как в эталоне (`01` §1.9.2):
список ролей человека, активная помечена, рядом подпись текущей роли под именем.

Мобильный: нижняя панель из четырёх иконок — Навчання, Каталог, Звіти (если есть скоуп), Профіль.

> [исправлено, `gamification`: по мокапам `31` «Общие элементы» кабинет — «Навчання · Каталог ·
> Бонуси · Профіль»] Ранее: «…Звіти (если есть скоуп), Профіль». Вкладка «Бонуси» ведёт в магазин
> подарков и видна при включённом модуле «Бонуси і магазин».

## 5.1 Вход

- Один экран: поле телефона с маской `+380 __ ___ __ __`, кнопка «Отримати код».
- Второй шаг: шесть ячеек кода, подпись «Код надіслали в Telegram» или «в SMS», таймер
  повторной отправки 60 секунд, ссылка «Не приходить код».
- Ошибка: «Код невірний. Залишилось 2 спроби» — текстом под ячейками, коралловым.
- Если номер в нескольких пространствах — список карточек пространств.
- Внизу — инфоблок гостевой страницы (настраивается тенантом).

## 5.2 Моє навчання (главный экран сотрудника)

- Приветствие с именем, под ним — счётчик «2 завдання · 1 з дедлайном».
- Табы: `Актуальні` · `Прострочені` · `Завершені`.
- Карточка курса: обложка 1:1 слева (или цветная плашка с буквой), название `title-l`,
  метаданные `body-s` (уроков, минут, срок), полоса прогресса, одна кнопка.
- Дедлайн ближе трёх дней — строка «Атестація до 24 вересня» коралловым, иначе приглушённым.
- Пустое состояние: «Курсів поки немає. Керівник призначить навчання» + ссылка на каталог.

## 5.3 Урок

- Шапка: название курса, «Урок 3 з 6», кнопка выхода (сохраняет позицию).
- Тело: блоки контента в одну колонку, ширина текста 60–72 знака.
- Липкий низ: кнопка «Далі» (солнечная), слева — прогресс курса тонкой полосой.
- Кнопка «Далі» неактивна, пока не выполнено условие завершения; подпись объясняет почему
  («Ще 20 секунд», «Подивись відео до кінця»).
- Видео: свой плеер, докрутка запрещена до первого полного просмотра (настройка курса).

## 5.4 Тест

- Один вопрос на экран. Сверху: «Питання 2 з 5», таймер (если есть), последние 60 секунд — коралловый.
- Варианты — блоки с границей, выбранный — бежевая подложка с рамкой чернил.
- После ответа (если `show_answers=after_question`): верный — бирюзовая подложка, неверный —
  коралловая, под ним разбор текстом. Разбор обязателен.
- Навигация: «Далі», «Пропустити» (если разрешено), карта вопросов по свайпу вниз.
- Экран результата: крупная цифра (бирюзовая при зачёте, коралловая при провале), балл,
  разбор ошибок списком, кнопка «Повторити» (если остались попытки) или «До навчання».
- Состояние «На перевірці»: цифра не показывается, вместо неё — «Наставник перевірить
  розгорнуті відповіді. Зазвичай це займає до доби».

## 5.5 Черга перевірки (наставник)

- Список: человек, курс, вопрос, давность («2 години тому»), точка.
- Фильтры: точка, курс, давность. Сортировка по давности, самые старые сверху.
- Карточка проверки: текст вопроса, эталон (если есть), ответ человека, файлы/фото,
  история его попыток. Две кнопки: «Зараховано» / «Не зараховано» + поле комментария.
- После решения — сразу следующая карточка, без возврата в список.
- Просроченные более 48 часов помечаются коралловой точкой.

## 5.6 Призначення (управление)

- Список: название, тип (вручну / автоматично / за каталогом / за профілем), автор, метки,
  создано, старт, дедлайн, сколько людей, статус.
- Создание — шаги в одном экране:
  1. **Що** — курс или программа;
  2. **Кому** — конструктор аудитории: люди, позиции, точки, подразделения, роли, сегмент;
     рядом живой счётчик «Під умову підпадає 38 людей» и ссылка «Переглянути список»;
  3. **Коли** — старт, дедлайн (дата или «14 днів з призначення»), повтор (переаттестация);
  4. **Нагадування** — за сколько дней, в какие каналы, руководителю тоже или нет;
  5. **Параметри** — обязательность, можно ли досрочно, показывать ли в каталоге.
- Отмена назначения показывает, скольких людей это затронет и что будет с начатыми.

## 5.7 Редактор курса

- Слева дерево: модули и уроки, drag-and-drop, кнопки «+ Модуль», «+ Урок».
- Справа — редактор выбранного урока: блоки, добавление через «+», перетаскивание.
- Верх: название, статус (чернетка / опубліковано), кнопки «Попередній перегляд», «Опублікувати».
- Публикация — модалка: чек-лист проверок (зелёные/красные пункты) и обязательный changelog.
- Вкладка «Налаштування курсу»: категория, обложка, теги, язык, строгий порядок уроков,
  сертификат (шаблон, срок действия), видимость в каталоге.

## 5.8 Редактор теста

Две вкладки, как в эталоне:
- **Питання** — состав: добавить из банка (фильтр по категории, метке, сложности),
  создать новый, задать вес, порядок, пометить критическим; счётчик «Кількість запитань».
- **Опис та налаштування** — описание, метки, авторы, изображение карточки, проходной балл,
  лимит времени, число попыток, перемешивание вопросов и вариантов, показ ответов,
  нужна ли ручная проверка, нужно ли очное подтверждение.

## 5.9 Люди

- Таблица: ПІБ, посада, рівень посади, місто, підрозділ, ролі, мітки, дата реєстрації,
  остання активність. Вкладки: Активні / Заблоковані / Усі.
- Карточка человека: профиль, размещения (точка + позиция, история), роли, назначенное,
  история обучения, сертификаты, сессии, кнопка «Надіслати запрошення».
- Импорт: загрузка xlsx/csv → предпросмотр с построчной валидацией → «Застосувати» →
  отчёт (создано / обновлено / ошибок) и файл ошибок.

## 5.10 Отчёты

- Единый каркас: фильтры сверху (период, точка, позиция, курс, статус), KPI-плитки,
  таблица или столбики, кнопка «Вивантажити».
- Правила графиков: максимум два цвета (бирюза — норма, солнце — ниже нормы), каждое
  значение подписано числом, сеток нет.
- Готовность смены — главный отчёт: строки = точки, колонки = позиции, в ячейке процент
  людей с пройденным обязательным; клик проваливается в список людей.

## 5.11 Настройки

- Простір: название, логотип, язык, часовой пояс, тихие часы, инфоблок входа.
- Ролі: список, редактор скоупов, кнопка «Переглянути систему як роль».
- Сповіщення: шаблоны по кодам и каналам, переменные подсказкой, предпросмотр, тест-отправка.
- Профілі навчання: позиция → список курсов со сроками (закрывает массовые назначения).
- Правила автоматизації: триггер → условие → действие, журнал срабатываний.
- Інтеграції: Telegram-бот, SMS-провайдер, вебхуки, API-токены (показ один раз при создании).

## 5.12 Состояния, которые нужно нарисовать для каждого экрана

Загрузка (скелетон, не спиннер) · пусто · ошибка сети с кнопкой «Спробувати ще раз» ·
нет прав · офлайн (баннер «Немає зв'язку, прогрес збережеться») · длинные тексты и
длинные имена · 320px ширина.

## 5.13 Разделы модулей R2–R3

Навигация растёт до восьми разделов, как в эталоне; пункт появляется, когда модуль включён
в настройках тенанта и у человека есть скоуп.

| Раздел | Пункты |
| --- | --- |
| Навчання | + Траєкторії · Очні заняття · Вебінари · Комплексні тести · Запити |
| Контент | + Практикуми · Програми · Шкали оцінки · Бейджі |
| Люди | + Функціональні керівники · Спеціальні звіти · Протокол конфліктів |
| Розвиток | Мій план · Мої цілі · Компетенції · Профілі посад · Цілі співробітників · Заявки |
| Оцінка | Процедури · Чек-листи · Завдання оцінки · Критерії · Шкали · Звіти |
| Хаб | Новини · Оголошення · Події · Wiki · Оргструктура · Форум · Чат · Магазин |
| Для роботи | Робочі задачі · Налаштування задач |
| Звіти | + Зведений звіт · Збережені звіти з розкладом |

Правила те же: одно главное действие на экран, состояние подписано словом, таблицы —
с фильтром сверху и выгрузкой, карточки на мобильном вместо таблиц.

Ключевые новые экраны:
- **Практикум**: задание, поле ответа (текст/файл/фото), история доработок, комментарии тредом.
- **Очное занятие**: карточка с датой, местом, тренером, местами; кнопка записи; QR для отметки.
- **ИПР**: цели карточками со статусами, прогресс по каждой, кнопка «Надіслати на перевірку».
- **Чек-лист смены**: пункты списком с переключателями, поле фото, итоговый процент внизу.
- **Объявление**: модальное окно при входе с кнопкой «Ознайомився» — закрывается только ею.
- **Магазин**: каталог призов, баланс баллов в шапке, история заказов.

## 5.14 Экраны, снятые с эталона (состав и отличия Lola)

### 5.14.1 Карточка назначения

Четыре блока в колонку, как в эталоне (`15` §14.2): **Контент · Налаштування ·
Призначення · Результати**. Каждый блок — карточка с подписью-объяснением и одной кнопкой.
Свёрнутое состояние блока «Налаштування» показывает сводку («Поріг 85%, час необмежений»).

Отличие Lola: блок «Налаштування» доступен сразу, а не после выбора контента —
при пустом контенте показывает общие параметры (сроки, награды) и подсказку,
какие появятся после выбора. В эталоне блок заблокирован, и это сбивает: непонятно,
почему кнопка не нажимается.

### 5.14.2 Параметры назначения

Боковое меню из пяти групп (**Загальне · Термін виконання · Результат · Нагороди ·
Метод призначення**) и форма справа. Набор полей группы «Загальне» зависит от типа
контента — состав снят в `15` §14.3.

Отличие Lola: над формой — строка «Ці правила діють тільки для цього призначення»
с ссылкой на сам контент. Это снимает главную путаницу эталона: люди ищут настройки
теста в тесте.

### 5.14.3 Аудитория назначения

Вкладки **Всі · Призначено (N) · Не призначено**, фильтры
(посада, місто, підрозділ, мітки, рівень, дата реєстрації, спосіб призначення),
таблица с кнопкой в строке и массовым действием по чекбоксам.
Сверху — «Інші способи»: CSV и конструктор аудитории.

Колонку **«Спосіб призначення»** показываем всегда: без неё непонятно, почему
человеку прилетело задание (эталон, `15` §14.4).

### 5.14.4 Конструктор аудитории

Четыре измерения (місто, посада, підрозділ, мітка), у каждого три режима:
«будь-який», «тільки ці», «всі, крім». Под ними — живая строка
«Буде призначено: N людей» со ссылкой «показати список».

Отличается от эталона местом: там конструктор доступен только внутри правила
автоматизации (`17` §14.2), у нас — и в разовом назначении.

### 5.14.5 Редактор траектории

Полотно с блоками Start и Finish, блоки контента перетаскиваются из панели слева.
Клик по стрелке открывает условие перехода (`17` Г-17.1). Справа — параметры
траектории и режим назначения (четыре варианта эталона).

Перед публикацией — кнопка «Перевірити»: недостижимые блоки, пути без Finish, циклы.
Ошибки подсвечиваются прямо на полотне, а не списком внизу.

### 5.14.6 Редактор вопроса

Общий низ у всех семи типов: «Група», «Мітки», «Бали». Верх — конструктор ответа
по типу (`12` §14.6). У множественного выбора — «Скільки варіантів можна обрати»
и «Метод підрахунку балів» (за формулою / все або нічого).
У свободного ответа — **«Підказка для перевіряючого»**, видна только наставнику.

### 5.14.7 Очередь проверки

Одна очередь с двумя режимами переключателем: **«За здачами»** (по умолчанию) и
**«За практикумами»**. В режиме сдач — карточка: кто, откуда, что сдал, сколько ждёт,
кнопки «Взяти в роботу» / «Зарахувати» / «На доопрацювання» / «Не зараховано».
Критерии — чеклистом внутри карточки.

Ответы на свободные вопросы — отдельная вкладка с фильтром по меткам вопросов,
чтобы наставник видел только свою тему (`12` §14.4).

### 5.14.8 Отчёт по типу контента

Четыре части в порядке эталона (`22` §13.7): крупные цифры «Огляд успішності»,
график обращений, таблица «Статистика» из шести строк, таблица людей.
Кнопка «Перерахувати» — в строке человека, с подтверждением и причиной.

### 5.14.9 Объявление с подтверждением

Карточка объявления у сотрудника: текст, вложения, кнопка **«Ознайомився»**
внизу (солнечная, липкая). После нажатия — дата и «Дякуємо».
У руководителя — счётчик «Ознайомились 42 із 117» со списком не подтвердивших
и кнопкой «Нагадати».

### 5.14.10 Магазин и выдача

Витрина — карточки с остатком и стоимостью. После покупки статус заказа виден
и человеку («Готується» → «Можна забрати на точці» → «Видано»), и руководителю точки,
у которого есть экран «До видачі» со списком (`21` Г-21.1).

Сделано (`gamification`, 24.09.2026; `21` §17): кабинет — `/learn/shop` («Магазин подарунків»,
вкладки «Усі» / «Придбані»), `/learn/bonuses` (баланс и история начислений и списаний), плитка
«Бонуси» и карточка «N бонусів · Вистачить на «…» · Магазин» в профиле, вкладка «Бонуси» нижней
панели (с модулем); админка — «Навчання · Мотивація»: `/admin/shop` (товары, «До видачі»,
«Видано», «Скасовано», категории; руководитель точки видит только выдачу своей точки),
`/admin/bonuses` (журнал, «Керування бонусами», «Нарахувати вручну»), `/admin/rewards`
(«Правила нарахування»).

### 5.14.11–5.14.20 Экраны пакета `docs/v2`

> Патч `docs/v2/39-patches.md` П-05. Десять новых экранов, полное описание — в документе
> пакета у каждого модуля; здесь — состав и место в навигации (`5.0`).

**5.14.11 `/candidates`.** Воронка по колонкам статусов (`candidate_statuses`), карточки
кандидатов с ИИ-оценкой и источником; фильтр по вакансии и рекрутеру.
`docs/v2/28-recruiting-candidates.md`.

**5.14.12 `/candidates/:id`.** Карточка кандидата: анкета, резюме, ИИ-оценка собеседования
(вкладка «Співбесіда», `docs/v2/30-ai-interview.md` §5.3), решение о найме, история статусов.
`docs/v2/28-recruiting-candidates.md`.

**5.14.13 `/vacancies`.** Список вакансий тенанта со статусом публикации и числом откликов.
`docs/v2/29-vacancies.md`.

**5.14.14 `/vacancies/:id`.** Карточка вакансии: критерии отбора (шкалы `docs/20-assessment.md`),
публикации, отклики, шаблон параметров назначения. `docs/v2/29-vacancies.md`.

**5.14.15 `/j/:token`.** Публичная страница вакансии без сессии (`docs/v2/29-vacancies.md`
§10, публичный контур `/api/v1/public/*`): карточка вакансии и форма отклика.

**5.14.16 `/library/modules`.** Каталог переиспользуемых модулей контента с версиями,
`usageCount`, предложением модуля из чужого урока (`library_module_proposals`).
`docs/v2/31-module-library.md`.

**5.14.17 `/org-structure`.** Дерево **подчинения** (`org_nodes`) — кто кому подчинён; с
конфликтами оргструктуры (`org_conflicts`). `docs/v2/32-org-structure.md`.

> [исправлено, реализация PR-30: «дерево подразделений и позиций» — это соседний экран
> `/admin/org` (справочники `org_units` и `locations`). `/org-structure` — третья сущность:
> дерево подчинения, источник истины для `resolveManager()`]
> Ранее: «Дерево подразделений и позиций с обнаруженными конфликтами оргструктуры»

Два таба одного дерева: «Співробітник — перегляд» (витрина, `org.structure.view` — у всех
ролей) и «Адмін — конструктор» (`org.structure.edit`). У рядового сотрудника таба
конструктора **нет вовсе** и иконок управления на карточках нет; руководитель правит только
поддерево узлов, где он держатель, и drop за его пределы блокируется подсказкой «Ви можете
змінювати лише свою гілку». Импорт CSV и откат к снимку — PR-31.

**5.14.18 `/offboarding`.** Список и карточка кейсов офбординга: чек-лист, выходное интервью,
закрытие доступа. `docs/v2/33-lifecycle.md`.

**5.14.19 `/storage`.** Потребление хранилища по владельцу и по этапу жизненного цикла
(переключатель «За категорією» / «За етапом»), заявки на удаление. `docs/v2/34-storage.md`.

**5.14.20 `/settings/lifecycle`.** Настройка восьми платформенных этапов жизненного цикла и
их возможностей (`capabilities`) для тенанта. `docs/v2/33-lifecycle.md`.
