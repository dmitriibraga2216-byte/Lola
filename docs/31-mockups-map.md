# Lola LMS — мокапы экранов (19.09.2026) и карта соответствия

Пакет `lola-mockups` (90 экранов: 18 на телефоне для линейного персонала, 72 на десктопе
для администратора, включая панель оператора) лежит в `docs/mockups/` — `index.html`
(обзор по разделам) и `screens/<Имя>.html` (по экрану). Мокапы сделаны по утверждённому
бренд-буку и являются **визуальной спецификацией**: каждый экран Lola собирается по своему
мокапу. Где мокап расходится с текстом ТЗ, побеждает мокап (он новее); расхождение и
решение фиксируются в `28`. Данные в мокапах (имена, точки, цифры) — примеры, они не
переносятся; тексты интерфейса берутся из мокапов дословно в `i18n/locales/uk.json`.

**Ре-аудит 21.09.2026** (ветка `reaudit-31`, main = 8faa307): для каждой строки проверено
наличие страницы в `app/pages/**` и присутствие 5–8 заметных текстов мокапу в
`i18n/locales/uk.json`. Итог по 84 строкам: **59 ✅ · 24 🟡 · 2 ❌**. Изменения к предыдущей
версии (была сверена в debts-9, #63): шесть строк ошибочно числились «нет экрана», хотя
страница уже была сделана в более ранних PR — `StudyHistory` (`/learn/profile/study-history`,
#47), `AttemptRequests` (`/admin/attempt-requests`, #36), `Comments` (`/admin/comments`, #51),
`OrgConflicts` (`/admin/org-conflicts`, #45), `LearningRequests` (`/admin/catalog/requests`,
#51), `NotificationSchedule` (вкладка «Час відправлення» в `/admin/settings/notifications`,
#64) — все переведены в ✅, тексты найдены дословно. Одна строка, наоборот, понижена:
`ChecklistReport` фактически показывает не разрез «по пунктах / по людях / по точках» из
мокапу, а панель проходжень/дисципліни/mystery shopper — переведена ✅ → 🟡.

**22.09.2026 (#69, `answers-1`):** `ChecklistReport` собран по мокапу (разрез «по пунктах / по
людях / по точках», см. строку ниже) — 🟡 → ✅. Итог по 84 строкам: **29 ✅ · 53 🟡 · 2 ❌**.

**22.09.2026 (`screens-3`):** точечно добраны шесть строк админки. `Main` — исправлен
дословный текст колонки (`assign.col.people`: «Людей» → «Люди»). `TaskCard` — в шапку
картки добавлены «Зберегти» / «Відхилити зміни» (docs/15 §14.2): вибір компетенцій тепер
редагується інлайн з відстеженням «незбереженого», а не миттєво в модалці. `ContentCourses`,
`CoursePlan`, `ContentTests`, `ContentPolls` — проверены построчно по извлечению заметных
текстов мокапу (h1–h3, кнопки, капслок-чипи, `scripts/mockupText.ts`): все совпадают
дословно, кроме «Імпортувати» (свідомо не додано — рішення Q-03, docs/34). Все шесть —
🟡 → ✅. `ContentWorkshops` и `ContentComplexTests` дособраны (шапка `PageHeader` + «Додати»,
колонки за мокапом), но остались 🟡 — «Автор» и «Мітки»/«Теми» не показуються дослівно:
дані відсутні на сервері (немає join на users, немає колонки `tags` у `workshops`, немає
поняття «тема» у `complex_tests`), серверна робота поза межами verstka-PR. Итог по 84
строкам: **35 ✅ · 47 🟡 · 2 ❌**.

**22.09.2026 (`screens-5`):** ре-аудит блока «розвиток та оцінка» (10 строк). `Assessments`,
`Assessment`, `Checklists`, `CriteriaGroups` фактически уже полностью собраны по мокапам (сделано
раньше в #66, строки не были обновлены) — 🟡 → ✅. `Competencies` и `PositionProfile` доведены
точечно: у `Competencies` добавлена таблица-бібліотека по мокапу (групування по категорії) и
фільтр «Група», у `PositionProfile` таблицю вимог перейменовано в «Компетенції та норма» с
групуванням по категорії — обе остаются 🟡 из-за данных, которых нет на сервере (см. остаток в
строках). `DevelopmentPlans` не сделан: это другая сущность (`development_plans` — план с
периодом и кроками-цілями), для админского списка по всій команді сервер не даёт ендпоінта (є
лише `myPlan` для однієї людини) — без серверной логики экран не собрать, статус не менялся.
Итог по 84 строкам: **39 ✅ · 43 🟡 · 2 ❌**.

Три коммита после debts-9 (`debts-8` #64, `debts-4` #65, `debts-6` #66) правили ещё восемь
экранов (`Trajectory`, `QuestionEditor`, `Assessments`, `PositionProfile`, `ReportTest`,
`Settings`, `Roles`, `Profile`, `Login`) — изменения там аддитивные (preview-as, вага
оцінювачів, кілька посад у профілі, drag-n-drop полотна, звіт по іншим типам), статус этих
строк не изменился. Остальные строки без изменений кода/переводов с debts-9 — статус
подтверждён точечной проверкой, тексты и страницы совпадают с уже записанным.

**22.09.2026 (`screens-6`):** точечно добраны тринадцать строк налаштувань, звітів, журналів
і черг перевірки. `AutomationRules`, `AutomationRule`, `Trajectories` — вже були зроблені
дослівно, лише виправлено мапування рядка `Trajectories` (`/admin/programs` →
`/admin/trajectories`, помилка попереднього ре-аудиту): 🟡 → ✅. `ReviewQueue` — додано підпис
«у черзі» під часом очікування (раніше показувалась лише цифра годин): 🟡 → ✅. `ReviewAnswers`,
`SessionsLog`, `PlatformTenants` (крім двох виправлених дослівних текстів — заголовок колонки
й кнопка створення тенанта) вже були зроблені дослівно, рядок не оновлювався: 🟡 → ✅.
`Certificates` — виправлено дослівний текст колонки («Остання видача» → «Дата зміни»): 🟡 → ✅.
`CustomReports` — сторінка була одразу редактором без списку: додано список зверху за
мокапом (колонки, «Побудувати», «Створити звіт»): 🟡 → ✅. Три рядки лишились 🟡 — доробки
зроблено, але залишок вимагає серверної роботи поза межами verstka-PR: `NotificationTemplates`
(колонки E-mail/Telegram — дефолтний стан каналу на рівні коду не змодельований),
`NotificationTemplate` (вкладки/заголовки додані), `Integrations` (SMTP-поля підписано
дослівно; групи `Аутентифікація`/`Джерела користувачів`/`Вебінари`/`Google Analytics`/
`AI provider` і частина полів SMTP на сервері не існують — нова функціональність, не вёрстка).
После ребейза на `screens-5` (ре-аудит блока «розвиток та оцінка», см. абзац выше) итог по
84 строкам: **59 ✅ · 24 🟡 · 2 ❌**.

**22.09.2026 (`screens-7`):** закрыты небольшие серверные пробелы (одне поле в відповіді або
простий join) під сім рядків, доведено до мокапу. `People` — лічильники в чипах `Активні ·
Заблоковані · Усі` (`listPeople` рахує `counts` по тим же фільтрам, крім вкладки): 🟡 → ✅.
`MyTrajectory` — «Затримка N днів» у назві кроку (сервер уже рахував `days`/`firesAt`,
`TrajectoryLadder.vue` їх не використовував); шлях у таблиці виправлено на
`/learn/trajectories/:id` (`/learn/programs` — інша сутність, docs/17 Г-17.3): 🟡 → ✅.
`DevelopmentPlanMobile` — наставник плану (`mentor_id` доданий міграцією 0052, `[рішення]`
docs/19 §3.4): 🟡 → ✅. `PositionProfile` — приклад «Відповідність профілю N% — ПІБ»
(`profileCoverage` тепер віддає `perPerson`): 🟡 → ✅. `ContentWorkshops`,
`ContentComplexTests` — колонка «Автор» через join на users; лишились 🟡 через «Мітки»/«Теми»
(даних і визначень немає, не вигадуємо). `Competencies` — колонка «СТВОРЕНО»; лишився 🟡 через
фільтр «Мітки» (немає поля тегів). Свідомо не торкались (окремі задачі, не «одно поле/join»):
`DevelopmentPlans` як адмінська сутність, дерево `Goals`, групи налаштувань
`NotificationTemplates`/`Integrations` (`screens-6`), `content_ratings` для `Knowledge` (D-042,
R2) — докладніше `33` §В. Итог по 84 строкам: **63 ✅ · 20 🟡 · 2 ❌**.

**23.09.2026 (`screens-8`):** закрыт небольшой остаток восьми строк. `MyTasks` — `kind`
(`test`/`course`) у `/learning/my`, підпис «Пройти» для курсу-обгортки з єдиним уроком-тестом.
`Catalog` — картка веде на попередній перегляд «Детальніше», запис/заявка — кнопка в панелі.
`Lesson` — власний плеєр відео поверх нативного `<video>`. `Workshop` — прибрана окрема кнопка
«Зберегти чернетку» (чернетка зберігається автоматично). `Assessment360` — кроковий режим по
групі критеріїв з прогресбаром. `Meetup` — «Скасувати запис» стала липкою кнопкою на всю
ширину. `PersonCard` — «Рейтинг» у зведенні «Навчання» (`personLearning` перевикористовує
розрахунок `studyHistory`). `Import` — права колонка «Що буде зроблено»/«Помилки в рядках» за
мокапом. Всі вісім — 🟡 → ✅. Свідомо лишились 🟡: `TestResult`/`Profile` (плашка бонусів, R3,
`33` D-069), `ContentWorkshops`/`ContentComplexTests` («Мітки»/«Теми» — немає `tag_scope` під
практикуми і поняття «тема» у складі комплексного теста, `33` D-074/D-075). Итог по 84 строкам:
**71 ✅ · 11 🟡 · 2 ❌** (пересчитано по фактическим маркерам строк).

**Добавление `spec-development` (23.09.2026).** Закрыты два долга «окрема задача», сознательно
не тронутых в `screens-7` (см. абзац выше): `DevelopmentPlans` — адмінський список планів
(`GET /development/plans`, `?tab=`, область видимості за скоупом) і картка з редагуванням;
`Goals` — дерево «Стратегічний план» на `development_goals` (`parent_id`/`is_strategic`, докс/19
§14.4 `[рішення]`), той самий життєвий цикл статусів і протокол, що й у цілей ІПР. Обидва
рядки 🟡 → ✅. Итог по 84 строкам: **73 ✅ · 9 🟡 · 2 ❌**.

## Общие элементы

- **Админка (1440)**: левая колонка 236px — четыре брендовых квадрата вместо логотипа;
  пять разделов `Навчання · Контент · Люди · Звіти · Налаштування`, у активного раздела
  подпункты (`Main`: Завдання · Траєкторії · Черга перевірки · Каталог); внизу карточка
  человека с инициалами и активной ролью (`01` §1.9.2). Шапка страницы: хлебные крошки,
  заголовок 900, справа одно солнечное действие и при необходимости «Відхилити зміни».
  Фильтры и вкладки — чипы со счётчиками, активный чип чернильный. Списки — таблицы
  с колонками ЗАГЛАВНЫМИ, строки-карточки. Правая колонка 300px для сводок и параметров.
- **Кабинет (390×844)**: без шапки, заголовок 28px/900, нижняя панель `Навчання · Каталог ·
  Бонуси · Профіль`, липкая солнечная кнопка внизу у сценариев прохождения, стрелка «←»
  и прогресс «3 з 5» у урока/теста.
- **Цвета состояний**: солнце — действие и «В ПРОЦЕСІ»; бирюза — результат и «ВИКОНАНО»;
  коралл — «ПРОВАЛЕНО», «Протерміновано», «Критично»; приглушённый — «НЕ РОЗПОЧАТО».
- **Подсказки-обоснования** (жёлтые/красные плашки в мокапах: «Ці правила діють тільки для
  цього призначення», «Файл застосовується цілком або ніяк», «Баланс — не збережене поле…»)
  переносятся в интерфейс как есть — это тексты для пользователя, а не комментарии дизайнера.

Обозначения: ✅ соответствует · 🟡 частично · ❌ не сделан или не по мокапу · PR — очередь `30` §4;
`shell` — PR `ui-shell` (каркас), R3 — не в R1.

## Телефон · сотрудник (18)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `Login` | `/login` шаг кода | ✅ (visual-mockups) шість комірок коду, таймер «Надіслати ще раз через N с», «Не приходить код», підвал з контактом — усі тексти мокапу знайдені автотестом; мокапа кроку телефону нема, перевірено лише структурно (docs/28) | shell |
| `MyTasks` | `/learn` | ✅ (23.09, `screens-8`) чипы со счётчиками, карточки, секції ПРОСТРОЧЕНО/ЦЬОГО ТИЖНЯ/ПІЗНІШЕ — є; кнопка дії не розпочатого завдання — «Відкрити» для курсу/ознайомлення/практикуму, «Пройти» для тесту. `myLearning` тепер рахує `kind` (`test` — курс-обгортка з єдиним обов'язковим уроком-тестом, інакше `course`) окремим запитом по `modules`/`lessons`, клієнт лише обирає підпис | shell, 8 (kind) |
| `EmptyState` | `/learn` пусто | ✅ (22.09, `screens-1`) текст «Завдань поки немає» + «Відкрити каталог» — дослівно | shell |
| `Catalog` | `/learn/catalog` | ✅ (23.09, `screens-8`) вкладки «Завдання / Траєкторії», пошук, обкладинка-плашка, бейдж «Вільний доступ / За заявкою» — є; картка тепер веде на попередній перегляд («Детальніше» — модалка з назвою/описом/метою), сам запис/заявка/відкриття ресурсу — кнопка всередині панелі (`enrollTask` викликається з панелі, заявка й самозапис не змінились); e2e `learner.spec.ts` (тести 2/3/4) оновлені під новий крок | 20 |
| `Lesson` | `/learn/:e/:lesson` | ✅ (23.09, `screens-8`) шапка «← · N з M», підпис розділу, «Часта помилка», низ «Ще N секунд · Далі», плашка «Відео · тривалість» — є; відеоплитка тепер власний плеєр поверх нативного `<video>` без `controls` (плей/пауза, перемотка `<input type=range>`, час) — без бібліотек, клавіатура (пробіл/стрілки) | 8 |
| `Test` | `/learn/quiz/:id` | ✅ (22.09, `screens-1`) «Питання N з M», таймер-пігулка, підпис «Одна правильна відповідь · N бали», варіанти-картки, «Пропустити / Відповісти» — усі тексти співпадають дослівно; колір вибраного варіанта виправлено на сонячний (був фоновий, не бренд-токен) | 7 |
| `TestResult` | результат теста | 🟡 (22.09, `screens-1`; screens-8: перевірено, осознанно без змін) картку переробили за мокапом: нейтральний фон з кольоровою рамкою і круглим бейджем-іконкою (✓/✕) замість суцільної заливки; «Поріг · бали · спроба», «Помилки — N» з «Ви відповіли … правильно —», «До навчання» — тексти співпадали й раніше; залишок — плашка бонусів, R3 (`33` D-069) | 7 |
| `Poll` | `/learn/surveys/:id` | ✅ (22.09, `screens-2`) звірено дослівно: kicker «ПИТАННЯ N З M · АНОНІМНО» (текст-трансформ у CSS), варіанти-картки з точкою, «або Свій варіант відповіді», липка сонячна «Далі», шкала і чекбокси на токенах — усе вже було зроблено раніше, цим пакетом лише перевірено | 16 |
| `Workshop` | `/learn/workshop/:id` | ✅ (23.09, `screens-8`) нумерований список критеріїв з бірюзовими цифрами, фото-плитки, `<label>` «Коментар для наставника», «Відправити на перевірку» — є; окрема кнопка «Зберегти чернетку» прибрана (мокап показує лише кінцеву кнопку) — чернетка й далі зберігається автоматично: при виході з поля коментаря (як і раніше) і тепер також при доданні/видаленні фото | 7 |
| `Checklist` | `/learn/checklists/:id` | ✅ (22.09, `screens-1`) «N з M балів · %», пункти ✓/✕/? кольором, «Завершити перевірку» — дослівно; виправлено «Вага N» (було з малої літери «вага») і додано `text-transform: uppercase` для плашки «Коментар обов'язковий», щоб вона виглядала як у мокапі | 16 |
| `Assessment360` | `/learn/assessment/:id` | ✅ (23.09, `screens-8`) «Ви оцінюєте … як колега», попереджувальна картка, шкала 0–5, kicker-заголовок групи — є; доданий кроковий режим (степпер): одна група критеріїв на екран, прогресбар зверху, «Далі» веде до наступної групи (на останній — до підсумку), «Назад» — до попередньої; подана/протермінована оцінка лишається переглядом усіх груп одразу (без кроків, для читання). Поле «Н/З» і коментарі — понад мокап, не прибирали | 16 |
| `Meetup` | `/learn/meetups/:id` | ✅ (23.09, `screens-8`) бейдж «Ви записані», «Записано N з M» з прогресбаром місткості, дата/місце/тренер — є; QR на екрані учасника з мокапу — свідома розбіжність, ухвалена замовником 22.09.2026 (`33` D-028, `34` Q-07): код лишається на екрані тренера (безпечніше); «Скасувати запис» тепер липка кнопка на всю ширину (`.sticky-bottom`, над нижньою панеллю вкладок каркасу `learner`), а не чіп серед інших дій — інші функції сторінки (вебінар, матеріали, учасники) понад один екран мокапу лишились як є | 14 |
| `MyTrajectory` | `/learn/trajectories/:id` (шлях у таблиці був помилковий — програми (`/learn/programs`) і траєкторії (`/learn/trajectories`, `screens-6`) різні сутності, docs/17 Г-17.3) | ✅ (22.09, `screens-2`; screens-7: «Затримка N днів» ✅) додано відсоток і прогресбар до «Пройдено N з M», лінія-таймлайн між кружками кроків, стрілка для активного кроку (замість номера); «Затримка N днів» — вузол `delay` вже повертав `days` і `firesAt` (`myTrajectory`, server/services/trajectories.ts), але `TrajectoryLadder.vue` не показував кількість днів у назві кроку — тепер назва завжди «Затримка N днів» / «Закриття доступу N днів» від сервера, дата розблокування («Відкриється {дата}») показувалась і раніше. «Ще не призначено» — немає такого статусу кроку, не показуємо (без змін) | 9 |
| `DevelopmentPlanMobile` | `/learn/development` | ✅ (22.09, `screens-2`; screens-7: наставник плану ✅) додано картку «Ціль плану» (мітка + велика ціль + період `дд.мм.рррр — дд.мм.рррр`), дати цілей тепер людяні («до 30 вересня» замість ISO); наставник плану — у схемі `development_plans` не було поля (він є лише на рівні окремої цілі, docs/19 §3.5), додано `mentor_id` міграцією 0052 `[рішення]` (docs/19 §3.4) за аналогією з ціллю, призначає керівник (`POST /development-plans/:id/mentor`) — показуємо поруч з періодом «· наставник ПІБ» | 17 |
| `Notice` | объявление с подписью | ✅ (22.09, `screens-1`) бейдж «Ознайомлення» (виправлено з сонячного на бірюзовий колір — мокап), «Опубліковано · діє до», файл-картка, липка «Ознайомився» з поясненням — усі тексти вже були дослівно | 11 |
| `StudyHistory` | `/learn/profile/study-history` | ✅ (ре-аудит 21.09.2026; сделан ранее в #47, строка была не обновлена) «Моя історія навчання», «Поточний рейтинг», график «Мій рейтинг / Зовнішній» столбиками, список с датой и статусом («виконано»/«провалено») | 17 |
| `Shop` | магазин (нет) | ❌ R3 | R3 |
| `Profile` | `/learn/profile` | 🟡 (22.09, `screens-1`; screens-8: перевірено, осознанно без змін) ініціали, посада · точка, «Мої сертифікати», тижнева активність — є; плитки «Рейтинг» (`currentRating` з `/me/study-history`) / «Бонуси» (заглушка «—», R3) / «Виконано» — назви як у мокапі; залишок: «Магазин» серед посилань і реальний баланс бонусів — R3 (`33` D-069) | shell (каркас), 17 (рейтинг), R3 (бонусы) |

## Десктоп · навчання (14)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `Main` | `/admin/assignments` | ✅ (22.09, `screens-3`) заголовок, кнопка «Додати призначення», чипи, колонки НАЗВА/ТИП/АВТОР/СТВОРЕНО/ЛЮДИ/СТАН — виправлено `assign.col.people` («Людей» → «Люди», дослівно з мокапу); банер «змінено» і чіп «ВИКОНАНО» умовні, не зустрілись на тестовій фікстурі (не дефект, docs/28) | 6 |
| `TaskCard` | `/admin/assignments/:id` | ✅ (22.09, `screens-3`) четыре блока (Контент · Налаштування · Призначення · Результати) и кнопки есть (#35); «Зберегти» / «Відхилити зміни» (docs/15 §14.2) додані в шапку картки — вибір компетенцій тепер відстежує «незбережене» замість модалки з миттєвим збереженням | #35 |
| `TaskParams` | `…/params` | ✅ (Spec 15, докс/33 D-066) боковое меню пяти групп, плашка «Ці правила діють тільки для цього призначення», сегменты, тумблеры с пояснениями | 6 |
| `TaskAudience` | `…/audience` | ✅ (Spec 15, докс/33 D-066) чипы `Усі · Призначено · Не призначено`, фильтры-чипы, «Призначити вибраним / через CSV», «Вибрано N із M», колонка «Спосіб» | 6 |
| `AutomationRules` | `/admin/rules` | ✅ (22.09, `screens-6`) колонки Назва · Аудиторія · Відкладення · Використання · Створено · Опубліковано — усі дослівно, перевірено побудово | 9 |
| `AutomationRule` | `/admin/rules/:id` | ✅ (22.09, `screens-6`) плашка «Коли спрацьовує», чотири вимірювання сегментами «Будь-який · Тільки ці · Усі, крім», справа «Буде призначено N · Показати список», «Відкладене призначення», «Використовується для» — усе дослівно | 9 |
| `Trajectories` | `/admin/trajectories` | ✅ (22.09, `screens-6`) виправлено мапування рядка — мокап відповідає списку траєкторій (`/admin/trajectories/index.vue`), не `/admin/programs`; колонки Назва · Склад · Режим призначення · Людей · Дата зміни · Опубліковано вже були зроблені дослівно | 9 |
| `Trajectory` | `/admin/trajectories/:id` | ✅ (Spec 17, докс/33 D-066: мапінг на `/admin/programs/:id` був помилковий — це лінійний режим програм, граф-редактор живе на `/admin/trajectories/:id`) палитра блоков (Завдання, Комплексний тест, «І», «АБО», затримка, закриття доступу, розгалуження), полотно с узлами, «Режим призначення» справа, «Перевірити» | 9 |
| `ReviewQueue` | `/admin/review-workshops` | ✅ (22.09, `screens-6`) чипы `Здачі практикумів · Відповіді на перевірку · Чек-листи`, карточки з інціалами, «N год у черзі» (додано підпис «у черзі», раніше показувалась лише цифра) — коралова рамка на простроченій, «Взяти в роботу» | 7 |
| `ReviewAnswers` | `/admin/review` | ✅ (22.09, `screens-6`) чипы `Неперевірені · Перевірені · Усі`, фильтры «мітка питання · Поза програмами · Поза курсами · Точка», карточка відповіді з питанням і «Бал N з N · Зарахувати / Не зараховано» — усе вже було зроблено дослівно | 7 |
| `LearningRequests` | `/admin/catalog/requests` | ✅ (ре-аудит 21.09.2026; сделан ранее в #51, строка была не обновлена) «Прийом заявок на навчання», вкладки `Завдання · Траєкторії навчання`, колонки Завдання · Людина · Дата заявки · Стан, «Схвалити / Відхилити» | 20 |
| `AttemptRequests` | `/admin/attempt-requests` | ✅ (ре-аудит 21.09.2026; сделан ранее в #36, строка была не обновлена) «Запити на спроби тестування», колонки ТЕСТ · ЛЮДИНА · ВИКОРИСТАНО · ДАТА · СТАН, чип «Очікує», «Дати спробу» | 7 |
| `Comments` | `/admin/comments` | ✅ (ре-аудит 21.09.2026; сделан ранее в #51, строка была не обновлена) «Коментарі», фільтри Джерело/Стан, колонки Джерело · Автор · Дата · Коментар, стани «Не прочитано / Прочитано» | 20 |
| `Certificates` | `/admin/certificates` | ✅ (Spec 24, докс/33 D-066; 22.09 `screens-6` виправлено дослівний текст колонки `certificates.lastIssued`: «Остання видача» → «Дата зміни», як у мокапі) колонки Назва · Формат · Видано · Термін дії · Дата зміни · Опубліковано; чипа «Бейджі» нема — рішення в коді: бейджі R3, вкладки не буде | 12 |
| `Scales` | `/admin/settings/scales` | ✅ (Spec 24, докс/33 D-066) чипы `Шкали завдань · Шкали анкет`, диапазоны и уровни карточками | 12 |

## Десктоп · контент (10)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `ContentResources` | `/admin/resources` | ✅ (Spec 11, докс/33 D-066) «Знайдено N», фильтры Тип · Автор · Мітки · Категорія · Опубліковано, колонки Назва · Тип · Автор · Мітки · Дата зміни · Опубліковано | 8 |
| `ResourceForm` | `/admin/resources/:id` | ✅ (Spec 11, докс/33 D-066) тип `Сторінка · Адаптивна сторінка · Файл · Посилання`, редактор, «Дозволити друк», «Сповістити про оновлення», справа «Публікація» (статус, версія) и «Доступ і категорії» | 8 |
| `ContentCourses` | `/admin/courses` | ✅ (22.09, `screens-3`) колонки Назва (Код · розділи, елементи) · Тривалість · Результат по · Автор · Дата зміни · Опубліковано — усі тексти дослівно збігаються; «Імпортувати» свідомо не додано (рішення Q-03, docs/34: формат файлу не описаний, кнопку прибрали з усього контенту) | 8 |
| `CoursePlan` | `/admin/courses/:id` | ✅ (22.09, `screens-3`) библиотека слева с фильтрами и «Знайдено», план справа з «Розділ N», три кнопки («Зберегти», «Створити і підключити ресурс», «Створити та підключити тест», «Додати розділ»), «Поріг N%» у теста — усі дослівно | 8 |
| `ContentTests` | `/admin/quizzes` | ✅ (22.09, `screens-3`) колонки Назва · Питань · Автор · Мітки · Дата зміни · Опубліковано — усі дослівно; «Імпортувати» не додано (Q-03) | 7 |
| `QuestionEditor` | `/admin/questions/:id` | ✅ (Spec 12, докс/33 D-066) чипы семи типов, текст, варианты-карточки с чекбоксом, справа «Група питань · Скільки варіантів · Метод підрахунку · Бали · мітки» | 7 |
| `ContentPolls` | `/admin/surveys` | ✅ (22.09, `screens-3`) колонки Назва · Питань · Режим · Приватність · Дата зміни · Опубліковано — усі дослівно; «Імпортувати» не додано (Q-03) | 16 |
| `ContentWorkshops` | `/admin/workshops` | 🟡 (22.09, `screens-3`; screens-7: «Автор» ✅; screens-8: перевірено, осознанно без змін) шапка через `PageHeader` + «Додати», колонки Назва · Автор · Критеріїв · Мітки · Дата зміни · Опубліковано додані дослівно; «Імпортувати» не додано (Q-03). Залишок: «Мітки» лишається «—» — у таблиці `workshops` немає колонки `tags`, і жодного `scope` під практикуми в `TAG_SCOPES` (shared/enums.ts); вигадувати новий scope мовчки не можна (CLAUDE.md п.13) — питання замовнику (`33` D-074) | 7 |
| `ContentComplexTests` | `/admin/meetups/complex` | 🟡 (22.09, `screens-3`; screens-7: «Автор» ✅; screens-8: перевірено, осознанно без змін) шапка через `PageHeader` + «Додати», колонки Назва · Склад · Теми · Автор · Дата зміни · Опубліковано додані дослівно; «Імпортувати» не додано (Q-03). Залишок: «Теми» — у складі комплексного теста немає поняття «тема» (лише частини-тести `parts`), чи це мітки складових тестів чи окреме поле не визначено в ТЗ — питання замовнику, не вигадано мовчки (`33` D-075) | 7 |
| `ContentPrograms` / `ContentMeetups` / `ContentCategories` | программы, занятия/вебинары, категории | 🟡 списки по шаблону; категории — карточки с числом ресурсов и перетаскиванием | 9 / 14 / 12 |

## Десктоп · люди (9)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `People` | `/admin/people` | ✅ (ре-аудит 22.09.2026, screens-4; screens-7: лічильники ✅) колонки за умовчанням вирівняні з мокапом (Посада · Рівень · Підрозділ · Ролі · Прийнято · Активність), під ПІБ — e-mail; лічильники в чипах `Активні · N · Заблоковані · N` — `listPeople` рахує `counts` (active/blocked/all) по тим самим фільтрам, крім вкладки. Залишок (косметика): фільтри лишились за кнопкою «Фільтри», а не окремими чипами зверху | 15 |
| `PersonCard` | `/admin/people/:id` | ✅ (ре-аудит 22.09.2026, screens-4; screens-8: «Рейтинг» ✅) вкладки, поля «Дата призначення поточної посади» і «Прихований», зведення «Навчання»/«Безпека»/«Ролі» — вже були. «Рейтинг» (screens-8): `personLearning` тепер повертає `currentRating` — той самий розрахунок, що й «Поточний рейтинг» у «Мій розвиток» (`studyHistory`, кількість завершених курсів/програм/тестів), а не окрема геймифікація | 15 |
| `UserGroups` | `/admin/people/groups` | ✅ (ре-аудит 22.09.2026, screens-4) чипи `Усі · 40 · З оргструктури · Створені вручну`, «Походження» — окрема плашка на картці групи | 15 |
| `OrgStructure` | `/admin/org` | ✅ (ре-аудит 22.09.2026, screens-4) дерево зліва зі счётчиками — було; додано праворуч картки співробітників обраного підрозділу/точки (ПІБ + посада, лінк на людину), заголовок показує назву обраного вузла | 15 |
| `Dictionaries` | `/admin/refs` | ✅ (ре-аудит 22.09.2026, screens-4) чипи довідників — були; перелік переоформлено карткою з підписом-лічильником зліва від кожного запису і кнопкою «Додати» (за мокапом) замість завжди відкритого поля вводу | 15 |
| `Tags` | метки (в refs) | ✅ (ре-аудит 22.09.2026, screens-4) уже повністю за мокапом: «Керування мітками», колонки Мітка · Область дії · Опис · Використань, чипи-фільтр по області | 15 |
| `Import` | `/admin/import` | ✅ (ре-аудит 22.09.2026, screens-4; screens-8: права колонка ✅) вкладки джерел `CSV · API · Active Directory · Azure AD · HRM`, CSV-флоу — є. Права колонка (screens-8): «Що буде зроблено» (підсумок створити/оновити/пропустити/помилок + «Застосувати») і «Помилки в рядках» (перші 5 рядків з помилками) — окремі картки праворуч (`grid`, згортається в один стовпець до 900px), як у мокапі; існуюча таблиця всіх рядків нижче лишилась без змін (робочий e2e-флоу), `tests/e2e/admin.spec.ts` тест 7 — `.first()` там, де текст помилки тепер видно і в таблиці, і в бічній картці | 15 |
| `ImportLog` | протокол импорта (в journals) | ✅ (ре-аудит 22.09.2026, screens-4) колонки Дата · Джерело · Хто запустив · Створено · Оновлено · Помилок · Стан — вже були; заголовок вкладки виправлено на «Протокол імпорту користувачів» (було «Журнал імпорту») в `i18n/locales/*.json` — сам `app/pages/admin/journals.vue` поза межами цієї задачі | 15 |
| `OrgConflicts` | `/admin/org-conflicts` | ✅ (ре-аудит 21.09.2026; сделан ранее в #45, строка была не обновлена) «Протокол конфліктів в оргструктурі», колонки Дата · Людина · Конфлікт · Деталі · Стан, «Вирішено / Не вирішено» | 15 |

## Десктоп · розвиток та оцінка (10)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `Competencies` | `/admin/development/competencies` | 🟡 (22.09.2026, `screens-5`; screens-7: колонка «СТВОРЕНО» ✅) додано зверху таблицю-бібліотеку за мокапом — НАЗВА(категорія)·ІНДИКАТОРИ(компетенції в ній)·ГРУПА(тип)·ІНДИКАТОРІВ·СТВОРЕНО, і фільтр «Група»; нижче лишилось наявне управління окремими компетенціями (мокап його не показує). Колонка «СТВОРЕНО» (screens-7): `/competencies` тепер віддає `createdAt`, у групі — дата останньої доданої компетенції. Залишок: фільтра «Мітки» немає — у компетенції немає поля тегів, новий scope мовчки не вигадуємо (CLAUDE.md п.13) | 17 |
| `PositionProfile` | `/admin/development/profiles` | ✅ (22.09.2026, `screens-5`; screens-7: приклад «Відповідність профілю N% — ПІБ» ✅) «Цілі посади», «Обов'язки посади», кілька посад, «Використовувати рівні посади» вже були; таблицю вимог перейменовано в «Компетенції та норма» (докс), рядки згруповані по категорії компетенції як у мокапі. Приклад «Відповідність профілю N% — ПІБ» (screens-7): `profileCoverage` тепер віддає `perPerson` (по кожній людині — `userId`, `fullName`, `percent`, `fit`), відсортовано за відсотком | 17 |
| `DevelopmentPlans` | `/admin/development/plans` | ✅ (23.09.2026, `spec-development`) закрито серверний пробіл, знятий у `screens-5`/`screens-7`: `GET /development/plans` (`?tab=active\|inactive\|done`, область видимості за скоупом) і `GET/PATCH /development/plans/:id` під адмінську таблицю Людина·Ціль·Період·Кроків·Прогрес·Стан з чипами `Активні · Неактивні · Виконані`; клік по рядку — картка плану з кроками-цілями, редагуванням періоду/цілі, наставником і переходом статусу | 17 |
| `Goals` | `/admin/development/strategic` | ✅ (23.09.2026, `spec-development`) дерево «Напрямок → цілі» з власником, строком, довідником «Статуси цілей» і «Протоколом» — на `development_goals` (`parent_id`/`is_strategic`, докс/19 §14.4 `[рішення]`, не окрема таблиця), той самий життєвий цикл і `goal_status_log`, що й у цілей ІПР; колишній редактор `strategic_plans` (період/бюджет/KPI, докс/19 §3.8) переїхав на `/admin/development/strategic-plans` без втрати функціоналу | 17 |
| `ExternalRequests` | `/admin/development/requests` | ✅ (22.09, `screens-2`) додано: підказка маршруту «Маршрут: керівник точки підтверджує потребу, HR або бюджет — оплату», чипи-лічильники `Зовнішнє навчання · N` / `Кар'єрний розвиток · N` замість select-фільтра, під іменем людини — «Посада · Точка» (мінімальне поле в `requestsTable()`: join по `user_placements`/`positions`/`locations`, без міграції), колір бейджа проміжних станів — сонячний (мокап); колонки Тема · Людина · Вартість · Відповідальний · Дата · Стан вже були | 17 |
| `Assessments` | `/admin/assessment/…` | ✅ (ре-аудит 22.09.2026, `screens-5`; сделано ранее в #66) колонки Назва · Тип · Критеріїв · Шкала · Дата зміни · Опубліковано — все на месте, тексты найдены дословно | 16 |
| `Assessment` | форма анкеты | ✅ (ре-аудит 22.09.2026, `screens-5`; сделано ранее в #66) библиотека критериев (фільтри «Групи критеріїв» · «Мітки»), красная плашка заморозки, «Критерій — індикатор» + «Норма» на рядок, справа «Параметри анкети» и «Оцінювачі» с вагою і «анонімно» — все на месте | 16 |
| `Checklists` | `/admin/checklists` | ✅ (ре-аудит 22.09.2026, `screens-5`) колонки Назва · Пунктів · Шкала · Підрахунок · Дата зміни · Опубліковано — все на месте, включая примечание «У чек-листа вага пункту...» | 16 |
| `CriteriaGroups` | `/admin/assessment/criteria` | ✅ (ре-аудит 22.09.2026, `screens-5`) карточки групп с тегами и перечнем критериев, справа «Де використовуються» (В анкетах/У чек-листах/Привʼязано до компетенцій) — все на месте | 16 |
| `ChecklistReport` | `/admin/checklists/report` | ✅ (#69, `answers-1`, 22.09.2026) вкладки «По пунктах · По людях · По точках» з фільтрами період/точка/чек-лист, колонки ПУНКТ/ВАГА/ВИКОНАНО/ЧАСТКА/ДИНАМІКА (прогрес-бар); «По людях» — на єдиному каркасі ReportFrame (докс/22 §13.3); попередні KPI-плитки/дисципліна/топ-провалів/по тижнях і Mystery лишені окремими вкладками «Прогонів»/«Mystery» | 10 |

## Десктоп · звіти та журнали (7)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `ReportTest` | `/admin/reports/tasks/test` | ✅ (Spec 22) пять плиток, график обращений, «Статистика» из шести строк, таблица «Кращий · Результат · Спроб · Ліміт» | 10 |
| `SummaryReport` | `/admin/reports/summary` | ✅ (Spec 22) шаги `1 Користувачі · 2 Завдання · 3 Конфігурація · 4 Результат`, справа «Вибірка: людей N · завдань N» | 10 |
| `CustomReports` | `/admin/reports/builder` | ✅ (22.09, `screens-6`) сторінка була відразу редактором без списку — додано список зверху (`PageHeader`, підказка про постачальника дослівно, колонки Назва · Походження · Створено, «Побудувати» на рядку, «Створити звіт» у шапці); «Постачальник»-походження в Lola нема — рядки лише конструкторські, це пояснює сама підказка мокапу | 10 |
| `StatusLog` | `/admin/journals?tab=task-status` | ✅ (Spec 22) колонки Дата · Людина · Завдання · Тип · Стан · Результат · Подія | 10 |
| `SecurityLog` | `/admin/journals?tab=security` | ✅ (Spec 22) фильтры, тумблер «Повідомляти про зміни на E-mail», Рівень чипом, IP + гео, браузер | 4 (данные), 10 (экран) |
| `SessionsLog` | `/admin/journals?tab=sessions` | ✅ (22.09, `screens-6`; побудовано раніше, рядок не оновлювався) графік «Середній час у системі», «Унікальних людей · Сесій», таблиця з каркасом (людина/підрозділ) · ІР+гео · браузер — усе дослівно | 4, 10 |
| `Birthdays` / `Contacts` / `Events` | `/admin/birthdays`, `/admin/contacts`, `/admin/events` | ✅ (Spec 21) дни рождения (чипы «Майбутні · Минулі», календарь), контакты карточками, события таблицей | 11 |

## Десктоп · інформація та мотивація (8)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `Knowledge` | `/admin/knowledge`, `/learn/knowledge` | ✅ (Spec 21) «Мої закладки», поиск по источникам, чипы `Усі · Ресурси · Новини · Оголошення`, «Переглядів»; дерево категорий слева и «Оцінок» — 🟡 | 11 |
| `KnowledgeAdmin` | `/admin/knowledge/access` | ✅ (Spec 21) тумблер «Використовувати обмеження доступу», группы доступа таблицей | 11 |
| `News` / `NewsForm` | `/admin/news`, `/admin/news/new` | ✅ (Spec 21) колонки Тема · Автор · Актуально · Переглядів · Реакцій; форма с «Актуально до», «Дозволити реакції», главным изображением | 11 |
| `Notices` / `Notice` | `/admin/notices`, `/learn/notices/:id` | ✅ (Spec 21; підтверджено visual-mockups) карточка с бейджами типа и режима, «Ознайомились N із M», вкладки «Не підтвердили / Підтвердили», «Нагадати»; мобильный экран с липкой кнопкой «Ознайомився» | 11 |
| `SimpleNotices` | `/admin/simple-notices` | ✅ (Spec 21) таблица Назва · Опубліковано · Діє до · Реакцій · Стан | 11 |
| `Bonuses` / `ShopAdmin` | бонусы и магазин | ❌ R3 | R3 |

## Десктоп · налаштування та платформа (9)

| Мокап | Экран Lola | Состояние | PR |
| --- | --- | --- | --- |
| `Settings` | `/admin/settings/policies` | ✅ (Spec 24, докс/33 D-066) боковое меню десяти групп, сегменты и тумблеры с пояснениями, «Відхилити зміни / Зберегти» | 12 |
| `Roles` | `/admin/settings/roles` | ✅ (Spec 24, докс/33 D-066) таблица ролей (Скоупів · Людей · Тип), справа группы скоупов чекбоксами с кодами, «Створити роль» | 12 |
| `NotificationTemplates` | `/admin/settings/notifications` | 🟡 (22.09, `screens-6`) чипы `Глобальні · Кастомні` тепер справжні фільтри (раніше — статичні бейджі-лічильники), додані колонки E-mail/Telegram і «Змінено»; залишок: колонки E-mail/Telegram показують стан лише коли є кастомний оверрайд каналу («—» інакше) — дефолтний (глобальний) стан тумблера на рівні коду не змодельований на сервері (`DEFAULT_TEMPLATES` — лише текст, без прапорця каналу), тумблери в мокапі клікабельні, у нас — ні; «Подія (+код)» — людської назви події немає, показуємо тільки код | 13 |
| `NotificationTemplate` | редактор шаблона | 🟡 (22.09, `screens-6`) додано вкладки «Глобальний / Кастомний» (раніше — статичний бейдж, тепер перша показує стандартний текст лише на перегляд), заголовки «Канали» і «Коли надсилати» над відповідними полями, заголовок + підказка «Змінні шаблону» / «Натисніть, щоб вставити в текст» над кнопками вставки; текстовий шаблон вже був дослівно («Текстовий шаблон · Telegram і SMS») | 13 |
| `NotificationSchedule` | `/admin/settings/notifications` (вкладка «Час відправлення») | ✅ (ре-аудит 21.09.2026; сделан ранее в #64, строка была не обновлена) «Час відправлення повідомлень», «Обмежити період відправлення повідомлень», час по всіх класах подій (дні народження, річниці, дайджест керівника, протерміновані завдання, нагадування програми) | 13 |
| `Integrations` | `/admin/settings/integrations` | 🟡 (22.09, `screens-6`) SMTP-поля отримали дослівні підписи мокапу («Домен або IP SMTP-сервера», «SMTP server login», «Reply To», «Відправляти від імені») і підказку для Reply To («Щоб поштовий провайдер не прийняв розсилку за спам»); залишок — велика частина мокапу поза модел лю: групи `Аутентифікація` (ADFS/Active Directory/Entra ID), `Джерела користувачів` (HRM), `Вебінари`/`Google Analytics`/`AI provider` як окремі групи налаштувань, а також поля SMTP «Час затримки в черзі», «Максимальний розмір вкладення», тумблери SSL/«Режим відлагодження»/«Ігнорувати помилки TLS» — на сервері (`server/services/secrets.ts#SECRET_KEYS`) цих полів і провайдерів нема зовсім, це не вёрстка, а нова серверна функціональність | 13 |
| `Translations` | `/admin/settings/translations` | ✅ (Spec 24, докс/33 D-066) «Мова · Тільки змінені», колонки Ключ · Стандартний · Свій · Хто · Дата, экспорт/импорт json | 12 |
| `TenantStats` | `/admin/settings/usage` | ✅ (Spec 24, докс/33 D-066) плитки «Активні · Диск · SMS», «Що не входить у ліміт», «Останній збір», тариф | 12, 18 |
| `PlatformTenants` | `/ops` | ✅ (22.09, `screens-6`) виправлено дослівний текст: заголовок колонки `ops.col.tenant` («Простір» → «Тенант») і кнопка `ops.newTenant` («Новий простір» → «Створити тенанта»); колонки Тенант (домен у підрядку) · Тариф · Активні · Диск · Створено · Стан і секція «Тенанти платформи» вже були зроблені дослівно | 18 |

## Порядок работ по интерфейсу

1. **`ui-shell`** (следующий PR): каркас админки по мокапам — `SideNav` (разделы, подпункты,
   карточка человека с активной ролью), `PageHeader` (крошки, заголовок, действия), `Chips`,
   `DataTable` (заглавные колонки, строки-карточки), `SidePanel`; каркас кабинета — нижняя
   панель `BottomNav`, экран профиля и пустое состояние, шаг кода на входе.
2. После него каждый функциональный PR очереди `30` §4 доводит свои экраны до мокапа —
   это входит в его Definition of Done (колонка PR выше).
3. Экраны R3 (`Shop`, `ShopAdmin`, `Bonuses`, плашка бонусов в `TestResult`) не делаются в R1.
