# Lola LMS — аудит №2 на 19.09.2026 (вечер): состояние, задачи, решения

Второй аудит за день. Основание — `main` после PR #28, финальный пакет ТЗ (`docs/00–25`,
`_conventions`, новый `CLAUDE.md`), мокапы 90 экранов (`docs/mockups/`, карта `docs/31`),
предыдущий аудит `docs/29` и дельта `docs/30`. Формат тот же: **А** — состояние по
документам, **Б** — единый список задач в порядке выполнения, **В** — решения, принятые
без заказчика с момента `29` (проверить), **Г** — что требует решения заказчика сейчас.

## Сделано после `docs/29` (за день)

| PR | Что |
| --- | --- |
| #19 | Демо-стенд: образ в GHCR, compose на 1 ГБ, туннель Cloudflare; сегодня добавлен сервис `migrate` (миграции при каждом обновлении — до этого стенд отставал по схеме) |
| #20 | Область видимости отчётов по точкам (закрыта дыра «руководитель видел всю сеть») |
| #21–#24 | docs/19 часть 2 (матрица, порог заявок, ИПР), docs/20 часть 2 (подпись, тайный покупатель), docs/21 хаб, docs/22 часть 2 (каркас отчётов, выгрузки, журналы, дайджест) |
| #25 | docs/23 уведомления: правило канала, настройки человека, версии шаблонов, троттлинг, ретраи, эскалация, бот |
| #26 | Финальный пакет ТЗ + `docs/30` (дельта и очередь из 20 PR) |
| #27 | **Паритет 1**: правила прохождения только в назначении (`taskParams.ts`), перечисления `docs/02` в `shared/enums.ts` + CHECK, `schema-parity.spec.ts`, индекс `tenant_id` у всех таблиц |
| #28 | **Паритет 2**: пять статусов прохождения, признаки вместо статусов, автозакрытие по сроку, «Мої завдання» по мокапу |
| (этот) | Мокапы в `docs/mockups/`, карта `docs/31` |

Стенд: `https://pray-yesterday-lie-entertaining.trycloudflare.com` (образ `main` на 10:34, схема 0000–0025). Вход по любому номеру сида, код показывается на экране.

## А. Состояние по документам

| Документ | Готовность | Что осталось (подробно — `30` §2, экраны — `31`) |
| --- | --- | --- |
| CLAUDE.md пп. 11–15 | 11 ✅ · 12 ✅ · 13 🟡 · 14 ❌ · 15 🟡 | `request_context` во всех журналах; 404 для чужих файлов/отчётов; перечисления `trajectory_node_kind`, `notice_kind`, `security_severity` |
| 01 роли | 🟡 | активная роль в сессии и меню, `position_role_map` |
| 02 модель | 🟡 | таблицы из `30` §2: `question_groups`, `attempt_results`, `attempt_requests`, `trajectory_nodes` узловой модели, `notices`, `scales(range\|levels)`, `tags.scope`, `automation_rule_dimensions`, `task_parameters`, `access_groups`, `comments`, `tenant_usage` (`meetup_sessions`/`meetup_session_registrations` — Spec 18, под именем `meetup_sessions`, а не `sessions`: коллизия с `sessions.active_role_id`) |
| 04 API | 🟡 | новые пути алиасами (`/content`, `/tasks`, `/me`, `/enrollments/:id/items`); эндпоинты `recalculate`, `attempt-requests`, `audience/*`, `validate`, `role/switch`, `comments`, `notices/*`, `settings/*` |
| 05 экраны | 🟡 | всё, что помечено 🟡/❌ в `31` |
| 10 каталог | 🟡 | группы доступа каталога, режимы доступа предмета, приём заявок, лента комментариев |
| 11 контент | 🟡 | ресурс как тип с версиями/категориями/доступом, карточка курса (`code`, `duration_days`, `result_mode`), правила зачёта по типу, лимиты файлов |
| 12 тесты | 🟡 | группы вопросов, `scoring_method`, `grader_hint`, очередь по ответам, запросы попыток, пересчёт, копия/ссылка из банка, типы `classification`/`answer_by_map` |
| 13 практикумы | ✅ | режим «За практикумами», захват 30 мин |
| 14 сертификация | 🟡 | сертификат/бейдж как параметры назначения, выдача по траектории |
| 15 назначения | 🟡 | 11 типов в UI, карточка из четырёх блоков, пять групп параметров, аудитория списком + CSV, `on_leave_condition`, `assignment_competencies`, `task_parameters`, баннер «N завдань змінено» |
| 16 люди | 🟡 | поля карточки, `people.password`, метки со `scope`, `is_org_derived`, конфликты оргструктуры, «Не перезаписувати», журнал безопасности Г-16.2 |
| 17 траектории | 🟡 | узловая модель, `assign_mode`, валидация, измерения правил |
| 18 занятия | ✅ | Spec 18: сессии на назначении (`meetup_sessions`), «Анонс», отметка задним числом с аудитом, зачёт вебинара по `webinarMinWatchPct`, занятие как урок курса (`28` «Spec 18») |
| 19 развитие | ✅ | Spec 19: `user_competencies.source/valid_until`, подтверждение по завершению (`assignment_competencies`), уровни должностей в профиле, `display_as`, история обучения и рейтинг (`28` «Spec 19») |
| 20 оценка | 🟡 | заморозка, тип/нормы/правила комментирования, опрос: конфиденциально/анонимно/свій варіант/по шкалі/з умовами |
| 21 хаб | ✅ | Spec 21: объявления как назначаемый тип, поиск по источникам, группы доступа, закладки, дни рождения, контакты, события, гостевая (`28` «Spec 21») |
| 22 отчёты | 🟡 | единая левая часть, контекст проходження, отчёт по типу из четырёх частей, сводный мастер, журналы `task-access`/`task-status`/`org-conflicts` |
| 23 уведомления | 🟡 | `body_mjml`, `scope global/custom`, время по классам событий, `email_layout`, SMTP-поля |
| 24 настройки | 🟡 | политики десяти групп, модули, редактор ролей, шкалы, `usage.collect`, переводы, impersonate 60 мин |
| 25 мультитенантность | 🟡 | задачи по тенантам, round-robin, лимит активных, `suspended`/`purge`, `platform_audit`, `tenancy.spec.ts`, резолв по `Host` |
| 26–27 сервер, шлюз | 🟡 | прод на Proxmox не разворачивался (ждёт VM и домен — `29` Б.18); демо-стенд работает |
| Мокапы (`31`) | 4 ✅ · ~55 🟡 · ~30 ❌ | каркас (`ui-shell`) и доводка экранов внутри каждого PR |

## Б. Список задач (в порядке выполнения)

Один пункт = один PR с зелёным CI, мержится самостоятельно. Оценка — в рабочих сессиях
(1 сессия ≈ то, что делалось за один PR сегодня). Мокапы — обязательная часть DoD каждого PR.

| № | Ветка | Содержание | Мокапы | Модель | Оценка |
| --- | --- | --- | --- | --- | --- |
| 1 | `design-mockups` ✅ | мокапы в репо, карта `31`, `migrate` в демо-compose | — | sonnet | сделано |
| 2 | `ui-shell` ✅ (#30) | каркас админки (сайдбар с разделами/подпунктами, карточка человека с ролью, `PageHeader`, `Chips`, `DataTable`, `SidePanel`), каркас кабинета (`BottomNav`, профиль, пустое состояние), шаг кода на входе | Main (рамка), Login, EmptyState, Profile | sonnet | 1 |
| 3 | `parity-3-context` ✅ (#32) | `request_context` во всех журналах + хелпер с geo; 404 вместо 403 для чужих файлов/отчётов/страниц; `security_severity`; тест | SecurityLog, SessionsLog (данные) | opus | 1 |
| 4 | `parity-4-active-role` ✅ | `sessions.active_role_id`, `/me/role/switch`, меню по активной роли, аудит с обеими; `position_role_map`; `valid_until`/`reason` у роли (`29` Б.15) | карточка человека в сайдбаре, PersonCard «Ролі» | opus | 1 |
| 5 | `spec-15-tasks` ✅ | 11 типов контента в назначении, пять групп параметров (zod по типу), карточка из четырёх блоков, аудитория списком + CSV + конструктор, «Спосіб призначення», `on_leave_condition`, напоминания Г-15.1, `assignment_competencies`, `task_parameters`, баннер «N завдань змінено» | Main, TaskCard, TaskParams, TaskAudience | opus | 2–3 |
| 6 | `spec-12-questions` ✅ | группы вопросов + `one_per_group`, `scoring_method`, `grader_hint`, вложения к free, типы `classification`/`answer_by_map`, очередь по ответам с метками, запросы попыток, «Перерахувати», копия/ссылка из банка, протокол после последней попытки | QuestionEditor, ReviewQueue, ReviewAnswers, AttemptRequests, Test, TestResult, Workshop, ContentTests | opus | 2 |
| 7 | `spec-11-content` ✅ | ресурс как тип с версиями/категориями/доступом/печатью, карточка курса, раздел обязателен, правила зачёта по типу, `tick {seconds, scrollPct, videoPct}`, `notifyAssigned`, лимиты файлов | ContentResources, ResourceForm, ContentCourses, CoursePlan, Lesson, ContentCategories | opus | 2 |
| 8 | `spec-17-trajectories` ✅ | узловая модель (`and/or/delay/stop_delay/branch/mentor`), `assign_mode`, валидация, измерения правил таблицей, `preview/usages` | Trajectories, Trajectory, MyTrajectory, AutomationRules, AutomationRule, ContentPrograms | opus | 2 |
| 9 | `spec-22-reports-frame` ✅ | единая левая часть, контекст проходження, отчёт по типу из четырёх частей + «Перерахувати», сводный мастер, журналы `task-access`/`task-status`/`org-conflicts`, `severity` + почта | ReportTest, SummaryReport, CustomReports, StatusLog, SecurityLog, SessionsLog, ChecklistReport | opus | 2 |
| 10 | `spec-21-notices` ✅ | объявления как назначаемый тип (`notice_kind`, охват, «Нагадати»), простые объявления, поиск по источникам, группы доступа, закладки, `views_count`, дни рождения (`29` Б.16), контакты, события, гостевая | Notices, Notice, SimpleNotices, Knowledge, KnowledgeAdmin, News, NewsForm, Birthdays, Contacts, Events | opus | 2 |
| 11 | `spec-24-policies` ✅ | политики десяти групп, модули (wiki по умолчанию выключена), редактор ролей, шкалы `range\|levels`, `usage.collect` + «Статистика», категории с порядком, переводы, impersonate 60 мин + запреты (`29` Б.13), slug (`29` Б.12), акцент (`29` Б.14) | Settings, Roles, Scales, Translations, TenantStats, Certificates | opus | 2 |
| 12 | `spec-23-templates` ✅ | `body_mjml`, `scope global/custom`, `{{#_tr}}`, время по классам событий, `email_layout`, SMTP-поля, второй токен Telegram | NotificationTemplates, NotificationTemplate, NotificationSchedule, Integrations | sonnet | 1–2 |
| 13 | `spec-18-sessions` ✅ | сессии на назначении, «Анонс», отметка задним числом, `webinar_min_watch_pct`, занятие как элемент курса (`29` Б.3) | ContentMeetups, Meetup | sonnet | 1 |
| 14 | `spec-16-people-2` ✅ | поля карточки, `people.password`, метки со `scope`, `is_org_derived`, конфликты оргструктуры, «Не перезаписувати», журнал безопасности Г-16.2 | People, PersonCard, UserGroups, OrgStructure, Dictionaries, Tags, Import, ImportLog, OrgConflicts | sonnet (opus, если всплывут конфликты оргструктуры/валидность) | 2 |
| 15 | `spec-20-assessment-2` ✅ | заморозка, тип оценки и нормы, правила комментирования, опрос: конфиденциально/анонимно/свій варіант/по шкалі/з умовами, чек-лист по мокапу | Assessments, Assessment, Assessment360, Checklists, Checklist, CriteriaGroups, Poll, ContentPolls | opus | 2 |
| 16 | `spec-19-competencies` ✅ | `user_competencies.source/valid_until`, подтверждение компетенций по завершению задания, уровни должностей в профиле, `display_as`, история обучения и рейтинг в кабинете | Competencies, PositionProfile, DevelopmentPlans, DevelopmentPlanMobile, Goals, ExternalRequests, StudyHistory | sonnet (opus, если всплывут конфликты оргструктуры/валидность) | 1–2 |
| 17 | `spec-25-tenancy` | задачи по тенантам, round-robin, лимит активных, `suspended`/`purge`, `platform_audit`, резолв по `Host`, `tenancy.spec.ts` 1–10 | PlatformTenants | opus | 1–2 |
| 18 | `spec-04-routes` | новые пути алиасами; старые снимаются в конце R1 | — | sonnet | 1 |
| 19 | `spec-10-catalog` | группы доступа каталога, режимы доступа, приём заявок, лента комментариев с маршрутизацией | Catalog, LearningRequests, Comments | sonnet | 1 |
| 20 | `workflow-economy` ✅ | раздел «Экономия контекста» и таблица моделей в CLAUDE.md, пометки моделей в §Б, `pnpm check`/`test:quiet`/`e2e:quiet`, `scripts/deploy-demo.sh`, авто-деплой стенда из CI | — | sonnet | сделано |
| 21 | `visual-mockups` | визуальные тесты по мокапам: Playwright-скриншоты ключевых экранов (Main, Login, TaskCard, Profile) против `docs/mockups`, порог различий, отчёт в CI | Main, Login, TaskCard, Profile | sonnet | 1 |
| 22 | `prod-proxmox` | развёртывание на VM по `26`/`27` (ждёт VM и домен — Г.1) | — | sonnet | 1 |

Итого ≈ 31 сессия. Первые четыре (2–5) закрывают сквозные проверки `07` и главный экран
администратора; после них стенд можно показывать заказчику как «эталон по мокапам».

## В. Решения без заказчика с момента `29` (проверить)

Все записаны в `docs/28` («Финальный пакет ТЗ», «Паритет 1», «Паритет 2», «Мокапы»):

1. Расхождения внутри финального пакета решены в пользу перечислений `docs/02` и снятого с эталона (`17` узлы, не рёбра; `assessment`, не `poll360`; коды типов вопросов из `02`).
2. `wiki` уже сделана, ТЗ говорит «не делаем» — остаётся в коде, по умолчанию выключена модулем.
3. Правила прохождения: `assessment_forms.pass_score`, `checklists.pass_score` (анкеты, `08` §12.8) и `questions.time_limit_sec` (`12` §3.2) оставлены; `news.ack_due_at` — долг закрыт в Spec 21: объявления перенесены из `news` в `notices`, срок подтверждения — `assignments.due_at` (миграция 0034 переносит данные).
4. Пять статусов: `scheduled`/`expired`/`cancelled`/`requested` стали признаками; автозакрытие по сроку через 14 дней просрочки (настройка тенанта); «Мої завдання» — три чипа мокапа поверх пяти групп API.
5. Назначения тестов и комплексных тестов записи не создают до PR 5 — правила берутся при старте попытки.
6. Мокапы побеждают текст ТЗ при расхождении; экраны R3 из мокапов (магазин, бонусы) не делаются.

## Г. Что требует решения заказчика сейчас

| № | Вопрос | Варианты | Моя рекомендация |
| --- | --- | --- | --- |
| Г.1 | Прод на Proxmox: нужны VM (4 vCPU / 8 ГБ / 100 ГБ) и домен для `<slug>.lola.app` или свой | (а) выдать VM + домен → разворачиваю по `26`/`27`; (б) пока стенд в песочнице | (а), как только есть VM: до этого всё проверяется на стенде |
| Г.2 | Порядок очереди Б | (а) как выше — сквозное, затем назначения; (б) сначала «витрина» (ui-shell → назначения → отчёты), сквозное позже | (а): без `request_context`/404/активной роли каждый следующий экран придётся переделывать |
| Г.3 | Ссылка на стенд: быстрый туннель меняет адрес при перезапуске машины | (а) оставить как есть; (б) именованный туннель Cloudflare с постоянным поддоменом (нужен аккаунт Cloudflare и домен) | (б), когда появится домен из Г.1 |

Больше открытых вопросов нет: всё остальное закрыто «делай по рекомендациям» (`29` Б) и решениями в `28`.
