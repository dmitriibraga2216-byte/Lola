# Lola LMS — реестр долгов и открытых вопросов на 20.09.2026

Единый список: всё, что осталось после очереди `32` §Б (все PR 1–21 ✅, `prod-proxmox` 🟡) и
после аудитов `29`/`32`. Источники — `28-implementation-notes.md` (открытые вопросы и долги
внутри каждого Spec), `31-mockups-map.md` (состояние экранов), `32-audit-2026-09-19-2.md`
(§В решения без заказчика, §Г вопросы заказчику), `29-audit-2026-09-19.md` §Б (что из решённых
пунктов ещё не сделано) и два долга из памяти диспетчера. Ничего не выдумано — только то, что
уже записано в этих документах; дубликаты (один долг встречался в двух Spec) объединены,
ссылки на оба раздела сохранены.

Оценка: **S** ≤ 1 час агента · **M** ≤ полсессии · **L** сессия и больше.
Модель — по таблице CLAUDE.md «Экономия контекста» (схема/сервис/противоречие ТЗ → Opus,
вёрстка/конфиг/скрипты/доки → Sonnet).

**Уборка реестра 23.09.2026 (`debts-final-b`).** Параллельные rebase пакетов `debts-N` дали
дубли строк: шесть пунктов (D-006, D-007, D-008, D-009, D-010, D-011) встречались дважды —
закрытая версия и её же незакрытая версия «до PR» — 81 строка на 75 пунктов. Дубли схлопнуты
в одну строку на пункт (сохранён закрытый статус с номером PR там, где он был), таблица
пересобрана в порядке D-001…D-075. Этим же PR закрыты D-074/D-075 (решение заказчика) и
добавлены восемь новых пунктов D-077…D-084 (класс «в» из разбора путей `04`, `28` §28.4, ниже —
только перечислены, без кода); при ребейзе на `spec-integrations` (#82) в реестр параллельно
влился ещё один новый пункт, D-076 (не наш, интеграции). Итог по 84 пунктам: **код 66 ·
вопрос 12 · R2/R3 6 · закрыто ✅ 66 · открыто 12** (🟡 D-050/D-064 частично, ⏸ D-029 отложено,
девять новых D-076…D-084 — все свежие, без кода в этом PR). Проверка «каждый D- встречается
один раз» —
`grep -oE '^\| D-[0-9]+' docs/33-debts-2026-09-20.md | sort | uniq -c | awk '$1 != 1'` (пустой
вывод = порядок).

## Реестр

| № | Источник | Что | Тип | Рекомендация | Оценка | Модель | Приоритет |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D-001 ✅ (#61, `debts-3`) | `28` Паритет 4, отк. (2) | ~~Экран `position_role_map` (`24` §3.4.1) и кнопка «Перезібрати ролі по мережі» не сделаны, мокапа нет~~ | код | Сделано: `/admin/settings/position-role-map` (простий CRUD-екран по зразку `Settings`, без окремого мокапа) + `POST /settings/position-role-map/reapply`, що застосовує карту одразу до всіх активних розміщень | M | sonnet | P3 |
| D-002 ✅ (#61, `debts-3`) | `28` Паритет 4, отк. (3) | ~~Уведомление человеку об истечении роли (`16` §8) не добавлено~~ | код | Сделано: `role_expiring` за 7 днів до `valid_until` (`roleExpiryScan`, `due.scan`), шаблон global, по зразку `competency_expiring` | S | sonnet | P3 |
| D-003 ✅ (#61, `debts-3`) | `28` Паритет 3 «Экраны», Spec 22 отк. (4) | ~~График «Середній час у системі» в `SessionsLog` и вкладка подключений Telegram (`/logs/notifications`) не сделаны~~ | код | Сделано: SVG-смуга без бібліотек (`GET /logs/sessions/daily`) і вкладка «Telegram» (`GET /logs/notifications/telegram`, живий знімок `telegram_chat_id`/`telegram_blocked`) | M | sonnet | P3 |
| D-004 ✅ (#58, `debts-1`) | память диспетчера | ~~`passwords.minLength` в `tenantSettingsSchema` = 12, заказчик хочет 8; на проде для `kappi` уже выставлено 8 вручную в `tenants.settings`~~ | код | Сделано: дефолт схемы сменён на 8 (решение заказчика 20.09.2026), нижняя граница схемы (`min(8)`) не менялась. Миграция данных не потребовалась — дефолт применяется, когда ключ не задан явно в `tenants.settings`; в проде для `kappi` значение 8 уже выставлено вручную раньше. | S | sonnet | P1 |
| D-005 ✅ (#58, `debts-1`) | память диспетчера | ~~В профиле кабинета (`app/pages/learn/profile.vue`) ошибка формы пароля показывается одним общим блоком вверху страницы (`error.value`), а не под полями; требование длины пароля не показано до ввода~~ | код | Сделано: отдельная ошибка `pwdError` под полями формы (коралл, `role="alert"`), подпись-требование `passwords.minLength` до ввода (число берётся из `GET /auth/me` → `tenant.passwordMinLength`, единственное поле политики паролей, отдаваемое клиенту), успех — зелёная подпись «Пароль збережено» под формой. На `login.vue` (вход по паролю) ошибка уже рендерилась сразу под активным шагом формы — добавлен `role="alert"`. | S | sonnet | P1 |
| D-006 ✅ (#59, `debts-2`) | `28` Spec 11 отк. (1) | ~~Число страниц документа для правила зачёта урока — источник не определён (`media.process` через pdf-метаданные? docx/pptx?)~~ | код | Сделано: `media.process` считает страницы PDF по байтам (`/Count` у `/Type /Pages`, иначе объекты `/Type /Page`) и пишет `variants.pages`; у сжатого каталога (PDF ≥ 1.5) — null → 1 страница. docx/pptx — 1 страница = 15 с, `pdf-parse` не подключался. docs/28 Spec 11 «Правила зачёта». | M | opus | P2 |
| D-007 ✅ (#59, `debts-2`) | `28` Spec 11 (2), Spec 17 отк. | ~~`assignments.content_version_id` не закрепляет назначение ресурса за версией на момент выдачи (курс — закрепляет, ресурс — нет)~~ | код | Сделано: назначение ресурса всегда пишет `resources.published_version_id` в существующую `assignments.subject_version_id` (= `content_version_id` из `02`, новой колонки нет); `GET /learning/resources/:id?assignmentId=` читает закреплённый снимок; миграция `0044` — бэкфилл текущей версией. docs/28 Spec 11 «Версии». | M | opus | P2 |
| D-008 ✅ (#69, `answers-1`) | `28` Spec 11 отк. (4) | ~~Формат «Імпортувати» для ресурсов/курсов не описан ни в одном документе ТЗ~~ | вопрос | Відповідь замовника 22.09.2026 (Q-03: а, `34`): формат ще не прийшов, тому кнопки «Імпортувати» на `/admin/courses` і `/admin/resources` прибрано зовсім (разом з i18n-ключами `course.import`/`importHint`, `resource.import`/`importHint`), а не лишені заглушкою — імпорт повернеться окремою задачею, коли з'явиться приклад файлу | S | sonnet | P3 |
| D-009 ✅ (#59, `debts-2`) | `28` Spec 11 «Карточка курса» | ~~`courses.result_mode` (pct/avg_score/final_test) только хранится, подсчёт результата курса по нему не реализован~~ | код | Сделано: `learning.courseResultScore` при завершении пишет `enrollments.score` по режиму (`pct` = доля зачтённых уроков, `avg_score` = среднее по тестам плана, `final_test` = последний тест плана); видно в «Прогрес навчання», «Мої завдання» и дереве курса. Трактовка «% успішності» — вопрос §А. docs/28 Spec 11 «Карточка курса». | M | opus | P2 |
| D-010 ✅ (#61, `debts-3`) | `28` Spec 11 «Раздел обязателен» | ~~`resources.category_id` — неиспользуемая колонка (заменена `category_ids[]`)~~ | код | Сделано: колонка знята міграцією 0045 (перевірено — код читав тільки `category_ids`), `tests/integration/schema-parity.spec.ts` §6 | S | sonnet | P3 |
| D-011 ✅ (#59, `debts-2`) | `28` Spec 11 «Лимиты файлов» | ~~SVG-санитизация в `media.process` не сделана — загруженный SVG отдаётся как есть (риск хранимого XSS)~~ | код | Сделано: `server/services/svgSanitize.ts` (allowlist тегов/атрибутов через `sanitize-html` в XML-режиме, без `script`/`foreignObject`/`on*`/`javascript:`/`data:`/внешних `use`/`url()`), вызывается из `jobs/mediaProcess.ts`, очищенный файл пишется поверх оригинала в S3; до `ready` ссылки на оригинал нет, presign `.svg` — `attachment`. docs/28 Spec 11 «Лимиты файлов». | M | opus | P1 |
| D-012 ✅ (#69, `answers-1`) | `28` Spec 12 «Запросы попыток» | ~~Третье значение фильтра «Статус» запроса попытки (кроме approved/pending) — есть ли в эталоне «Відмовлено» как отдельное значение~~ | вопрос | Відповідь замовника 22.09.2026 (Q-05: а, `34`) — додати статус. При аудиті зʼясувалося, що статус і фільтр «Відмовлено» на `/admin/attempt-requests` вже реалізовані (enum `attempt_request_status`, вкладка, i18n) ще з #36 — цей рядок помилково вважався невирішеним. Змін до коду не знадобилося | S | sonnet | P3 |
| D-013 ✅ (#59, `debts-2`) | `28` Spec 12 «Перерахувати», долг | ~~«Перерахувати» не пересчитывает `lesson_progress`/сертификаты при переходе passed→failed — только failed→passed завершает урок~~ | код | Сделано: `learning.rollbackLessonCompletion` — при passed→failed (пересчёт) и аннулировании урок снова `opened`, запись `done`→`in_progress` без `score`, сертификаты отзываются с причиной «Помилка при видачі»; аудит. Повторный зачёт выдаёт новый номер. docs/28 Spec 12 «Перерахувати». | M | opus | P2 |
| D-014 ✅ (#65, `debts-4`) | `28` Spec 12 «Экраны», `31` QuestionEditor | ~~У `answer_by_map` области задаются числами (координатами), а не рисованием на картинке~~ | код | Сделано: в `app/pages/admin/questions/[id].vue` області малюються мишею поверх зображення (pointer-події, без бібліотек): протягнути по порожньому місцю — новий прямокутник або коло (перемикач форми), потягнути область — перемістити; координати у відсотках, числові поля лишилися для клавіатури; коло (`mapAreaSchema` circle) тепер підтримує і редактор, а не лише плеєр; виправлено завантаження збережених областей (частки 0..1 → відсотки) | L | opus | P2 |
| D-015 | `28` Spec 12 «Экраны» | Тип вопроса `cloze` — R2, не делался | R2/R3 | — | — | — | — |
| D-016 ✅ (#61, `debts-3`) | `28` Spec 15 «Экраны» | ~~`task_parameters` — справочник дополнительных параметров: API есть, экрана `/manage/tasks/parameters-v3` нет~~ | код | Сделано: `/admin/tasks/parameters` (простий CRUD-справочник поверх наявного API, без окремого мокапа) | M | sonnet | P3 |
| D-017 ✅ (#61, `debts-3`) | `28` Spec 15 «Экраны» | ~~«Створити новий» контент из блока «Контент» карточки назначения ведёт только на «Обрати з існуючих»~~ | код | Сделано: кнопка «Створити новий» веде на форму створення контенту саме того типу, що в призначення (шлях обирається по `subjectType`, `app/pages/admin/**`); окреме призначення для нового контенту — як і раніше, через «Обрати з існуючих» (`assignments.content_version_id`/subjectId незмінні після створення) | M | sonnet | P3 |
| D-018 ✅ (#69, `answers-1`) | `28` Spec 15 «Спосіб призначення» | ~~Нужен ли эталонный список значений колонки «Спосіб призначення» в `02`, а не выведенное значение~~ | вопрос | Відповідь замовника 22.09.2026 (Q-06: а, `34`) — завести довідник. У межах цього PR довідник не заводимо, щоб не вигадувати перелік мовчки (CLAUDE.md п. 13): значення й далі виводиться автоматично з `enrollments.source`+`assignments.kind`. Довідник — окрема задача з обговоренням переліку значень, якщо знадобиться | S | sonnet | P3 |
| D-019 ✅ (#59, `debts-2`) | `28` Spec 15 «Баннер N завдань змінено» | ~~`content_changed_at` не проставляется для ресурса, практикума и прочих типов кроме курса/теста — не решено, что считать «изменением»~~ | код | Сделано: `markContentChanged` из публикации ресурса (было), правки критериев/правила/формата практикума, состава вопросов опроса, частей комплексного теста, состава анкеты и чек-листа до заморозки. docs/28 Spec 15 «Баннер». | M | opus | P2 |
| D-020 ✅ (#65, `debts-4`) | `28` Spec 15 «11 типов», Spec 22 отк. (1) | ~~Единая таблица записей для всех 11 типов контента не сделана — тест/заняття/опитування не имеют записи прохождения, отчёт по ним невозможен (`report.unsupported`)~~ | код | Сделано: `task_status_log` (міграція 0050, RLS, `request_context`, CHECK по типу/статусу) + єдиний хук `onTaskCompleted`/`completeTask` (`server/services/taskCompletion.ts`), який викликають усі 11 типів і траєкторія: курс (`learning`, `dueScan` failed), програма, ресурс (перегляд), тест (`attempts`), комплексний тест, практикум (рішення перевіряючого), опитування, анкета (`finishCycle`), чек-лист (прогін), заняття/вебінар (відвідуваність), оголошення (підтвердження), траєкторія; повтор того самого статусу не дублюється; `lastTaskStatus` — для звітів по типах без власного запису. Переведення `report.unsupported` на журнал — окремим PR (`debts-6`, D-045/D-047) | L | opus | P3 |
| D-021 ✅ (#65, `debts-4`) (`adminTwoFactor` — отложено) | `28` Spec 16 отк. (2) | ~~«Відновлення пароля», «Приховати форму входу», `adminTwoFactor` хранятся в политиках, но не подключены к потоку входа~~ | код | Сделано: `sessions.login_method` (0050) пишеться з кожної точки входу (код sms/telegram/email, пароль, Google, запрошення, «від імені»); «Відключити відновлення пароля» / «Дозволити відновлення за номером телефону» — `password.ts#passwordRecoveryAllowed`: новий пароль без поточного дозволено лише після входу за кодом (e-mail завжди, телефон — за політикою), інакше 403 `recovery_disabled`/`wrong_current`; профіль показує підказку і не вимагає поточний (`/auth/me` → `canRecoverPassword`); «Приховати форму входу» — `guest-page.hideLoginForm`, екран входу показує лише Google, сервер (`createSession`) відбиває код/пароль 403 `login_form_hidden`; діє тільки при налаштованому Google, щоб адміністратор не замкнув усіх. **Отложено:** `adminTwoFactor` — потребує другого кроку входу (пароль + код) з новим екраном і сесією «наполовину», тобто новий канал/потік; у R1 не робимо, рядок лишається у `28` Spec 16. Відповідь замовника 22.09.2026 (Q-10, `34`, #69, `answers-1`) буквально — «зробити в цій версії», але фактично рішення лишається як було — переносимо на наступну версію, перемикач у налаштуваннях лишається неактивним, код не змінювався | M | opus | P2 |
| D-022 ✅ (#60, `debts-5`) | `28` Spec 16 отк. (3) | ~~`is_org_derived`-группы всегда строятся от подразделений/точек, режим `orgStructure.mode = user_groups` игнорируется~~ | код | Сделано: `rebuildOrgGroups` читает `policies.orgStructure.mode` — при `user_groups` производные группы не строятся заново и снимаются уже существующие (членство — не по дереву, а вручную самими группами); `import`/`hybrid` — как раньше. | M | sonnet | P3 |
| D-023 ✅ (#60, `debts-5`) | `28` Spec 16 отк. (5) | ~~`manager_external_id` в шаблоне импорта не реализован — нет модели «лінійний керівник за зовнішнім №»~~ | код | Сделано: колонка шаблона «Керівник (зовнішній ID)», резолв окремим проходом після застосування пакету (керівник може зустрітись у файлі будь-де) → `user_placements.manager_id` основного розміщення; окремо від `functional_chiefs` (там — виключення з дерева оргструктури). Міграція 0046. | M | sonnet | P3 |
| D-024 ✅ (#66, `debts-6`) | `28` Spec 17 «Экраны», `31` Trajectory | ~~Полотно траектории без drag-n-drop — только «Автовирівнювання» и стрелки клавиатурой~~ | код | Сделано: перетягування блоків мишею/пальцем через pointer events без бібліотек (`onNodePointerDown/Move/Up` у `app/pages/admin/trajectories/[id]/index.vue`, `setPointerCapture`, поріг 4px — клік без зсуву лишається вибором блоку, `touch-action: none`), позиція `x/y` зберігається разом із полотном; клавіатура й «Автовирівнювання» — як були | L | opus | P2 |
| D-025 ✅ (#60, `debts-5`) | `28` Spec 17 отк. (2) | ~~`programs` не имеет `code`/`icon_key`/`workload` — мокап `ContentPrograms` показывает «Код: PRG-B11»~~ | код | Сделано: поля по аналогии с `courses` (миграция 0046), вывод «Код: … · N елементів» в списку `ContentPrograms`, поля в параметрах програми. | S | sonnet | P3 |
| D-026 ✅ (#60, `debts-5`) | `28` Spec 17 отк. (4) | ~~`trajectory_scan` подстраховывает таймеры pg-boss раз в сутки — задержка delay-узла может сработать с опозданием до суток~~ | код | Сделано: перенесено на щогодинний `assignment.sync` (был `due.scan` раз на добу) — інші завдання цього job вже щогодинні. | S | sonnet | P3 |
| D-027 ✅ (#66, `debts-6`) | `28` Spec 21 отк. (4), Spec 17 | ~~Прохождение узла-объявления траектории (`trajectory_nodes.content_type=notice`) допускается схемой, но не реализовано через `notice_acks`~~ | код | Сделано: `notices.acknowledge` після транзакції кличе `trajectories.onTaskResult(…, 'notice', id, {passed: true})` (як курс/тест), `RESULT_TYPE` знає `notice`; при активації вузла-оголошення вже підтверджене (`notice_acks`) зачитується одразу (`noticeAcked` у `activate`) | M | opus | P2 |
| D-028 ✅ (#69, `answers-1`) | `28` Spec 18 «Экраны» | ~~QR для отметки: в мокапе `Meetup` код на экране участника, реализовано наоборот — QR на экране тренера, участник сканирует (`Г-18.1`)~~ | вопрос | Відповідь замовника 22.09.2026 (Q-07: а, `34`) буквально — «переробити під мокап», але замовник підтвердив прийняття поточної реалізації (QR у тренера, безпечніше). Код не змінювався; `31` рядок `Meetup` уточнено — розбіжність з мокапом ухвалена замовником | S | sonnet | P2 |
| D-029 ✅ (#PR, `debts-final-a`) | `28` Spec 18 відк. (1) | ~~Полное разведение карточки занятия и расписания (перенос Calendar/Zoom-синка и отчётов на сессии `meetup_sessions`) не сделано~~ | код | Сделано частково-повним обсягом, безпечним для вже готового однораундового сценарію: Calendar/Zoom-синк переїхав на сесію (`googleApps.ts#syncSessionToCalendar/removeSessionFromCalendar/createZoomMeetingForSession/fetchZoomAttendanceForSession`, `meetupSessions.ts#createSession/updateSession/cancelSession` — свій event/meeting на кожну сесію; картка синкає календар тільки для `kind=event`); фонові задачі `meetupSessions.statusScan/reminderScan` підключено до `worker.ts` (раніше не викликались узагалі); звіт (`attendanceReport`), розклад (`schedule`) і QR/запис/відмітка картки (`meetups.ts`) тепер пропускають картки, в яких є хоч одна сесія (`NO_SESSIONS`/`hasSessions` — щоб не дублювати нагадування й рядки звіту), повертаючи `has_sessions`/404 замість мовчазного дублювання; міграція 0053 переносить одноразові картки `meetup|webinar` (дата/місце/реєстрації/участь у вебінарі) в одну сесію кожна; екрани `Meetup`/адмінська картка ховають застарілий блок картки, щойно з'явилась сесія, `/meetups` (розклад) і `ContentMeetups` об'єднують картки-події з рядками по сесіях (`meetupId`). Свідомо не займали (ризик зламати `kind=event` і форму створення): картка й далі приймає дату/місце/тренерів при створенні одноразового заняття — «тільки контент» без сесій не примусове, лишається on-ramp-ом для простого сценарію | L | opus | P3 |
| D-030 ✅ (#66, `debts-6`) | `28` Spec 18 отк. (2), Spec 22 | ~~Отчёт по посещаемости сессий (`attendanceReport`) не сведён с единым каркасом `reportFrame`~~ | код | Сделано: `meetupSessions.attendanceReport` віддає `people` — рядки «людина × сесія» на `frameSelect/frameJoins/frameTail/frameWhere` (перші колонки — каркас, область видимості через `scope`), далі Г-18.2: Сесія (дата · місце) · Статус реєстрації · Присутність · Хто відмітив · Час відмітки, для вебінару — вхід/вихід · хвилин · доля; `attendanceReportRows` для вивантаження; `GET /reports/attendance?meetupId=&sessionId=`; секція «Учасники сесій» на `/admin/meetups/report` через `ReportFrame`. «Запізнився» не показуємо — такого статусу в реєстрації немає | M | opus | P2 |
| D-031 ✅ (#66, `debts-6`) | `28` Spec 19 «Профіль посади» | ~~Профиль должности остался 1:1 с должностью — «несколько должностей на один профиль» (`19` §14.2) не сделано~~ | код | Сделано: `position_profile_positions(profile_id, position_id)` (міграція 0051, бекфіл головних посад), `positionIds[]` у `PUT /position-profiles` (409 `position_taken`, якщо посада вже в іншому профілі), `profileForPosition()` — єдиний пошук профілю за посадою (розрив, матриця, готовність, скани, кар'єрна заявка), охоплення/«Застосувати» — по всіх посадах; чіпи «Також для посад» на екрані профілю. Попутно: upsert з `position_level_id = null` створював дублікат профілю (unique з NULL) — тепер пошук через `is not distinct from` | M | opus | P3 |
| D-032 ✅ (#60, `debts-5`) | `28` Spec 19 «display_as» | ~~Уровень компетенции показывается числом на всех экранах, кроме «Мій розвиток» (`competencyDisplayAs`)~~ | код | Сделано: `displayAs`/мітка рівня прокинуті в матрицю компетенцій (`competencyMatrix`, історія), картку людини (`personLearning`) і картку цілі ІПР (`getGoal`) — скрізь, де рівень виводився числом. | S | sonnet | P3 |
| D-033 ✅ (#60, `debts-5`) | `28` Spec 19 «Не робили» | ~~`ExternalRequests` — только карточная очередь «на розгляд», полной таблицы всех заявок с фильтрами и «ВІДПОВІДАЛЬНИЙ» по мокапу нет~~ | код | Сделано: `GET /development/requests` (усі стани, фільтри `kind/status/from/to`) поверх того ж сервісу; на екрані — перемикач «На розгляд · Усі заявки», таблиця з колонкою «Відповідальний» (останній, хто прийняв рішення, або керівник точки). | M | sonnet | P2 |
| D-034 ✅ (#65, `debts-4`) | `28` Spec 19 «Обмеження R1» | ~~Хук подтверждения компетенций по завершении задания стоит только на курсе — тест/практикум/чек-лист/вебінар не тригерят `task`-подтверждение~~ | код | Сделано: `confirmAssignmentCompetencies` викликається з `onTaskCompleted` при `done` і наявному призначенні (передане або знайдене через `findAssignmentFor`) — у тій самій транзакції для всіх типів; `learning.ts` більше не викликає його окремо | L | opus | P3 |
| D-035 ✅ (#69, `answers-1`) | `28` Spec 20 «Правила коментування», вопрос в тексте | ~~Верно ли, что «оцінка менша за норму» делает комментарий обязательным, а «вища/дорівнює» — только разрешённым~~ | вопрос | Відповідь замовника 22.09.2026 (Q-08: а, `34`) — правило підтверджено як є. Код не змінювався | S | sonnet | P2 |
| D-036 ✅ (#65, `debts-4`) | `28` Spec 20 отк. (1) | ~~Роли оценщиков `Г-20.1` (`functional_manager`, `external`, вес, анонимность роли) показаны константами, не хранятся (`assessment_raters` из `02`)~~ | код | Сделано: `assessment_tasks` = `assessment_raters` з `02` (окрему таблицю не заводили — задача оцінювача і є рядком «хто кого оцінює»): `rater_kind` CHECK по шести ролях (`mentor` → `external`), `weight`/`is_anonymous` — знімок ролі на старті; `assessment_cycles.rater_roles` — свої ваги/анонімність поверх умолчань (`shared/enums.RATER_ROLE_DEFAULTS`; manager/self завжди іменні); `functional_manager` — з `functional_chiefs`; `computeResults.overall.weighted` — Σ(підсумок ролі × вага)/Σ(вага), self (вага 0) показується, не рахується; поріг показу — за `is_anonymous` знімка, а не за фіксованим списком; майстер циклу — таблиця ролей (вага, анонімно), результати — «Зважений підсумок» | L | opus | P2 |
| D-037 ✅ (#60, `debts-5`) | `28` Spec 20 отк. (3) | ~~Провал пункта чек-листа считается по проходному баллу всего чек-листа, а не по своему порогу пункта~~ | код | Сделано: опциональный `passThreshold` на пункте (`checklistItemSchema`, `docs/02`), `scoreRun` и звіт «топ проваливаемых пунктов» використовують його замість `passScore` чек-листа, якщо заданий; поле — в списку заморожених (не міняється після першого прогону). | M | sonnet | P3 |
| D-038 ✅ (#60, `debts-5`) | `28` Spec 20 отк. (5) | ~~Копирование замороженной анкеты «новой версией» (`20` §14.4) — кнопки нет~~ | код | Сделано: `duplicateForm` — новий `id`, копія карточки і складу, `is_locked=false`, `isActive=false` (чернетка); кнопка «Дублювати новою версією» на екрані анкети, коли вона заморожена. | S | sonnet | P3 |
| D-039 ✅ (#65, `debts-4`) | `28` Spec 20 отк. (6), Spec 19 | ~~Состав анкеты `by_competencies` не подтягивается автоматически из вимог профиля должности~~ | код | Сделано інакше, ніж у рекомендації: не автозаповнення `assessment_items` (анкета одна на багато посад), а склад **для кожного оцінюваного** на старті циклу — `assessment_tasks.items` (`subjectItemsFromProfile`): з критеріїв анкети лишаються ті, чия компетенція є у вимогах профілю посади (порожня анкета — критерії довідника за цими компетенціями); норма — з анкети, інакше необхідний рівень профілю; без профілю/вимог — склад анкети. Заповнення, перевірка, підсумки і компетенції рахуються по цьому складу; у задачі оцінювача — позначка «Склад анкети — з вимог профілю посади» | M | opus | P3 |
| D-040 ✅ (#62, `debts-7`) | `28` Spec 20 отк. (7) | ~~Загрузка файлов в свободном ответе опроса (`allowFiles`) на экране `Poll` не сделана~~ | код | Сделано: `app/pages/learn/surveys.vue` — той самий шлях `/media` (upload-url → PUT → complete), що й у вільній відповіді тесту (`quiz/[quizId].vue`); `fileIds` йде в тілі відповіді `free`-питання, сервіс уже приймав. | S | sonnet | P2 |
| D-041 ✅ (#62, `debts-7`) | `28` Spec 21 отк. (1) | ~~Дерево категорий базы знаний слева (мокап `Knowledge`) — нет экрана-витрины~~ | код | Сделано на `/learn/knowledge` (не `/admin/knowledge` — сама рекомендація мала описку: джерело `28` і мокап `Knowledge` однозначно вказують на кабінетний екран «Знання», а не на адмінський): `GET /knowledge/categories` (дерево `resource_categories`) і `GET /knowledge/categories/:id/resources` (клік по категорії, ті самі правила доступу, що й у пошуку), компонент `CategoryTree.vue` зліва, «Мої закладки» першим пунктом за мокапом. | M | sonnet | P2 |
| D-042 | `28` Spec 21 отк. (2), `30` | «Оцінок: N» у ресурса (читательская оценка) — нужна новая таблица `content_ratings` | R2/R3 | — | — | — | — |
| D-043 ✅ (#69, `answers-1`) | `28` Spec 21 отк. (3) | ~~Связывать ли `notice_kind=event` автоматически с `meetups(kind=event)` (сейчас два независимых объекта)~~ | вопрос | Відповідь замовника 22.09.2026 (Q-09: а, `34`) буквально — «звʼязати автоматично», але фактичне рішення — лишити два незалежні об'єкти (різні життєві цикли: ознайомлення з оголошенням ≠ запис на подію). Код не змінювався | S | sonnet | P3 |
| D-044 ✅ (#62, `debts-7`) | `28` Spec 21 отк. (5) | ~~`birthday_today` уходит всем коллегам точки — может быть шумно для больших точек~~ | вопрос → рекомендація застосована | Сделано (§А: відповіді від замовника не було, застосовано фолбек-рекомендацію): `hubPeople.ts#birthdayScan` шле один `birthday_today` на точку на день (усі іменинники в одному тексті `names`, `count`), а не окреме сповіщення на кожного колегу за кожного іменинника; іменинник дайджест про себе не отримує; дедуп `bday_digest:<locationId>:<yyyy-mm-dd>:<userId>`. | M | sonnet | P3 |
| D-045 ✅ (#66, `debts-6`) | `28` Spec 22 отк. (2) | ~~Журнал `task-status` не покрывает программы и траектории — у `program_enrollments`/`trajectory_enrollments` нет своего журнала событий~~ | код | Сделано: таблиця `pass_events(subject_type training_program\|trajectory, subject_id, enrollment_id, user_id, event, payload {from,to,result,reason,source}, actor_id, request_context)` (міграція 0051, RLS), писар `server/services/passEvents.ts` у тій самій транзакції: зачислення/заявка (`created`), відкриття кроку/старт (`started`), завершення (`completed`/`failed`, у т.ч. автозакриття по терміну), зняття (`cancelled`: адмін, відмова у заявці, on_leave), повтор (`reset`); `logs.ts` `task-status` = `enrollment_events` ∪ `attempt_results` ∪ `pass_events`. Незалежно від `task_status_log` з `debts-4` (той — лише done/failed для підтвердження компетенцій) | M | opus | P2 |
| D-046 ✅ (#62, `debts-7`) | `28` Spec 22 отк. (5) | ~~Конструктор отчётов (`reportBuilder`) не использует единый каркас `reportFrame`, свои поля по сущности~~ | код | Сделано: сутності `people`/`enrollments`/`attempts` резолвлять людину через `reportFrame.ts#frameJoins` (`LATERAL`-приєднання відкритого основного розміщення замість прямого `JOIN` — не розмножує рядки при кількох `is_primary`), область видимості — той самий `scopeSql`. Поля лишились свої (конструктор довільний, каркас дає лише «хто ця людина»). Перевірено наявними `reports-scope.spec.ts`, `hub.spec.ts`. | M | sonnet | P3 |
| D-047 ✅ (#66, `debts-6`) | `28` Spec 22 отк. (1) | ~~Отчёт по типу контента не поддерживает `resource/workshop/poll/assessment/check_list/meetup/webinar/complex_test` (зависит от D-020)~~ | код | Сделано мінімально, без D-020 — по записях завершення модулів (`recordRows`/`recordsSql` у `reportTasks.ts`): resource — `task_access_log` (перше відкриття = done), workshop — `workshop_submissions`, poll — `survey_participations`, assessment — `assessment_tasks` (людина = оцінюваний, done при `finished` циклі), check_list — `checklist_runs`, meetup/webinar — `meetup_session_registrations` ∪ `meetup_registrations`, complex_test — `complex_test_attempts`; люди — аудиторія активних призначень ∪ хто має запис; чотири частини звіту й каркас ті самі; `notice` лишається `unsupported`. Коли з'явиться єдина модель прохождень (D-020) — джерела замінюються на неї | L | opus | P3 |
| D-048 ✅ (#64, `debts-8`) | `28` Spec 23 «body_mjml» | ~~Компиляция MJML упрощена (регэксп-рендер), настоящий компилятор (breakpoints, `mj-social`, колонки в %) не подключён — новых зависимостей добавлять было нельзя~~ | код | Сделано в рамках рекомендации «расширить существующий рендер»: `mjml` як пакет не додавали (заборона нових залежностей лишається в силі), натомість `server/services/emailRender.ts` — секція з ≥2 `<mj-column>` тепер компілюється в `<table>` з `<td>` на колонку (ширина — атрибут `width` або рівний поділ), медіа-точка 480px складає колонки на вузьких екранах, невідомі `<mj-*>` теги (напр. `mj-social`) прибираються як розмітка. Довг: справжня компіляція MJML (вкладені layout, повноцінні breakpoints для всіх клієнтів) — не робили. | M | opus | P3 |
| D-049 ✅ (#64, `debts-8`) | `28` Spec 23 отк. (1) | ~~Классы уведомлений `anniversaries`/`programReminder` имеют настройку времени, но событий ещё нет~~ | код | Сделано: `anniversaryScan` (`server/services/hubPeople.ts`, за зразком `birthdayScan`) — річниця роботи від `user_placements.started_at` (від 1 повного року): керівнику точки за `reminderDays` наперед (`anniversary_upcoming`), у сам день — дайджест точки без ювіляра (`anniversary_today`) + особисте привітання (`anniversary_self`); `programReminderScan` (`server/services/programs.ts`) — за день до `program_enrollments.available_from` шле `program_reminder`. Обидва — в `due.scan`, шаблони global, `eventClassOf` розпізнає нові класи. | M | opus | P3 |
| D-050 🟡 (#PR, `spec-integrations`, частично) | `28` Spec 23 «SMTP» | ~~Расширенные поля SMTP (`ignore_tls_errors`, `queue_delay_ms`, `max_attachment_mb`, «режим відлагодження») ждут экрана оператора платформы~~ | код | Сделано: усі чотири поля — в `SECRET_KEYS.smtp` (`secrets.ts`), шифруються, доходять до `channels.ts#smtpTransportConfig`/`sendEmail` (SSL і `debug_mode` — прапорці транспорту nodemailer, `queue_delay_ms` — реальна затримка перед відправкою, `max_attachment_mb` — відсічка до з'єднання); екран `/admin/settings/integrations` показує їх тенанту з людськими підписами (докс/09 §9.7.1). `ignore_tls_errors` — виняток за текстом §9.7.1 п. 3 («видно тільки оператору платформи»): тенантський `PUT` його відхиляє (`PLATFORM_ONLY_KEYS`), значення ставить лише `PUT /platform/tenants/:id/smtp-tls` (`platformTenants.ts`, журнал у `platform_audit`). Залишок: власного екрана панелі оператора в Lola немає взагалі (жодної сторінки `app/pages/platform/**`) — тумблер для цього прапорця можна поставити тільки прямим викликом API; заводити перший екран панелі оператора заради одного чекбокса — окрема, більша задача поза цим PR | M | opus | P2 |
| D-051 | `28` Паритет «Долги R1» | Push-уведомления (PWA) — R2, не делались | R2/R3 | — | — | — | — |
| D-052 ✅ (#64, `debts-8`) | `28` Spec 24 отк. (3) | ~~«Переглянути систему як роль» (`preview-as`) не сделано~~ | код | Сделано: `POST /settings/roles/preview-as` — `sessions.preview_role_id` (миграция 0049), `loadAccess` при заданому `preview_role_id` рахує права рівно по цій ролі (область — весь тенант), власні ролі актора на час перегляду не діють; не можна «приміряти» роль зі скоупом, якого немає у самого актора (`scope_not_owned`, Г-24.1); мутації заборонені `03.guards` (403 `preview_forbidden`), крім самого виходу (`DELETE .../preview-as`); плашка в `app.vue` як у impersonate, кнопка на `/admin/settings/roles`. | M | opus | P3 |
| D-053 ✅ (#64, `debts-8`) | `28` Spec 24 отк. (4) | ~~Замок модуля «Доступно на тарифі» не работает — в схеме тарифов нет `plans.modules[]`~~ | код | Сделано: `plans.modules[]` (миграция 0049, `null` = без обмежень); `moduleLock()` в `03.guards` — 403 `module.plan_locked` з текстом «Доступно на тарифі «…»» (найдешевший за `sort` тариф без обмеження); `GET /settings/tenant` віддає `lockedModules` для підпису на `/admin/settings/policies`. Дефолтні тарифи лишені без обмежень — конкретна матриця «модуль → тариф» не описана в ТЗ, не вигадували. | M | opus | P3 |
| D-054 ✅ (#64, `debts-8`) | `28` Spec 24 отк. (5), Spec 25 отк. (1) | ~~`tenant_limits`: жёсткая блокировка операций при превышении (диск, SMS) и `limit_warning` 80% не подключены — лимиты только считаются~~ | код | Сделано: `media.ts#createUploadUrl` блокує завантаження понад `effectiveLimits().storageGb` (400 `media.storage_limit`, перевірка по поточному обсягу, не по нічному знімку); `channels.ts#sendSms` читає `effectiveLimits().smsPerMonth` замість жорсткої константи тарифу; `usage.ts#checkLimitsAndNotify` при кожному `collectUsage` — `limit_warning` (80%) / `limit_exceeded` (100%+) по людям/диску/SMS адміністраторам тенанта (`enqueueNotification`) і оператору платформи (`platform_audit` `tenant.limit_warning`/`tenant.limit_exceeded`). Навчання не зупиняється. | M | opus | P2 |
| D-055 ✅ (#64, `debts-8`) | `28` Spec 25 отк. (2) | ~~`api_per_minute` и `webhooks` из `tenant_limits` не читаются лимитерами (`validateBearer` держит константу 60/мин, вебхуки не ограничены)~~ | код | Сделано: `apiTokens.ts#validateBearer` читає `effectiveLimits().apiPerMinute` (фолбек — колишня константа 60, тепер `DEFAULT_API_PER_MINUTE`); `webhooks.ts#createEndpoint` рахує активні `webhook_endpoints` тенанта проти `effectiveLimits().webhooks` → 409 `webhooks_limit` при створенні понад ліміт. | M | opus | P2 |
| D-056 ✅ (#63, `debts-9`) | `28` Spec 25 отк. (3) | ~~Повторный запуск `media.process` для медиа в статусе `processing` после `resume` тенанта не делается автоматически~~ | код | Сделано: `resumeTenant` (`server/services/platformTenants.ts`) находит медиа тенанта в `processing` и переставляет `media.process` заново; `after.requeuedMedia` в `platform_audit`, тест на реальный `pgboss.job` в `tenancy.spec.ts` | S | sonnet | P3 |
| D-057 ✅ (#63, `debts-9`) | `28` Spec 25 отк. (5) | ~~`platform.request` пишется на каждый GET панели оператора — при росте числа операторов стоит ограничить~~ | код | Сделано: `server/middleware/01.session.ts` пишет `platform.request` только на не-GET (действия) и на GET карточки конкретного тенанта (`/platform/tenants/:id`, `/platform/tenants/:id/...`); заодно добавлен сам `GET /platform/tenants/:id` (см. D-064) | S | sonnet | P3 |
| D-058 ✅ (#63, `debts-9`) | `28` Spec 25 отк. (6) | ~~`scripts/extract-tenant.ts` (`25` §11) и обезличенный слепок (`25` §16.3) не делались~~ | код | Сделано: `scripts/extract-tenant.ts <tenantId> [--out] [--anonymize] [--skip-media]` — дамп всех таблиц с `tenant_id` в порядке зависимостей + копия S3-префикса `t/<id>/`; `--anonymize` чистит `request_context`/`ip`/`user_agent`/`password_hash` и типовые персональные столбцы по имени, результаты обучения не трогает; smoke-тест на dev-БД `tests/integration/extract-tenant.spec.ts` | M | sonnet | P3 |
| D-059 | `28` Spec 25 отк. (4) | Собственный домен клиента и Google-вход по `Host` вместо `?tenant=` | R2/R3 | — | — | — | — |
| D-060 ✅ (#63, `debts-9`) | `28` Spec 10 отк. (1) | ~~Ресурсы базы знаний не попадают в `/me/catalog` — ресурсам не хватает `is_catalog_visible`/`assign_mode`~~ | код | Сделано: миграция 0048 добавляет `resources.is_catalog_visible`/`assign_mode` (тот же словник, что у курсу); `catalogResources()` (`server/services/resources.ts`) — збирач по тому самому доступу, що й прямий перегляд ресурсу (`canAccessResource`, не окремий тумблер каталогу); включено в `GET /me/catalog?kind=tasks` поруч із курсами (`type: 'resource'`), мінімальна правка `learn/catalog.vue` (відкриває ресурс напряму — самозапис/заявка для ресурсу не заведені, зафіксовано як залишок); тести `tests/integration/debts9-resource-catalog.spec.ts` | M | sonnet | P2 |
| D-061 ✅ (#62, `debts-7`) | `28` Spec 10 отк. (2) | ~~Заявки на программы, созданные до Spec 17 без `requested_at`, не показываются в `/manage/catalog/requests`~~ | код | Сделано: міграція `0047` — data-only бекофіл `requested_at = created_at` для `enrollments`(`source=catalog`)/`program_enrollments`/`trajectory_enrollments` зі `status='not_assigned'` і порожнім `requested_at`; тест `debts-7.spec.ts`. | S | sonnet | P3 |
| D-062 ✅ (#62, `debts-7`) | `28` Spec 10 отк. (3) | ~~`anyAdmin()` в маршрутизации комментариев берёт первого administратора без учёта `valid_until`/блокировки~~ | код | Сделано: `comments.ts#anyAdmin` фільтрує за `users.status='active'`, `not is_blocked` і `valid_until is null or > now()` — той самий критерій, що й `securityLog.ts#alertAdmins`; тест у `spec10-catalog.spec.ts`. | S | sonnet | P3 |
| D-063 ✅ (#63, `debts-9`) | `28` §28.4, класс «в» | ~~Единый эндпоинт `/content*` над 11 типами контента не сделан — методист работает по каждому типу отдельно~~ | код | Сделано: `server/services/contentCatalog.ts#listAllContent` + `GET /content?q=&type=&page=&perPage=` — агрегатор поверх уже существующих таблиц (courses/programs/resources/quizzes/complexTests/workshops/surveys/assessmentForms/checklists/meetups), тип виден только тому, у кого уже есть скоуп его «рідного» CRUD; тесты `tests/integration/debts9-content-catalog.spec.ts` | M | sonnet | P3 |
| D-064 🟡 (#63, `debts-9`; `debts-final-b`, частично, отложено) | `28` §28.4, класс «в» | Ещё ~110 путей `04` без прямого аналога (`/gift-store`, `/bonuses` — R3; `/saved-reports`; `/reports/trajectory`, `/reports/assessment`, `/reports/checklist` по `:id`; единая `/comments`; `/criteria-groups` и др.) — формы путей либо R3, либо расходятся с уже принятыми решениями Spec 20/22 | код | Сделано частично: `debts-9` добавил `GET /platform/tenants/:id`. `debts-final-b` (23.09.2026) прогнал `routes-parity` заново (108 путей без обробника) и разобрал их таблицей на 4 класса в `28` §28.4: (а) 7 алиасов сделаны в этом PR — `PATCH/DELETE /courses/:id/items/:id` (→ `/lessons/:id`), `PATCH/DELETE /tests/:id/question-groups/:id` (→ `/question-groups/:id`), `PATCH /tests/:id/questions/:id` (→ `/questions/:id`), `PATCH /scales/:id` (реэкспорт PUT), `DELETE /criteria/:id` (→ `/assessment/criteria/:id`) — покрытие 108 → 101 путей (`pnpm exec vitest run tests/integration/routes-parity.spec.ts` подтверждает, регресса нет); (б) R2/R3 без действия — `/gift-store/*`, `/bonuses/*`, `/me/bonuses`, `/me/badges` (10 путей); (в) реальный функционал, которого нет — вынесен восемью новыми строками D-077…D-084 (выше) с оценкой, без кода в этом PR; (г) форма пути — решение или уже устаревший текст `04` (77 путей: `/assessments`→`/assessment/forms`, `/criteria-groups`→`/assessment/groups`, `/checklists`/`/complex-tests`/`/polls` — PUT-upsert вместо CRUD, `/goals`/`/goal-statuses`/`/requests`→`/development/*` с иной моделью, `/content*` — сознательно только витрина без записи (D-063), `/settings/translations` по `?key=` вместо пути, дерево курса и тело урока уже отдаются целиком (`GET /courses/:id`, `POST .../lessons/:lessonId/open`), health/ready/metrics вне `/api/v1` — как и раньше, плюс новое: 4 пути — артефакт парсера теста на ячейках `GET/POST | путь1, путь2` (перекрёстное произведение методов на путях вместо построчного), `{cancel,decide}` в одной ячейке `04` не разворачивается парсером — оба метода уже реализованы отдельными путями). Подробности и полный список — `28` §28.4. | L | sonnet | P3 |
| D-065 ✅ (#62, `debts-7`) | `31` Certificates, `28` Spec 24 «Экраны» | ~~Экран `Certificates` — только сводка по курсам (видано/дійсних/відкликано), таблицы «Назва · Формат · Видано · Термін дії» из мокапа нет~~ | код | Уточнение: таблиця шаблонів по мокапу вже була зроблена раніше (PR #41) — рекомендація застаріла. Реальний борг: `GET /certificates` (список виданих) і `POST /certificates/:id/revoke` мали API, але жоден екран їх не викликав. Сделано: `/admin/certificates` — «Переглянути видані» на рядку курсу, пошук за ПІБ/номером, фільтр курс/стан, відкликання з причиною (форма, мінімум 10 символів) поверх наявного сервісу (`listCertificates` дістав фільтри й `fullName`). | S | sonnet | P2 |
| D-066 ✅ (#63, `debts-9`) | `31` (сверка с `28`) | ~~Карта мокапов `31` не обновлена после Spec 15/17/24: экраны `TaskParams`, `TaskAudience`, `QuestionEditor`, `Settings`, `Roles`, `Translations`, `TenantStats`, `ContentResources`, `ResourceForm` помечены ❌, хотя по `28` уже реализованы — карта не синхронизирована~~ | код | Сделано: звірено з `app/pages/**` — `TaskParams`/`TaskAudience` (Spec 15), `Trajectory` (Spec 17, і виправлено сам шлях — граф-редактор на `/admin/trajectories/:id`, не `/admin/programs/:id`), `Settings`/`Roles`/`Translations`/`TenantStats`/`Scales` (Spec 24), `ContentResources`/`ResourceForm` (Spec 11), `QuestionEditor` (Spec 12) — усі реалізовані, ❌→✅ в `31`; `Certificates` (той самий пакет Spec 24) — 🟡, бейджі свідомо R3 (коментар у коді) | M | sonnet | P3 |
| D-067 ✅ (#63, `debts-9`) | `28` §28.5 «Известные ограничения» | ~~Визуальные тесты: фикстура `TaskCard` — назначение на курс, а мокап показывает тест; группы `MyTasks` (Прострочено/Цього тижня/Пізніше) не все покрыты одной фикстурой~~ | код | Сделано: фікстура `TaskCard` тепер квіз на 12 питань («Тест «Касова дисципліна на ТТ» · 12 питань» — дослівно з мокапу), а не курс; для `MyTasks` додано по картці на «Цього тижня»/«Пізніше». «Прострочено» — не борг фікстури: `taskGroupWhere('overdue')` (`enrollmentStatus.ts`) окрема від `new/planned/failed` група, а вкладка `active` (`app/pages/learn/index.vue`) запитує лише ці три — прострочені картки принципово живуть на окремій вкладці «Прострочено», мокап показує всі три секції разом (розбіжність мокапу з поведінкою застосунку, зафіксовано в коментарі й `ignore` структурної перевірки, а не приховано) | S | sonnet | P3 |
| D-068 | `29` Б.8 | Бейджі (гейміфікація) — в ТЗ упомянуты один раз, без описания правил | R2/R3 | — | — | — | — |
| D-069 | `28` Spec 19 «Рейтинг» | «Поточний рейтинг» считается количеством выполненного нарастающим итогом, а не баллами `points_ledger` (модуля геймификации ещё нет) | R2/R3 | — | — | — | — |
| D-070 ✅ (#69, `answers-1`) | `32` §Г.1, `29` Б.18 | ~~Прод на Proxmox: нужны VM (4 vCPU / 8 ГБ / 100 ГБ, по `26`) и домен для `<slug>.lola.app` или свой — сейчас развёртывание подготовлено (compose/скрипты/runbook), но не выполнено~~ | вопрос | Відповідь замовника 22.09.2026 (Q-01: а, `34`): прод лишається на `devbox` (Proxmox VM через Tailscale, 2 vCPU/4 ГБ) — окрема від стенду машина вже виконує роль прод-сервера; постійний домен `app.lmscappi.pp.ua` вже підключено. Виділена VM 4 vCPU/8 ГБ по `26` — окремою задачею, коли знадобиться за навантаженням | L | sonnet | P1 |
| D-071 ✅ (#69, `answers-1`) | `32` §Г.3 | ~~Ссылка на демо-стенд — быстрый туннель Cloudflare, адрес меняется при перезапуске машины~~ | вопрос | Відповідь замовника 22.09.2026 (Q-02: а, `34`): постійний домен вже є — `app.lmscappi.pp.ua`, замінює тимчасове посилання `trycloudflare.com` | S | sonnet | P2 |
| D-072 ✅ (#78, `hygiene-1`) | агенты 22–23.09 | ~~`drizzle-kit generate` падал с ошибкой коллизии снапшотов между миграциями 0049/0050 (`server/db/migrations/meta`)~~ | код | Сделано: причина — снапшоты 0049 и 0050 сгенерированы параллельно двумя агентами (`debts-8` и `debts-4`) от одного предка 0048, из-за чего `prevId` разошлись и хвост цепочки (0050–0052) не учитывал изменения друг друга; починено — восстановлена цепочка `prevId` (0048→0049→0050→0051→0052) и содержимое 0052 пересобрано из актуальной схемы `server/db/schema` (сами `.sql`-файлы миграций не трогали); `drizzle-kit generate` больше не создаёт диф, `pnpm db:migrate` на чистой БД проходит | S | sonnet | P2 |
| D-073 ✅ (#78, `hygiene-1`) | агенты 22–23.09 | ~~`tests/integration/auth.spec.ts` нестабилен — выбор «сотрудника» в `beforeAll` через `limit 1` без `order by` и без фильтра по `status`, а в сиде есть и `active`, и `invited` подходящие пользователи~~ | код | Сделано: запрос фильтрует `u.status = 'active'` и сортирует `order by u.id` перед `limit 1`; причина — параллельные агенты работали на одной dev-БД (см. `docs/00` о риске), порядок строк без `order by` не гарантирован и мог вернуть `invited`-пользователя | S | sonnet | P2 |
| D-074 ✅ (`debts-final-b`) | `31` рядок `ContentWorkshops` (`screens-8`) | ~~Колонка «Мітки» показати нема з чого: у таблиці `workshops` немає колонки `tags`, і в `TAG_SCOPES` (`shared/enums.ts`: `user \| course \| resource \| question \| task`) немає значення під практикуми~~ | вопрос | Рішення замовника 23.09.2026: «Мітки» у практикумів не заводимо. Колонку прибрано з `/admin/workshops` разом з i18n-ключем `workshop.col.tags` (не вигадуємо `tag_scope`, CLAUDE.md п. 13); `docs/31` рядок `ContentWorkshops` → ✅. | S | sonnet | P3 |
| D-075 ✅ (`debts-final-b`) | `31` рядок `ContentComplexTests` (`screens-8`) | ~~Колонка «Теми» показати нема з чого: у складі комплексного теста немає поняття «тема» (лише частини-тести `parts`) — не визначено, чи це мітки складових тестів, чи окреме нове поле~~ | вопрос | Рішення замовника 23.09.2026: «Теми» у комплексних тестів не заводимо. Колонку прибрано з `/admin/meetups/complex` разом з i18n-ключем `cx.col.topics`; `docs/31` рядок `ContentComplexTests` → ✅. | S | sonnet | P3 |
| D-076 | `09` §9.7 (`spec-integrations`) | Групи інтеграцій із мокапу `Integrations`/еталона — **ADFS**, **Entra ID**, **Active Directory**, **HRM**, **Google Analytics**, **AI provider** — названі в таблиці `09` §9.7 як заголовки груп, але жодних полів для них ніде в `docs/` не описано (навіть `16` §16.5, де AD/Azure/HRM згадані як джерела людей R2, відсилає за деталями саме до `09`, де їх нема) | вопрос | не вигадувати склад полів мовчки (CLAUDE.md п. «Чого не робити»): `SECRET_KEYS`/екран цих груп не заводили. «Вебінари» з тієї ж таблиці — виняток, не борг: це вже наявний `zoom` (docs/09 §9.1 «Zoom / Google Meet», OAuth-механіка §9.2 — поля вже описані і реалізовані). Коли замовник дасть перелік полів кожної групи (endpoint/tenant/client_id тощо) — заводити за зразком `smtp`/`telegram` | M | opus | P3 |
| D-077 | `28` §28.4, класс «в» (`debts-final-b`) | `GET /me/tasks/:id` — картка власного завдання (контент, строки, правила, прогрес) окремим ендпоінтом не зроблена; сьогодні клієнт збирає її з `/learning/my` (список) і per-type ендпоінтів | код | зробити тонку витрину поверх вже наявних сервісів (як `/content` над 11 типами, D-063), а не новий розрахунок | S | sonnet | P3 |
| D-078 | `28` §28.4, класс «в» (`debts-final-b`) | `POST /enrollments/:id/migrate-version` — перехід на нову версію матеріалу (`10` Г-10.1) не реалізовано, докс/04 сам позначає як борг | код | сервіс переприв'язки `enrollments`/`lesson_progress` на нову `content_version_id` зі збереженням прогресу, де це можливо | M | opus | P3 |
| D-079 | `28` §28.4, класс «в» (`debts-final-b`) | `GET /review/checklists` — черга заповнених чек-листів на узгодження відсутня; чіп «Чек-листи» на `/admin/review` просто веде на `/admin/checklists` (список, не черга на перевірку) | код | окремий сервіс-вибірка `checklist_runs` зі статусом «на розгляді» за аналогією з `review/queue` (`reviewQueue`) | M | sonnet | P3 |
| D-080 | `28` §28.4, класс «в» (`debts-final-b`) | Адмінський список планів розвитку — `GET /development-plans` (по всій команді), `PATCH`/`DELETE .../:id` відсутні; є лише `myPlan` для однієї людини і переходи станом/наставник (докс/04 сам позначає як борг) | код | список — join по точці/підлеглих (як `development/goals/team`), видалення — з каскадом цілей і перевіркою активних призначень | M | opus | P3 |
| D-081 | `28` §28.4, класс «в» (`debts-final-b`) | `PATCH`/`DELETE /courses/:id/sections/:id` — немає сервісу оновлення чи видалення модуля курсу окремо, є лише створення (`addModule`) | код | `updateModule`/`deleteModule` в `server/services/courses.ts` за аналогією з `updateLesson`/`deleteLesson`; видалення — тільки порожнього модуля або з каскадом уроків, рішення окремо | S | opus | P3 |
| D-082 | `28` §28.4, класс «в» (`debts-final-b`) | `DELETE /tests/:id/questions/:id` — немає сервісу видалення окремого питання; питання використовуються в банках і знімках попыток (`snapshot`), видалення використаного може зламати історію | код | `deleteQuestion` з перевіркою `questionInSnapshots` (вже є для інших цілей) — 409, якщо питання зустрічається у знімку хоч однієї спроби | M | opus | P3 |
| D-083 | `28` §28.4, класс «в» (`debts-final-b`) | `GET/POST/PATCH/DELETE /competencies/:id/indicators` — поняття «індикатор компетенції» окремо від самої компетенції не заведено (є лише `competencies`, без дочірньої сутності) | код | нова таблиця `competency_indicators(competency_id, text, sort_order)` міграцією + CRUD-сервіс; перевірити, чи `19`/`02` взагалі описують «індикатори» окремо від компетенції — не вигадувати мовчки (CLAUDE.md п. 13) | L | opus | P3 |
| D-084 | `28` §28.4, класс «в» (`debts-final-b`), `31` нотатка «Goals-дерево» | `GET /goals` — адміністративний список/дерево цілей розвитку відсутній; цілі сьогодні доступні тільки в контексті плану людини (`development/goals/team`, `/development/plans/:id`) — інша модель даних, вже зафіксовано як окрема задача в `31` | код | окремий агрегуючий ендпоінт + екран `Goals`-дерева (`31`, `screens-7` нотатка) — не «одно поле», окрема сесія | M | sonnet | P3 |

## §А. Вопросы заказчику

Сгруппировано по модулю. Для каждого — рекомендация и вариант «если не ответите — делаем по
рекомендации» (как и в `29`/`32`, если ответа нет, продолжаем работу по указанному варианту, а
не ждём). Версия для операционного директора простым языком, без кода — с добавленным
Q-10 (adminTwoFactor) — в [34-questions-2026-09-21.md](docs/34-questions-2026-09-21.md).

**Прод и стенд**
- **D-070** ✅ (#69, `answers-1`). ~~Нужны VM и домен под прод. Если не ответите — прод остаётся на
  `devbox` рядом со стендом (текущая конфигурация `prod-proxmox`), выкатка по готовности.~~
  Ответ 22.09.2026 (Q-01: а): прод остаётся на `devbox` (это и есть отдельная от стенда машина,
  2 vCPU/4 ГБ), постоянный домен `app.lmscappi.pp.ua` уже подключён. Выделенная VM 4 vCPU/8 ГБ
  по `26` — отдельной задачей по нагрузке.
- **D-071** ✅ (#69, `answers-1`). ~~Постоянный адрес стенда (именованный туннель) или оставить
  меняющийся адрес `trycloudflare.com`. Если не ответите — оставляем как есть до появления
  домена из D-070.~~ Ответ 22.09.2026 (Q-02: а): постоянный адрес уже есть — `app.lmscappi.pp.ua`.

**Контент**
- **D-008** ✅ (#69, `answers-1`). ~~Формат «Імпортувати» для ресурсов/курсов ТЗ не описывает. Если не
  ответите — оставляем кнопку-заглушку до отдельного запроса.~~ Ответ 22.09.2026 (Q-03: а):
  формат ещё не прислали, поэтому кнопки «Імпортувати» на `/admin/courses` и `/admin/resources`
  убраны совсем (не заглушка) вместе с неиспользуемыми ключами i18n; импорт — отдельная задача,
  когда придёт пример файла.
- **D-009 (уточнение)** ✅ (#69, `answers-1`). «% успішності проходження» как результат курса сделано
  как доля зачтённых обязательных уроков (= прогресс), «Середній бал» — среднее по тестам
  плана, «За підсумковим тестуванням» — последний тест плана. Ответ 22.09.2026 (Q-04: а) —
  подтверждено как есть, код не менялся.

**Тести**
- **D-012** ✅ (#69, `answers-1`). ~~Нужно ли третье значение фильтра «Статус» у запроса попытки. Если
  не ответите — оставляем текущие два состояния (`approved`/`pending` по `decided_at`).~~ Ответ
  22.09.2026 (Q-05: а) — добавить статус. При аудите выяснилось, что статус и фильтр
  «Відмовлено» на `/admin/attempt-requests` уже реализованы (ещё с #36) — пункт ошибочно
  числился нерешённым, кода менять не потребовалось.

**Призначення**
- **D-018** ✅ (#69, `answers-1`). ~~Нужен ли в `02` отдельный справочник значений «Спосіб
  призначення». Если не ответите — оставляем выведенное значение без отдельного
  перечисления.~~ Ответ 22.09.2026 (Q-06: а) — буквально «завести справочник», но в рамках
  этого PR справочник не заводим (не выдумываем перечисление молча, CLAUDE.md п. 13), значение
  по-прежнему выводится автоматически. Справочник — отдельная задача с обсуждением списка.

**Заняття**
- **D-028** ✅ (#69, `answers-1`). ~~QR для отметки посещения — на экране участника (как в мокапе) или
  на экране тренера (как сделано, безопаснее). Если не ответите — оставляем текущий вариант
  (тренер).~~ Ответ 22.09.2026 (Q-07: а) буквально — «переделать под мокап», но заказчик
  подтвердил принятие текущей реализации (QR у тренера); код не менялся, `31` строка `Meetup`
  уточнена.

**Оцінка**
- **D-035** ✅ (#69, `answers-1`). ~~Правило «оцінка менша за норму» → комментарий обязателен,
  «вища/дорівнює» — только разрешён. Если не ответите — оставляем как есть (иначе не
  выполняется требование §13.2).~~ Ответ 22.09.2026 (Q-08: а) — подтверждено как есть, код не
  менялся.

**Хаб**
- **D-043** ✅ (#69, `answers-1`). ~~Связывать ли объявление-«Подію» (`notice_kind=event`)
  автоматически с событием хаба (`meetups(kind=event)`). Если не ответите — оставляем два
  независимых объекта.~~ Ответ 22.09.2026 (Q-09: а) буквально — «связать автоматически», но по
  факту решение — оставить два независимых объекта (разные жизненные циклы); код не менялся.
- **D-044** ✅ (#62, `debts-7`). ~~`birthday_today` уходит всем коллегам точки — шумно для
  больших точек. Если не ответите — переводим на ежедневный дайджест точки.~~ Відповіді не було
  до дедлайну — застосовано рекомендацію-фолбек: щоденний дайджест точки замість окремого
  сповіщення на кожного колегу (докс/33 D-044).

## §Б. План PR по долгам

Пакеты по 5–10 долгов, один пакет = одна ветка `debts-N`, один PR. Внутри пакета — один тип
задач (модель CLAUDE.md), P1 — первым пакетом. R2/R3-пункты (D-015, D-042, D-051, D-059,
D-068, D-069) в пакеты не включены — см. §В. Прод (D-070/D-071) — отдельный пакет, выполняется
только после ответа заказчика по D-070 (или по истечении срока ответа — тогда по рекомендации).

| Пакет | Модель | Долги | Оценка |
| --- | --- | --- | --- |
| `debts-1` (P1) ✅ (#58) | sonnet | D-004, D-005 | S+S ≈ 0.5 сессии |
| `debts-2` (P1/P2, безопасность и сервисы контента) ✅ (#59) | opus | D-011, D-006, D-007, D-009, D-013, D-019 | M×6 ≈ 3 сессии |
| `debts-3` (каталог, ресурсы, роли) ✅ (#61) | sonnet | D-001, D-002, D-003, D-008, D-010, D-016, D-017 | ≈ 2.5 сессии |
| `debts-4` ✅ (#65) (тесты, назначения, компетенции — сервисы) | opus | D-014, D-020, D-021 (`adminTwoFactor` отложено), D-034, D-036, D-039 | L+M+M+L+L+M ≈ 6.5 сессии |
| `debts-5` ✅ (#60) (люди, траектории, занятия — конфиг/вёрстка) | sonnet | D-022, D-023, D-025, D-026, D-028 (🟡 без змін, вопрос), D-032, D-033, D-037, D-038 | ≈ 3 сессии |
| `debts-6` ✅ (#66) (траектории, занятия, отчёты — сервисы) | opus | D-024, D-027, D-029 (⏸ тоді відкладено, закрито окремо в `debts-final-a`), D-030, D-031, D-045, D-047 | L+M+L+M+L+M+L ≈ 7.5 сессии |
| `debts-7` ✅ (#62) (оценка, хаб, отчёты — вёрстка/конфиг) | sonnet | D-040, D-041, D-043 (🟡 ждёт відповіді), D-044, D-046, D-061, D-062, D-065 | ≈ 2.5 сессии |
| `debts-8` ✅ (#64) (развитие, уведомления, настройки — сервисы) | opus | D-048, D-049, D-052, D-053, D-054, D-055 | M×6 ≈ 3 сессии |
| `debts-9` ✅ (#63) (мультитенантность, API, документация) | sonnet | D-056, D-057, D-058, D-060, D-063, D-064 (частично, отложено), D-066, D-067 | ≈ 3.5 сессии |
| `debts-prod` ✅ (#69, `answers-1`) — ответ по D-070 получен 22.09.2026 (Q-01/Q-02: а), закрыто пометкой (прод на `devbox`, домен `app.lmscappi.pp.ua` уже есть), без миграции инфраструктуры | sonnet | D-070, D-071 | L+S ≈ 1.75 сессии |

Итого 66 долгов из реестра распределены в 10 пакетов (9 обычных + прод), суммарно
**≈ 33 сессии**, из них opus — пакеты 2/4/6/8 (≈ 20 сессий), sonnet — 1/3/5/7/9/prod (≈ 13.75
сессии). P1 закрывается первым пакетом (`debts-1`) за полсессии.

## §В. Не делаем в R1

R2/R3-пункты по `00-overview.md` («Не-цели релиза 1») и `07-stages.md` (этапы 7–11 — R2,
хаб — R2/R3):

- **D-015 cloze** (`12`) — тип вопроса, в очереди эталона не первый по частоте; R2.
- **D-042 content_ratings** («Оцінок: N» у ресурсу, `21`) — оценка контента читателями, не
  влияет на прохождение; R2.
- **D-051 Push (PWA)** (`23` §4) — прямо назван R2 в ТЗ.
- **D-059 собственный домен клиента и вход через Google по Host** (`25`) — R2/R3, сейчас
  достаточно `?tenant=`.
- **D-068 бейджі** (`22` §4.8, `29` Б.8) — правила не описаны в ТЗ, часть геймификации R3.
- **D-069 рейтинг на `points_ledger`** (`19`, `02` §2.10 «Геймификация») — таблица начисления
  баллов не построена, R3; текущая формула (нарастающий счётчик выполненного) — временная и
  достаточна для R1.

Также форум, чат, рабочие задачи, магазин и бонусы (docs/00 «Не-цели релиза 1», docs/03 §3.22)
не входят в реестр — они не начинались и не являются доработкой существующего, это R3 по ТЗ
целиком.

**Добавление `screens-7` (22.09.2026).** Пакеты `screens-1…6` довели вёрстку экранов до
мокапов из `31`; `screens-7` закрыл точечные серверные пробелы под неё (`31`: `People`,
`ContentWorkshops`/`ContentComplexTests` — «Автор», `MyTrajectory`, `DevelopmentPlanMobile`,
`Competencies`, `PositionProfile`). Осознанно не тронуты (каждая — отдельная задача, не
«одно поле/join»): группы налаштувань `NotificationTemplates`/`NotificationTemplate`/
`Integrations` (канали шаблонів, SMTP-провайдери, `Аутентифікація`/`Джерела користувачів` —
нової функціональності немає, `screens-6`); ~~`DevelopmentPlans` як окрема адмінська сутність
(немає ендпоінта списку планів по тенанту); `Goals`-дерево (інша модель даних — окрема
таблиця й сервіс)~~ — закрито в `spec-development` (`docs/28` «Spec 19 (продовження)»): список
і картка планів (`GET /development/plans[/:id]`, `PATCH`), дерево цілей на `development_goals`
(`parent_id`/`is_strategic`, не окрема таблиця); D-042 `content_ratings` (Knowledge, R2, вище в
реєстрі) — без змін.

**Добавление `screens-8` (23.09.2026).** Точечно добраны `MyTasks` (тип завдання `kind` у
`/learning/my` — курс-обгортка з єдиним обов'язковим уроком-тестом тепер підписана «Пройти»),
`Catalog` (картка веде на попередній перегляд «Детальніше», сам запис/заявка/відкриття —
кнопка всередині панелі), `Lesson` (власний плеєр відео поверх нативного `<video>`, без
бібліотек), `Workshop` (окрема кнопка «Зберегти чернетку» прибрана — чернетка тепер зберігається
автоматично при доданні/видаленні фото, як і раніше при виході з поля коментаря), `Assessment360`
(кроковий режим по групі критеріїв з прогресбаром, «Далі»/«Назад»; подана/протермінована оцінка
лишається переглядом усіх груп одразу), `Meetup` («Скасувати запис» — липка кнопка на всю ширину
над нижньою панеллю вкладок, а не чіп серед інших дій). Осознанно не тронуты: `TestResult` і
`Profile` — плашка бонусів/«Магазин», R3 (`D-069`, вище); `ContentWorkshops`/`ContentComplexTests`
— «Мітки»/«Теми» (D-074, D-075: немає `tag_scope` під практикуми і поняття «тема» у складі
комплексного теста — новое перечисление/поле мовчки не заводимо, CLAUDE.md п. 13).

**Добавление `spec-development` (23.09.2026).** Закрито два довги «окрема задача» зі `screens-7`:
`DevelopmentPlans` (`31`) — адмінський список планів (людина · ціль · період · кроків · прогрес ·
стан, вкладки «Активні · Неактивні · Виконані») і картка з редагуванням, видимість за скоупом
(`development.manage` — уся мережа, `development.team` — свої точки, узагальнений
`scopeForGrants()` поруч із `reportScope`); `Goals` (`31`) — дерево «Стратегічний план» на
`development_goals` (`parent_id`/`is_strategic`, не окрема таблиця), статус і протокол — той
самий механізм цілей ІПР. Подробиці рішень — `docs/19` §3.4/§14.4, `docs/02` «Развитие»,
`docs/28` «Spec 19 (продовження)». Не робили: видалення плану (лишається долгом «Spec 04»).

**Решения заказчика 23.09.2026 (`debts-final-b`).** По D-074/D-075 (выше — «Мітки» у практикумів
і «Теми» у комплексних тестів) получен прямой ответ, не через docs/34: обе колонки заводить не
нужно. Убраны из `/admin/workshops` (`app/pages/admin/workshops/index.vue`) и
`/admin/meetups/complex` (`app/pages/admin/meetups/complex.vue`) вместе с i18n-ключами
`workshop.col.tags`/`cx.col.topics` (использований больше не было — сняты из `uk.json`/`en.json`
целиком, не только из шаблона). D-074/D-075 → ✅; `docs/31` строки `ContentWorkshops`,
`ContentComplexTests` → ✅ (остаток был только в этих двух колонках).
