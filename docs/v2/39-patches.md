# Lola LMS — ТЗ. 39. Патчи к базовому ТЗ

## Назначение

Пакет `lola-spec-v2` добавляет одиннадцать модулей. Часть из них не может работать, пока в
базовом ТЗ не изменены существующие документы: в каталоге людей появились кандидаты, в
каталоге курсов — этапы, в очереди проверки — делегирование. Этот документ перечисляет **все**
такие изменения точечно: какой документ, какой раздел, что заменить и почему.

Правило применения: патч применяется к базовому документу правкой **с пометкой-исправлением**,
а не тихой заменой. В базовом документе остаётся строка вида
`> [исправлено пакетом v2, П-16.1] Ранее: «…». Причина: …`.

Патчи отсортированы по номеру базового документа. Приоритет: **блокирующий** — без него
пакет ломает существующее поведение; **обязательный** — без него новый модуль неполон;
**желательный** — улучшение, можно отложить.

---

## П-01. `01-roles.md` — новые скоупы

**Приоритет:** блокирующий.
**Что:** в таблицу скоупов добавить все скоупы, введённые пакетом.

**Единственный источник истины по составу — `docs/v2/41-api-delta.md` §2**, где скоупы сведены
с эндпоинтами. Таблица ниже — обзорная, по подсистемам; при расхождении с модульным
документом прав прав модульный документ, а этот патч пересобирается.

[решение] Имена скоупов приводятся к одному виду `домен.действие` (нижний регистр, точка).
Сводка `41` §8.5 зафиксировала шесть имён, которые названы в этом патче, но отсутствуют в
модульных документах (`vacancy.integration.manage`, `interview.scenario.edit`,
`storage.manage`, `review.capacity.manage`, `person.note.view`, `absence.manage`), и
расхождение количеств. Это расхождение закрывается при реализации так: **побеждает имя из
модульного документа**, а недостающие права добавляются туда, где описан соответствующий
экран. Отдельного скоупа не заводится там, где хватает существующего.

| Скоуп | Документ | Кому по умолчанию |
|---|---|---|
| `candidate.view`, `candidate.edit`, `candidate.assign`, `candidate.decide`, `candidate.hire`, `candidate.delete`, `candidate.status.manage` | 28 | рекрутер, HR, админ |
| `vacancy.view`, `vacancy.edit`, `vacancy.publish`, `vacancy.integration.manage` | 29 | рекрутер, HR, админ |
| `interview.listen`, `interview.scenario.edit`, `summary.send` | 30 | HR, админ; `interview.listen` — не наставнику |
| `library.use`, `library.publish` | 31 | автор, админ |
| `org.structure.view`, `org.structure.edit`, `org.structure.import` | 32 | все / HR / админ |
| `lifecycle.view`, `lifecycle.manage`, `offboarding.start`, `offboarding.complete` | 33 | все / админ / керівник точки / HR |
| `storage.manage` | 34 | админ |
| `billing.view`, `billing.manage` | 35 | владелец тенанта |
| `content_issue.view`, `content_issue.triage`, `content_issue.rescore` | 36 | автор, методист / админ |
| `review.delegate`, `review.capacity.manage` | 37 | наставник / HR |
| `person.note.view`, `person.note.write`, `person.document.manage`, `absence.manage` | 38 | HR, керівник точки |

**Почему блокирующий:** без регистрации скоупов роли пакета не назначаются, и любой новый
экран доступен либо всем, либо никому.

---

## П-02. `02-data-model.md` — ссылка на дельту

**Приоритет:** блокирующий.
**Что:** в конец документа добавить раздел «Дельта пакета v2» со ссылкой на
`docs/v2/40-data-model-delta.md` и список 73 новых таблиц одной строкой на подсистему.
**Почему:** `02-data-model.md` объявлен единственным источником истины по схеме. Если пакет
не отражён в нём, исполнитель спроектирует схему заново.

**Отдельно:** зафиксировать, что `users.hired_at` имеет тип `date` и пакетом **не
переопределяется** (см. `28` §3.2).

---

## П-03. `03-modules.md` — карта модулей

**Приоритет:** обязательный.
**Что:** добавить в перечень модулей одиннадцать новых пунктов со ссылками, и явно пометить:
- §3.7 «База знаний» — поведение уточнено: возможности этапа `knowledge` (`33` §3.3)
  отключают прогресс, дедлайн, оценку и сертификат.
- §3.8 «Опросы и обратная связь» — дополнено анонимностью и линейной шкалой (П-12.2),
  и **не путается** с `36-content-feedback.md`: там обратная связь по качеству контента,
  здесь опрос людей.
- §3.22 «Корпоративный хаб → Новости» — разведено с платформенной лентой (П-21.1).

---

## П-04. `04-api.md` — ссылка на дельту API

**Приоритет:** обязательный.
**Что:** добавить раздел со ссылкой на `docs/v2/41-api-delta.md` и упоминанием нового публичного
контура `/api/public/v1` (`29` §10), который не проходит через `withTenant()` в обычном виде.
**Почему:** публичный контур — единственное место в продукте, где запрос приходит без сессии;
он обязан быть виден в основном документе по API, а не только в модульном.

---

## П-05. `05-screens.md` — навигация

**Приоритет:** обязательный.
**Что:** перестроить боковое меню:

```
Навчання      → Каталог · Мої призначення · Бібліотека модулів [31]
Люди          → Співробітники · Кандидати [28] · Оргструктура [32]
Найм          → Вакансії [29] · Кандидати [28] · Співбесіди [30]      (виден при включённом этапе recruiting)
Перевірка     → Моя черга · Делеговані мною [37] · Делеговані мені [37]
Звіти         → … · Воронка найму [28] · Етапи [33] · Якість контенту [36]
Налаштування  → … · Етапи [33] · Статуси кандидатів [28] · Сховище [34] · Тариф [35]
```

[решение] Раздел «Найм» показывается, только если у тенанта включён этап `recruiting`. Для
тенанта, который взял Lola ради обучения, рекрутинг не должен занимать место в меню.

Добавить в `§5.14` десять новых экранов: `/candidates`, `/candidates/:id`, `/vacancies`,
`/vacancies/:id`, `/j/:token` (публичный), `/library/modules`, `/org-structure`,
`/offboarding`, `/storage`, `/settings/lifecycle`.

---

## П-07. `07-stages.md` — этапы внедрения и сквозные проверки

**Приоритет:** обязательный.
**Что:** заменить раздел об этапах согласно `docs/v2/42-stages-delta.md`.

Дополнить «Сквозные проверки соответствия эталону» четырьмя новыми (полные формулировки —
в `42` §5):
1. `grep` по кодам этапов (`'knowledge'`, `'attestation'`, …) в бизнес-логике вне справочника
   и миграций даёт пустой результат — все ветки идут через `stageCan()` (`33` §7.1).
2. Ни одна выборка людей не выполняется без явного фильтра по `users.kind` (П-16.1).
3. Перечень `media_assets.origin` в БД совпадает со списком `40` §4.
4. Каждая тенантная таблица пакета имеет RLS `enable` + `force` и tenant-first индекс
   (`40` §8).

---

## П-11. `11-content-lessons.md` — переиспользуемый контент

**Приоритет:** обязательный.
**Что:** добавить раздел «Переиспользуемые модули» со ссылкой на `31-module-library.md` и
зафиксировать, что `lessons.module_id` становится nullable (`31` §3), а урок может быть телом
библиотечного модуля.
**Почему:** без этого исполнитель прочтёт в базовом документе, что урок всегда принадлежит
разделу курса, и поставит `not null`.

---

## П-12. `12-tests-questions.md` — опросы и жалобы на вопрос

**Приоритет:** обязательный.

**П-12.1.** Добавить в раздел о типах вопросов: тип **«Лінійна шкала»** (числовой диапазон
с настраиваемыми границами и подписями концов) как восьмой тип. Снято со второго эталона.

**П-12.2.** В раздел об опросах добавить: переключатель **«Анонімне опитування»** и поле
**«Підказка для відповіді»** у открытого вопроса. Анонимность влияет на хранение: при
`is_anonymous = true` ответ не связывается с человеком на уровне выборки, а не только скрывается
в интерфейсе.

**П-12.3.** Добавить ссылку на `36-content-feedback.md` §7.7–7.8: жалоба на некорректный вопрос
**не блокирует попытку**, но время в форме жалобы компенсируется сдвигом дедлайна, а
подтверждённая жалоба запускает пересчёт результатов — и результат может только улучшиться.

**П-12.4.** Кнопка «Перерахувати» в отчёте по тесту получает второй вход — из карточки жалобы
(`36` §7.8). Логика пересчёта одна и та же, точек входа две.

---

## П-13. `13-workshops.md` — очередь проверки

**Приоритет:** обязательный.
**Что:** в раздел об очереди проверки добавить ссылку на `37-review-delegation.md` и
зафиксировать три изменения:
1. В очереди появляются работы **кандидатов**, а не только сотрудников
   (`review_queue_items.subject_kind`).
2. Появляется делегирование и четыре таба вместо одного списка.
3. Появляются метрики «Час на контент» и «Час на випробування», собираемые heartbeat-ами
   (`37` §7.10–7.12), а не разницей «открыл/закрыл».

**Отдельно (блокирующее замечание):** базовое ТЗ описывает `review_queue_items` как витрину
(`14` §3.3), а `37` делает по ней `alter table` на 21 колонку с собственным состоянием и
принимает на себя внешние ключи. Витрина этого не выдерживает.
[решение] `review_queue_items` повышается до полноценной таблицы с собственным состоянием и
строкой на единицу работы; её наполнение остаётся производным (пишется при завершении работы),
но удаление и перестроение «с нуля» запрещены. Внести в `14` §3.3 и в `02-data-model.md`.

---

## П-14. `14-certification.md` — сертификаты и офбординг

**Приоритет:** желательный.
**Что:** добавить: сертификат не выдаётся по курсам этапов с выключенной возможностью
`certificate` (`33` §3.3); при завершении офбординга ранее выданные сертификаты **не
отзываются** (`33` §7.7).

---

## П-15. `15-assignments.md` — назначения и возможности этапа

**Приоритет:** блокирующий.
**Что:** добавить правило: форма назначения читает возможности этапа курса и **не сохраняет**
ключи `params`, соответствующие выключенным возможностям (`33` §7.5). Не сохраняет — а не
сохраняет со значением по умолчанию.
**Почему блокирующий:** иначе у курса базы знаний в `params` окажется `deadline_mode`, и
отчёт «нарушены сроки» начнёт считать нарушения там, где сроков нет.

**Дополнительно:** зафиксировать, что вакансия (`29`) **не является** носителем правил
прохождения: она хранит шаблон параметров, который применяется созданием обычного
`assignments`. Инвариант 1 не нарушается.

---

## П-16. `16-people.md` — кандидаты в реестре людей

**Приоритет:** блокирующий. Это самый важный патч пакета.

**П-16.1.** Все выборки людей получают явный фильтр по `users.kind`.
До пакета `users` содержала только сотрудников, и выборки вида `select … from users where
tenant_id = …` были корректны. После миграции, вводящей `kind`, те же выборки начнут
возвращать кандидатов: в списке сотрудников, в адресатах рассылки, в отчётах по штату, в
подсчёте лимита активных пользователей, в выгрузках.

Требование: ни одна выборка людей не выполняется без явного `kind`. Проверяется сквозной
проверкой П-07.2. Рекомендуемая реализация — два представления или два репозиторных метода
(`employees()`, `candidates()`), а не ручной фильтр в каждом запросе.

**П-16.2.** Карточка человека дополняется четырьмя блоками из `38-people-extensions.md`:
лента активности, заметки, документы, нормы отсутствий; и блоком «Етап» из `33`.

**П-16.3.** В список сотрудников добавляется колонка рейтинга (`38` §3), с оговорками:
не выгружается по умолчанию, не может быть единственным условием массового действия,
публичной доски лидеров нет.

**П-16.4.** Руководитель человека определяется функцией `resolveManager()` из `32` §7.8;
`locations.manager_id` понижается до резервного источника. Переключение — по флагу тенанта
`org_structure_is_source_of_truth`.

---

## П-17. `17-programs-trajectories.md` — узлы графа и библиотека

**Приоритет:** обязательный.
**Что:**
1. Узел траектории может ссылаться на библиотечный модуль с закреплённой версией
   (`trajectory_nodes.library_version_id`, `31` §3).
2. Явно зафиксировать, что модель ветвления (`and / or / delay / stop_delay`) **сохраняется**,
   несмотря на то, что на втором эталоне ветвления нет вовсе (`33` §14). Это записано, чтобы
   при сверке со вторым эталоном никто не «упростил» граф.

---

## П-20. `20-assessment.md` — шкалы и оценки кандидата

**Приоритет:** обязательный.
**Что:** `candidate_scores` (`28` §3.4) и `vacancy_criteria` (`29` §3) используют существующие
`scales` / `scale_levels`. Зафиксировать в `20`, что реестр шкал обслуживает и рекрутинг.

**Замечание о порядке этапов:** `scales` в базовом ТЗ отнесены к R2–R3, а рекрутинг нужен
раньше. [решение] Минимальный срез `scales` / `scale_levels` (без процедур оценки персонала)
переносится в тот же этап, что и рекрутинг. Отражено в `42`.

---

## П-21. `21-hub.md` — две разные «новости»

**Приоритет:** обязательный.
**Что:** развести две сущности, которые на втором эталоне называются одним словом:

| | Кто пишет | Кто видит | Таблица |
|---|---|---|---|
| Новости компании | администратор тенанта | сотрудники тенанта | существующая, `21-hub.md` §«Новости» |
| Объявления платформы | оператор платформы | все тенанты, только чтение | новая, `platform_announcements` |

[решение] `platform_announcements` — платформенная таблица без `tenant_id`, вне RLS, с
опциональной адресацией (всем / по тарифу / конкретным тенантам) и отметкой прочтения на
пользователя. Описание — в `24-settings-platform.md` (П-24.2), одна таблица, 8 полей.
**Почему важно:** на эталоне раздел «Новини» — лента вендора, а клиент создавать новости не
может. Если скопировать это буквально, тенант потеряет свою корпоративную ленту, которая в
базовом ТЗ уже есть.

**Также:** публичная оргструктура (`21-hub.md`) заменяется витриной из `32` §5 — тем же
деревом в режиме только для чтения. Две реализации дерева не нужны.

---

## П-22. `22-reports.md` — новые отчёты

**Приоритет:** желательный.
**Что:** зарегистрировать двенадцать новых отчётов пакета: воронка найма, эффективность
рекрутера, источники кандидатов, время до найма, отказы по причинам (`28` §9); люди по этапам,
скорость прохождения этапов, офбординг (`33` §9); качество контента (`36` §9); проверяющие
(`37` §9); укомплектованность структуры (`32` §9); потребление хранилища (`34` §9).

Все они должны быть доступны конструктору выгрузок (`22` §3.26), а не только как готовые экраны.

---

## П-23. `23-notifications.md` — новые коды уведомлений

**Приоритет:** обязательный.
**Что:** зарегистрировать все новые коды уведомлений пакета в реестре шаблонов. Полный
перечень и точный счёт — в `docs/v2/41-api-delta.md` §6 (106 новых кодов; `limit_warning` и
`limit_exceeded` уже существуют в базовом `24` §8 и не дублируются).

[решение] Имена кодов уведомлений приводятся к виду `домен.событие` через точку. Три пары
кодов, описывающих одно событие дважды (`storage_addon_activated` / `addon_activated`,
`vacancy.ai_quota` / `ai_ops_exhausted`, `candidate.limit_warning` / `limit_warning`),
схлопываются в один код каждая — победителем считается код документа-владельца события
(`35` для лимитов и аддонов, `29` для квоты ИИ в вакансиях).

**Отдельное правило:** уведомления кандидатам (`28` §8) идут **вне** тихих часов тенанта, но
внутри разумного окна 09:00–20:00 по локальному времени кандидата. Тихие часы настроены под
сотрудников одной сети; кандидат к этой сети не принадлежит.

**Отдельное правило:** дайджест руководителю (`23` §13.2.1) теперь получает адресата из
`resolveManager()` (`32` §7.8), а не из `locations.manager_id`.

---

## П-24. `24-settings-platform.md` — настройки тенанта и платформы

**Приоритет:** обязательный.

**П-24.1.** В настройки компании добавить шесть блоков, снятых со второго эталона:
- брендирование контента колонтитулом (название и логотип компании в материалах);
- двухфакторная аутентификация (TOTP) — включение на уровне тенанта;
- API-токен тенанта для публичного API (хранится в `tenant_secrets`, показывается один раз);
- выбор языков интерфейса, доступных сотрудникам, из поддерживаемых платформой;
- поведение таблиц (подгрузка при прокрутке) — настройка **на браузер**, не на аккаунт;
- нормы отпуска и больничного по умолчанию (`38` §3), с переопределением на точке.

**П-24.2.** Добавить раздел «Объявления платформы» — таблица `platform_announcements` (П-21.1),
экран оператора и лента только для чтения у тенанта.

**П-24.3.** Добавить связь **«посада → курси за замовчуванням»**: у должности задаётся набор
курсов, которые назначаются человеку при найме или переводе на эту должность. Снято со
второго эталона (колонка «Треки» в справочнике должностей). Используется в `28` §5.5.
Реализуется как правило автоматизации, привязанное к должности, — отдельной таблицы не нужно.

**П-24.4.** Раздел «Тарифы и лимиты» заменяется на `35-billing-limits.md`; в `24` остаётся
только панель оператора и ссылка.

**П-24.5.** Добавить сущность **«група посад»** (`position_groups`) — замеченную в API второго
эталона и не имеющую экрана. [решение] Заводим как необязательную группировку справочника
должностей: нужна, чтобы назначать курсы не каждой должности отдельно, а группе
(«кухня», «зал», «адміністрація»). Одна таблица, четыре поля, ссылка из `positions`.

---

## П-25. `25-multitenancy.md` — лимиты и публичный контур

**Приоритет:** обязательный.
**Что:**
1. Оси лимитов заменяются перечнем из `35` §7.1 (одиннадцать осей вместо трёх).
2. Правило «обучение никогда не останавливается лимитами» сохраняется дословно и
   распространяется на новые оси: при исчерпании ИИ-лимита проверка деградирует в ручную,
   при исчерпании хранилища сдача видеоответа сохраняется на устройстве и досылается (`34` §7).
3. Добавляется раздел о публичном контуре `/api/public/v1` (`29` §10): как он проходит
   изоляцию тенанта, если сессии нет. Тенант определяется по `public_token` вакансии, и
   дальше запрос идёт через `withTenant()` с системной ролью минимальных прав.

---

## Сводка приоритетов

| Приоритет | Патчи | Смысл |
|---|---|---|
| Блокирующий | П-01, П-02, П-15, П-16.1, П-13 (замечание) | без них пакет ломает существующее поведение |
| Обязательный | П-03, П-04, П-05, П-07, П-11, П-12, П-13, П-17, П-20, П-21, П-23, П-24, П-25 | без них новый модуль неполон |
| Желательный | П-14, П-16.3, П-22 | улучшение, можно отложить |

**Порядок применения:** сначала блокирующие, затем обязательные в порядке номеров базовых
документов. П-16.1 применяется **в той же миграции**, что и добавление `users.kind`, — иначе
между миграцией и патчем существует окно, в котором кандидаты попадают в списки сотрудников.
