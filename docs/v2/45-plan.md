# Lola LMS — ТЗ. 45. План внедрения пакета `docs/v2`

**Фаза 2 задания `docs/v2/HANDOFF.md` §4.** Документ пересобирает `docs/v2/42-stages-delta.md`
под фактическое состояние репозитория и заменяет его в части нарезки и нумерации: `42`
остаётся источником обоснований (§4), сквозных проверок (§5) и рисков (§7), а список работ —
здесь.

Вход: `43-reconciliation.md` (что уже есть), `44-decisions.md` (19 принятых решений),
секции 13 «Критерии приёмки» документов `28`–`38`, `39-patches.md`.

**Кода в этой фазе не написано ни строки.**

---

## 0. Что изменилось против `42-stages-delta.md`

| `42` говорил | Факт | Следствие для плана |
|---|---|---|
| Этапы 12–17 по 2–9 недель | Этап — не единица работы: `pnpm check` должен проходить на каждом шаге | Единица — **PR** размером 1–3 сессии агента. 6 этапов → **40 PR** |
| Миграции `0004`–`0028` | В репозитории 54 миграции `0000`…`0053`, стиль `NNNN_<область>_<что>.sql` | Нумерация с **`0054`**, стиль `NNNN_v2_<область>_<что>.sql`; точный номер назначается при rebase (§5) |
| `0004_extensions.sql` — первый файл пакета | `ltree` и `vector` созданы в `0000_youthful_warhawk.sql` (`43` Р-5) | Миграция отменена, из плана исчезла |
| Срез `scales` переносится вперёд отдельной миграцией | `scales`/`scale_levels` существуют и используются тремя модулями (`43` Р-4) | Работа исчезла; `candidate_scores` ссылается на готовые таблицы |
| 73 таблицы | 70 (`org_structure_conflicts`, `person_notes`, `storage_quota_addons` не создаются) плюс `review_queue_items` заводится с нуля решением В-2 | **71 `create table`** и **15 таблиц под `alter`** |
| `0028_rls_delta` единым файлом | Момента, когда в базе есть все 70 таблиц, при поэтапном накате нет | RLS применяется в той же миграции, что и таблицы; сводная проверка — контрактный тест №6 в PR-40 |
| Этап 12 — `33` целиком + срез `scales` | `33` разложен на три PR, между ними встраивается `users.kind` | См. §3, дорожка Т1 |
| Этап 17 — оргструктура последней | Оргструктура ни от чего в пакете не зависит и может идти параллельно с 13-го PR | Дорожка Т7 запускается сразу после PR-12 |

---

## 1. Единица работы и правила

**PR — это единица.** Размер подобран так, чтобы `pnpm check` (lint → typecheck →
`test:quiet` → `e2e:quiet`) проходил на каждом, а работа влезала в **1–3 сессии агента**.
Ветка — `<область>-<номер>` по правилу `CLAUDE.md`, с префиксом пакета: `v2-<область>-NN`,
где `NN` — номер PR из этого документа.

**Цикл PR** — `HANDOFF` §5, без отклонений: ветка от актуального `main` → миграция → сервис
в `server/services` → контракт zod в `shared/schemas` → тонкий эндпоинт → тесты → i18n и
токены → гейт → PR → строка в `46-progress.md`.

**Условия входа** PR — что должно быть смержено в `main` и какой факт проверяется командой
перед началом. **Условия выхода** — что должно стать правдой после мержа; они и есть вход
для зависимых PR. Оба обязательны: PR без них в работу не берётся.

**Модель исполнителя** — по таблице `CLAUDE.md` «Экономия контекста», не по умолчанию.
Opus — схема, миграции, сервис с бизнес-логикой, стык модулей, разбор противоречий.
Sonnet — документы, i18n, вёрстка по готовому мокапу, тесты по готовому образцу, скрипты, CI.

**Оценка** — в сессиях агента: 1 сессия ≈ ветка от начала до зелёного гейта без
переключения контекста. Сумма по плану — §7.

---

## 2. Обязательные связки

Из `HANDOFF` §4 и `44-decisions.md` §6. Нарушение любой — переделка больше чем одного PR.

| Связка | Где соблюдена | Почему |
|---|---|---|
| `33` (этапы и флаги возможностей) — раньше всего, что трогает назначения | PR-05 и PR-06 предшествуют PR-13 (кандидатам назначают курсы), PR-15 (шаблон параметров вакансии) и PR-33 (сдвиг дедлайна по отпуску) | Флаги этапа решают, какие ключи `params` вообще сохраняются (`33` §7.5). Написать фильтр после того, как `params` уже пишутся, — значит чистить данные |
| `28` (кандидаты) раньше `29` (вакансии) и `30` (ИИ-собеседование) | PR-13, PR-14 → PR-15…17 → PR-27…29 | `vacancy_applications.candidate_id → users`, `interview_sessions.candidate_score_id → candidate_scores` |
| Миграция `users.kind` и патч П-16.1 — **в одном PR** | PR-04, целиком | `44` В-14: между миграцией и патчем существует окно, в котором любая выборка возвращает кандидатов |
| В-4 (переименование `media_assets`) раньше В-6 (перечень `origin`) | PR-11 → PR-12 | `44` §6: снимок drizzle пересобирается один раз |
| В-10 (ключи разбивки хранилища) зависит от В-3 (коды этапов) | PR-05 → PR-12 (`media_assets.stage_code`) | Ключи разбивки = коды этапов; сравнимость между тенантами держится на том, что перечень платформенный |
| Контрактные тесты и сквозные проверки — **до** первой миграции пакета | PR-01 — первый в плане | Проверка П-07.1 сегодня проходит тривиально (кодов этапов нет). Написанная после миграции, она стоит в разы дороже (`43` §1.4, `44` В-3) |
| `35` платформенный слой раньше тенантного | PR-08 → PR-09 → PR-10 | `tenant_addons.addon_code → plan_addons.code` |
| Ось `candidates_active` раньше создания кандидатов | PR-09 → PR-13 | `42` §2.2: без счётчика кандидаты попадут в оплачиваемый счёт сотрудников |
| `review_queue_items` раньше делегирования и SLA | PR-18 → PR-19 | `review_sla_events.queue_item_id`; `44` В-2 |
| Зеркальные колонки `workshop_submissions` снимаются **через один PR** после перевода очереди | PR-18 → PR-19 → PR-20 | `44` §7: понижение до зеркала и удаление разнесены, чтобы откат первого шага не стоил миграции данных |

---

## 3. Дорожки и порядок

Восемь дорожек. Внутри дорожки — строго последовательно. Между дорожками — параллельно,
если выполнены условия входа. Ширина параллелизма — **до четырёх дорожек одновременно**
(после мержа PR-12).

```
Т0 документы     02 ─ 03
Т1 каркас        01 ─ 04 ─ 05 ─ 06 ─ 07
Т2 деньги                  08 ─ 09 ─ 10
Т3 файлы                        11 ─ 12 ──────────────────────── 36
Т4 рекрутинг и ИИ                     13 ─ 14 ─ 15 ─ 16 ─ 17 ─ 27 ─ 28 ─ 29
Т5 проверка и время              18 ─ 19 ─ 20        21 ─ 22
Т6 контент и библиотека               23 ─ 24        25 ─ 26
Т7 люди и структура                        30 ─ 31   32 ─ 33 ─ 34 ─ 35
Т8 хвост                                                      37 ─ 38 ─ 39 ─ 40
```

`01` блокирует всё. `04`, `05`, `12` — три узких места: пока они не смержены, дорожки Т4–Т7
не стартуют.

---

## 4. Список PR

Легенда столбца «М»: планируемые номера миграций. Фактический номер назначается при rebase
по правилу §5. «⟵» — условия входа (номера PR, которые обязаны быть в `main`).

---

### Дорожка Т1 — каркас и инварианты

#### PR-01 · `v2-guard-01` · Контрактные тесты пакета и сквозные проверки

**Статус: выполнено** (2026-09-23, ветка `v2-guard-01`; подробности и allowlist —
`docs/v2/46-progress.md`, запись «Фаза 3, PR-01»).

- **Модель:** sonnet (образцы — `tests/integration/rls.spec.ts`, `schema-parity.spec.ts`). **Оценка:** 2 сессии. **М:** нет.
- **Входит:** пять контрактных тестов `HANDOFF` §7.2 = `40` §8, по файлу на проверку в
  `tests/integration`: (1) `tenant_id` + RLS `enable`+`force` + политика с `using` и
  `with check`; (2) индекс, начинающийся с `tenant_id`, не частичный либо частичный плюс
  полный рядом; (3) внешний ключ на `tenants`; (4) перечень `media_assets.origin` совпадает
  с `40` §4.2 в обе стороны плюс «ровно один `check` по колонке `origin`»; (5) расширение
  `schema-parity.spec.ts` на таблицы пакета (нет `attempts`, `pass_score`, `due_at`,
  `time_limit`).
  Пять сквозных проверок `HANDOFF` §7.3 как исполняемый `scripts/v2-crosschecks.sh` + шаг
  в CI: коды этапов, фильтр по `users.kind`, обращение к драйверу мимо `withTenant()`,
  строка интерфейса мимо i18n, цвет и отступ мимо токенов.
  Плюс `tests/unit/migrations-journal.spec.ts` и починка журнала (см. ниже).
- **Патчи:** П-07 целиком (переписать `docs/07-stages.md` под шесть сквозных проверок пакета
  и список этапов из этого документа).
- **Решения:** обслуживает В-6, В-8, В-13 — тесты пишутся под них заранее.
- **Условия входа:** нет.
- **Условия выхода:**
  - `pnpm test` содержит пять новых файлов `tests/integration/v2-contract-*.spec.ts`,
    все зелёные **на текущей схеме**: тесты 1–3 и 5 перечисляют таблицы пакета через список,
    который сейчас пуст; тест 4 сформулирован как «если колонка `origin` существует, перечень
    совпадает» и становится содержательным после PR-12;
  - `scripts/v2-crosschecks.sh` возвращает 0 на текущем `main` и ненулевой код на
    искусственном нарушении (проверяется в самом тесте);
  - `tests/unit/migrations-journal.spec.ts` требует строгого возрастания `when` и совпадения
    порядка `when` с порядком `idx`/`tag`;
  - **исправлено нарушение, уже существующее в `main`:** `0053_spec_development_goal_tree`
    имеет `when = 1790116110662`, что **меньше** `when` у `0052` (`1790120023875`). На
    накаченной БД drizzle такую миграцию молча пропускает. Правится значением `when` у `0053`
    на `max(when) + 1000`; данные не затрагиваются.
- **Приёмка:** `42` §5 проверки 9–12 запускаются и дают пустой результат (это фиксируется как
  базовая линия, а не как достижение); новый unit-тест падает на подложенной записи журнала
  с меньшим `when`; `pnpm check` зелёный.

#### PR-04 · `v2-people-04` · `users.kind` и три слоя фильтра (П-16.1)

**Статус: выполнено** (2026-09-23, ветка `v2-people-04`; подробности, перечень переписанных
выборок и allowlist — `docs/v2/46-progress.md`, запись «Фаза 3, PR-04»). Номер миграции по
факту — `0056_v2_users_kind.sql` (`0054` и `0055` заняты базовым ТЗ, как и предупреждает §5).

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0054_v2_users_kind.sql`.
- **Входит** (состав задан решением В-14, ни строкой больше):
  миграция — `users.kind text not null default 'employee'`, констрейнт
  `users_kind_chk check (kind in ('employee','candidate'))`, частичный индекс
  `(tenant_id, status) where kind = 'employee'`, `tenants.candidates_enabled boolean not null
  default false`;
  слой 1 — `server/services/repo/people.ts` с `employees()` / `candidates()`;
  слой 2 — `tests/integration/users-kind-filter.spec.ts`: сканер исходников с **поимённым
  allowlist** (`44` В-8), новая выборка мимо репозитория падает по умолчанию;
  слой 3 — **канареечный кандидат** в посеве фикстур (`tests/integration/fixtures`), не менее
  трёх на тенант;
  переписаны ≈50 списочных выборок из ≈148 точек ревизии и пять мест счётчика
  (`usage.ts:33`, `platform.ts:74`, `:96`, `:198`, `:234`).
- **Патчи:** П-16.1.
- **Решения:** В-8, В-14.
- **⟵ PR-01.**
- **Условия входа:** `scripts/v2-crosschecks.sh` в CI; в базе нет ни одного кандидата
  (`select count(*) from users where kind='candidate'` не исполним — колонки нет; проверка
  сводится к тому, что PR-04 — первая миграция пакета).
- **Условия выхода:** `candidates_enabled = false` у всех тенантов; сквозная проверка 10
  (`42` §5) зелёная на посеве с канарейкой; список сотрудников, адресаты рассылок, отчёт по
  штату, счётчик `users_active` и выгрузка не содержат канареечного кандидата; allowlist
  сканера пуст или каждая строка в нём объяснена комментарием.
- **Приёмка:** `38` §13 к.3 (фильтр `rating_pct` — подготовка), `35` §13 к.1 и к.10 (счётчик
  активных), сквозные 10 и 15 `42` §5; `pnpm check` + `pnpm db:generate && pnpm db:migrate`
  на чистой БД.
- **Риск и откат:** `42` §7.1 целиком. Откат — только вперёд: выключение флага; `drop column
  kind` запрещён. До мержа снять дамп `users` и `tenant_usage`.

#### PR-05 · `v2-lifecycle-05` · Этапы жизненного цикла и `stageCan()`

**Статус: выполнено** (2026-09-23, ветка `v2-lifecycle-05`; миграция — `0057`, а не `0055`:
`0055` занята в `main`, `0056` зарезервирована за параллельным PR-04. Решения и отступления —
`docs/28-implementation-notes.md` §28.10, запись — `docs/v2/46-progress.md`).

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0055_v2_lifecycle_stages.sql`.
- **Входит:** таблица `lifecycle_stages` (8 посеянных этапов, `code` неизменяем и закрыт
  констрейнтом, `capabilities jsonb` — **фиксированный перечень**, неизвестный ключ даёт
  `422`, а не игнорируется, `applies_to_candidate`); `alter courses` +
  `lifecycle_stage_id`, `stage_locked`; `stageCan()` — единственная точка проверки
  возможностей; перечень `STAGE_CAPABILITIES` в `shared/enums.ts` и в `docs/02` §«Перечисления»;
  эндпоинты `/settings/lifecycle-stages/*`.
- **Патчи:** —. **Решения:** В-3 (гибридная модель категорий).
- **⟵ PR-01, PR-02, PR-03.**
- **Условия входа:** сквозная проверка 9 (`grep` по восьми кодам этапов) даёт пустой
  результат — база для сравнения снята.
- **Условия выхода:** восемь этапов у каждого нового тенанта; ни одного `if (stage.code ===
  '…')` в `server/` и `app/` — сквозная проверка 9 по-прежнему пуста (коды живут только в
  справочнике, посеве и миграции); `capabilities` с неизвестным ключом отклоняется с `422`.
- **Приёмка:** `33` §13 критерии 6, 11, 12 (`grep` по кодам, смена этапа у курса с
  завершёнными прохождениями, восемь этапов у нового тенанта); контрактные тесты 1–3, 5.

#### PR-06 · `v2-lifecycle-06` · Возможности этапа в назначении (П-15)

**Статус: выполнено** (2026-09-23, ветка `v2-lifecycle-06`; миграция — `0058`, а не `0056`:
`0056` и `0057` заняты PR-04 и PR-05. Решения и отступления — `docs/28-implementation-notes.md`
§28.11, запись — `docs/v2/46-progress.md`). Экран `/settings/lifecycle` сделан в PR-05; счётчик
людей в этапе остаётся за PR-07 (`employee_lifecycle_state` появляется там).

- **Модель:** opus. **Оценка:** 2 сессии. **М:** `0056_v2_lifecycle_params.sql` (индексы).
- **Входит:** фильтр ключей `params` назначения по возможностям этапа в `server/services/taskParams.ts`
  — **одна точка**; форма назначения скрывает выключенные поля; экран `/settings/lifecycle`;
  сквозная проверка 14 (`42` §5, SQL) в наборе.
- **Патчи:** П-15. **⟵ PR-05.**
- **Условия входа:** `lifecycle_stages` в `main`, `stageCan()` доступен.
- **Условия выхода:** SQL сквозной проверки 14 даёт ноль строк; назначение курса этапа
  «База знань» не сохраняет `deadline_mode`, `attempts_max`, `pass_score`.
- **Приёмка:** `33` §13 критерии 1, 2, 3, 4, 8.

#### PR-07 · `v2-lifecycle-07` · Офбординг и состояние человека в цикле

**Статус: выполнено** (2026-09-23, ветка `v2-lifecycle-07`; миграция — `0060`, а не `0057`:
`0057` и `0058` заняты PR-05 и PR-06, `0059` — параллельным PR-08. Решения и отступления —
`docs/28-implementation-notes.md` §28.13, запись — `docs/v2/46-progress.md`). Там же разведён
дубль раздела `28.9`, оставшийся от параллельных веток PR-04 и PR-05.

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0057_v2_offboarding.sql`.
- **Входит:** `employee_lifecycle_state`, `offboarding_cases`; сценарий офбординга,
  повторный найм, сертификаты при выходе.
- **Патчи:** П-14. **⟵ PR-04, PR-05, PR-06.**
- **Условия входа:** `users.kind` в `main` (повторный найм переводит `kind`).
- **Условия выхода:** офбординг завершается, повторный наём того же человека не создаёт
  второй записи `users`.
- **Приёмка:** `33` §13 критерии 5, 7, 9, 10.

---

### Дорожка Т0 — документы и скоупы (параллельно всему)

#### PR-02 · `v2-docs-02` · Правка документов пакета под решения фаз 0 и 1

**Статус: выполнено.** Ветка `v2-docs-02`, объединена с PR-03 в один PR (не пересекаются по
файлам, обе без миграций). Подробности — `docs/v2/46-progress.md`.

- **Модель:** sonnet. **Оценка:** 2 сессии. **М:** нет.
- **Входит:** все правки из `43` §5 «Что из пакета отменяется» и `44` «Что меняется в пакете»,
  каждая с пометкой `> [исправлено, …] Ранее: «…»` по `HANDOFF` §1: нумерация миграций в
  `40` §5, снятие `0004_extensions.sql` и ссылки на `0001_foundation.sql`, путь
  `/api/v1/public/*` вместо `/api/public/v1` в `29` §10, `media.too_big`, `validation_failed`,
  одно написание `forbidden`, `35` §10 без префикса `/api/v1`, «шесть новых таблиц» → восемь
  в `38` §3.7, `hired_at` в `38` §3.1, формула `35` §7.3 под явные колонки, `30` §7.7
  `media_purge_after` → `purge_after`, П-25.1 «три оси» → «шесть осей».
  Плюс П-02 (ссылка на дельту в `docs/02`), П-03 (карта одиннадцати модулей в `docs/03`),
  П-04 (ссылка на дельту API и описание работающего публичного контура),
  П-05 (навигация и десять экранов в `docs/05`), П-17.2 (строка о сохранении ветвления графа),
  П-20 (реестр шкал обслуживает рекрутинг).
- **Патчи:** П-02, П-03, П-04, П-05, П-17.2, П-20.
- **⟵ PR-01** (только ради порядка в `main`; конфликтов по файлам нет).
- **Условия выхода:** `grep -rln "docs/v2" docs/*.md` даёт непустой результат; ни один
  документ пакета не противоречит `43` и `44`.
- **Приёмка:** `pnpm lint` (markdown не проверяется, но ссылки — да), ручная сверка: каждая
  строка `43` §5 имеет адресата.

#### PR-03 · `v2-roles-03` · Новые скоупы (П-01)

**Статус: выполнено.** Объединена с PR-02 в один PR (файлы не пересекаются, обе без
миграций). `SCOPE_GROUPS` пополнен фактически в `server/services/roles.ts` (не в
`shared/domain/roles.ts`, где живёт только `SCOPES`) — код уже так устроен. Точный состав
скоупов взят из §2 каждого модульного документа, а не из обзорного перечня ниже — детали
и расхождения в `docs/v2/39-patches.md` П-01 и `docs/01-roles.md` §1.10.

- **Модель:** sonnet. **Оценка:** 1 сессия. **М:** нет.
- **Входит:** `SCOPES` и `SCOPE_GROUPS` в `shared/domain/roles.ts` пополняются группами
  `candidate.*`, `vacancy.*`, `interview.*`, `library.*`, `org.structure.*`, `lifecycle.*`,
  `storage.manage`, `billing.*`, `content_issue.*`, `review.delegate`, `person.note.*`,
  `absence.manage`; i18n-подписи; `docs/01-roles.md`.
- **Патчи:** П-01. **⟵ PR-01.**
- **Условия входа:** —.
- **Условия выхода:** редактор ролей показывает новые группы; `tests/integration/scopes-http.spec.ts`
  зелёный (скоуп без эндпоинта ничего не ломает — проверяется явным тестом).
- **Приёмка:** `31` §13 к.5 (`library.use` без `library.publish`), `30` §13 к.12
  (`interview.listen`) — подготовка; сами сценарии закрываются в PR-25 и PR-29.

---

### Дорожка Т2 — тариф, лимиты, деньги

#### PR-08 · `v2-billing-08` · Платформенный слой тарифа и одиннадцать осей (В-5)

**Статус: выполнено** (2026-09-23, ветка `v2-billing-08`; миграция — `0059`, а не `0058`:
`0058` занята параллельным PR-06. Пять мест сырого SQL, переведённых на общую
функцию, перечислены в `docs/v2/46-progress.md`; отступления — `docs/28-implementation-notes.md`
§28.12).

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0059_v2_billing_plans.sql`.
- **Входит:** `alter plans` +8 колонок (`title_uk`, `tier`, `ai_included`, `ai_term_days`,
  `addons_allowed`, `is_active`, `valid_from`, `valid_to`) — PK остаётся `code`;
  `plan_prices` (FK `plan_code`, не `plan_id`), `plan_addons`, `tenant_addons`;
  `alter tenant_limits` +8 колонок (`billing_period`, `status`, `paid_until`, `grace_until`,
  `ai_until`, `autorenew`, `currency`, `ai_status`) и доведение осей с 6 до 11 **явными
  колонками**; `LIMIT_AXES` в `shared/enums.ts` и `docs/02`; одна функция эффективного лимита.
- **Патчи:** П-25.1 (в редакции 6 → 11). **Решения:** В-5.
- **⟵ PR-01.**
- **Условия входа:** пять мест сырого SQL, сравнивающих потребление с лимитом, выписаны
  поимённо в описании PR.
- **Условия выхода:** `storage_quota_addons` не существует (сквозная проверка 23 `42` §5);
  число в баннере лимита и число в расчёте счёта берутся из одной функции; `jsonb` использован
  только для нетарифных осей.
- **Приёмка:** `35` §13 критерии 7, 9, 10.

#### PR-09 · `v2-billing-09` · Потребление, предупреждения, деградация

**Статус: выполнено** (2026-09-23, ветка `v2-billing-09`; миграция `0061_v2_billing_usage.sql`
— `0060` заняла параллельный PR-07. Подробности — `docs/v2/46-progress.md`, запись «Фаза 3,
PR-09»; решения при отступлениях — `docs/28-implementation-notes.md` §28.12.)

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0061_v2_billing_usage.sql` (план называл `0059`).
- **Входит:** `usage_counters`, `usage_events`, `limit_notices`; `alter tenant_usage` +8
  колонок (`plan_id`, `candidates_active`, `storage_by_category`, `ai_ops`, `sms_out`,
  `telegram_out`, `integrations_active`, `axes`); `details.axis` рядом с `resource`,
  дедупликация предупреждений переводится на `axis`, пять именованных кодов `limit.*`
  отменены; деградация вместо остановки обучения на новых осях.
- **Патчи:** П-25.2. **Решения:** В-16.
- **⟵ PR-04, PR-08.**
- **Условия входа:** `users_active` считается `where kind='employee'` (следствие PR-04).
- **Условия выхода:** SQL сквозной проверки 15 (`42` §5) даёт ноль строк; одно открытое
  предупреждение на ось и уровень.
- **Приёмка:** `35` §13 критерии 1, 2, 3, 5, 8.

#### PR-10 · `v2-billing-10` · Оплата, смена тарифа, экраны

**Статус: выполнено** (2026-09-24, ветка `v2-billing-10`; миграция `0073_v2_billing_payments.sql`
— `0060` заняла параллельный PR-07 (офбординг), план ушёл вперёд до `0072` к моменту ветвления.
Подробности — `docs/v2/46-progress.md`, запись «Фаза 3, PR-10».)

- **Модель:** sonnet (вёрстка по мокапам + тонкие эндпоинты поверх готового сервиса).
  **Оценка:** 2 сессии. **М:** `0073_v2_billing_payments.sql` (план называл `0060`).
- **Входит:** `tenant_payments`, `plan_change_requests` (`plan_code`/`from_plan_code`/
  `to_plan_code`, а не `*_plan_id` — В-5, доисправлено в `35` §3.5); отложенный FK
  `tenant_addons.payment_id → tenant_payments(id)`, заявленный в PR-08; `POST
  /platform/tenants/:id/payments` (приём платежа оператором вручную — обходной путь `44` §8);
  `POST /platform/tenants/:id/plan-change` (прямая смена тарифа оператором, без preflight,
  §7.10) и `POST /platform/tenants/:id/extend` (точечная правка `paid_until`/`ai_until` без
  платежа); `GET /billing/summary`, `GET /billing/payments`; экран `/admin/settings/billing` +
  `/admin/settings/billing/history` (путь `/settings/billing*` документа не существует в этом
  кодовой базе — все тенантные настройки живут под `/admin/settings/*`, см. пометку у `35` §5);
  панель оператора (`/ops`: диалоги «Тариф і оплата» и підтвердження зміни тарифу); `docs/24`
  раздел тарифов заменён ссылкой на `35`.
- **Не входит** (нет в «Входит» и ни в одном PR плана — задокументировано как решение, не как
  недосмотр): самообслуживание владельца (`/billing/plan-change/preflight`, `/billing/plan-change`
  POST/DELETE, `/billing/addons`, `/billing/plans`, экран §5.2, форма §6.1); каталог тарифов
  оператора `/platform/plans/:id`; прикрепление файла счёта к платежу (`invoice_media_id`
  заведён в схеме, форма загрузки — нет). Критерий приёмки 4 (`35` §13) закрыт частично: экран
  показывает `ai_status`/`ai_until` корректно, но функциональная блокировка «Згенерувати трек»
  не проверена — в репозитории нет ни одного эндпоинта ИИ-генерации, который мог бы её
  показать (`ai_generate_ops`/`ai_review_ops`/`ai_interview_ops` пока только считаются, но
  ничего не вызывает).
- **Патчи:** П-24.4. **⟵ PR-09.**
- **Условия выхода:** платёжный провайдер не подключён и в `.env.example` не появился;
  продление тарифа оператором работает от прежней даты окончания (тест
  `tests/integration/v2-billing-payments.spec.ts`, «оплата на 10-й день просрочки в readonly»).
- **Приёмка:** `35` §13 критерий 6 — полностью; критерий 4 — частично (см. «Не входит»).

---

### Дорожка Т3 — файлы и хранилище

#### PR-11 · `v2-storage-11` · `media_assets`: переименование и мягкое удаление (В-4, В-17, В-19)

**Статус: выполнено** (2026-09-23, ветка `v2-storage-11`, **одним PR вместе с PR-12** — оба
про `media_assets`, второй зависит только от первого, объединение экономит один цикл rebase;
миграции `0062_v2_media_rename.sql` и `0063_v2_media_origin.sql`, а не `0061`/`0062`: `0061`
заняла параллельный PR-09. Подробности — `docs/v2/46-progress.md`, запись «Фаза 3, PR-11+12»;
решения при отступлениях — `docs/28-implementation-notes.md` §28.14.)

- **Модель:** opus. **Оценка:** 2 сессии. **М:** `0062_v2_media_rename.sql` (план называл `0061`).
- **Входит:** `uploaded_by` → `owner_user_id`, `checksum` → `checksum_sha256`
  (переименование, не новые колонки); `deleted_by`, `delete_reason`; событие `media.upload`
  в `audit_log` (сегодня `media.ts` не пишет аудит ни разу); `DELETE /media/:id` —
  **единственная одиночная ручка удаления**, долг `docs/04-api.md` §4.15; журналирование
  скачивания файла-доказательства, ссылка 120 с вместо 600.
- **Решения:** В-4, В-17, В-19. **⟵ PR-01.**
- **Условия входа:** ни одного файла, у которого владелец ≠ загрузивший (окно закрывается
  первым резюме кандидата — `44` §7).
- **Условия выхода:** `POST /storage/upload-intent` не появился; массового
  `DELETE /storage/files` нет; снимок drizzle пересобран один раз.
- **Приёмка:** `34` §13 критерии 2, 6.

#### PR-12 · `v2-storage-12` · Перечень `origin` и остальные колонки `media_assets` (В-6, В-10)

**Статус: выполнено** (2026-09-23, ветка `v2-storage-11`, одним PR с PR-11; миграция
`0063_v2_media_origin.sql`. Контрактный тест №4 стал содержательным: условная формулировка
«если колонка существует» снята, ожидаемый перечень берётся из `shared/enums.ts`, а не
зашит четвёртой копией.)

- **Модель:** opus. **Оценка:** 2 сессии. **М:** `0063_v2_media_origin.sql` (план называл `0062`).
- **Входит:** **одной миграцией** — `check`-констрейнт `origin` в редакции `40` §4.2,
  15 значений, `MEDIA_ORIGINS` в `shared/enums.ts` и `docs/02`; 12 колонок: `course_id`,
  `stage_code` (вместо `category_id` — решение В-10), `enrollment_id`, `source_entity`,
  `source_id`, `lifecycle`, `is_evidence`, `retention_until`, `orphaned_at`, `purge_after`,
  `last_accessed_at`, и обязательность `origin` на единственном входе загрузки.
- **Решения:** В-6, В-10. **⟵ PR-05, PR-11.**
- **Условия входа:** `lifecycle_stages` в `main` (`stage_code` = коды этапов); переименование
  из PR-11 смержено.
- **Условия выхода:** контрактный тест №4 становится содержательным и зелёным; по колонке
  `origin` **ровно один** `check`; сквозная проверка 11 (`42` §5) зелёная.
- **Приёмка:** контрактный тест №4; `34` §13 к.5 (сводка показывает исходное значение).

#### PR-36 · `v2-storage-36` · Квота, корзина, сроки хранения

**Статус: выполнено** (2026-09-24, ветка `v2-storage-36`; миграция `0083_v2_storage_quota.sql` —
номер назначен при финальном rebase. Экраны — `/admin/settings/storage` с вкладками «Кошик» и
«Політики зберігання», а не `/storage`: тенантные экраны живут под `/admin/settings/*`, как
решил PR-10 для биллинга. Подробности — `docs/v2/46-progress.md`, запись «Фаза 3, PR-36»;
решения при отступлениях — `docs/28-implementation-notes.md` §28.19 и пометки в `34`.)

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0083_v2_storage_quota.sql` (план называл `0087`).
- **Входит:** `storage_usage_counters`, `storage_usage_daily` (разбивка по девяти ключам —
  8 кодов этапов + `other`), `storage_retention_policies`, `storage_deletion_requests`,
  `storage_pending_uploads`; задача `storage.purge`; экраны `/storage`; одна формула квоты.
- **⟵ PR-09, PR-12.**
- **Условия входа:** ответ владельца продукта о сроке корзины **не требуется**: 30 дней живут
  строкой `storage_retention_policies`, `purge` помечает строки и освобождает квоту, но объект
  в S3 не удаляет (`44` §8).
- **Условия выхода:** сквозная проверка 23 зелёная; одна формула эффективного лимита на
  баннер и на счёт.
- **Приёмка:** `34` §13 критерии 1, 3, 4, 7.

---

### Дорожка Т4 — рекрутинг и ИИ

#### PR-13 · `v2-recruiting-13` · Кандидат как запись `users`: схема и карточка

**Статус: выполнено** (2026-09-23, ветка `v2-recruiting-13`, миграция `0064_v2_candidates.sql`,
а не `0063`: `0063` занял параллельный PR-12. Подробности — `docs/v2/46-progress.md`, запись
«Фаза 3, PR-13»; решения при отступлениях — `docs/28-implementation-notes.md` §28.15.
Два отступления от буквы плана: FK `users_candidate_status_id_fk` поставлен здесь же, а не
отложен до развязки PR-15 — справочник создаётся в этой же миграции, и цикл DDL снят
порядком внутри файла; ось `candidates_active` переведена с приближения «`status <> archived`»
на точное `candidate_state = 'active'` — колонка появилась именно здесь.)

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0064_v2_candidates.sql` (план называл `0063`).
- **Входит:** `candidate_statuses`, `candidate_status_history`, `candidate_scores`
  (FK на готовые `scales`/`scale_levels`), `candidate_comments`; 11 колонок `users`
  (`candidate_state`, `candidate_status_id`, `source`, `source_detail`, `recruiter_id`,
  `access_until`, `comm_language`, `resume_asset_id`, `converted_from_candidate_at`,
  `consent_given_at`, `consent_expires_at`) — **без `vacancy_id`**, он приезжает развязкой в
  PR-15; CRUD `/candidates/*`, карточка, дубликаты по телефону.
- **⟵ PR-04, PR-05, PR-09, PR-12.**
- **Условия входа:** ось `candidates_active` считается; `origin='candidate_resume'` в перечне;
  `candidates_enabled` по-прежнему `false`.
- **Условия выхода:** сканер `users-kind-filter` зелёный **с настоящими кандидатами** в
  посеве, а не только с канарейкой; чужой тенант и чужая точка дают `404`, не `403`.
- **Приёмка:** `28` §13 критерии 2, 5, 6, 9, 10, 11.

#### PR-14 · `v2-recruiting-14` · Воронка: канбан, найм, автоматика

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0068_v2_candidates_funnel.sql`.
> [исправлено, нумерация: `0063` занял PR-12, `0064` — PR-13, `0066` — PR-18, `0067` — PR-23]
> Ранее: «`0064_v2_candidates_funnel.sql`».
> [исправлено, PR-23 при ребейзе: PR-14 смержился раньше, и PR-23 переехал на `0069`;
> номер `0067` не занят никем] Ранее: «`0067` — PR-23».
- **Входит:** канбан (250 карточек в колонке — постраничная догрузка), транзакция найма
  (`kind` → `employee`, прогресс и оценки на том же `user_id`, перенос лимита),
  задачи `candidate.auto_archive` и `candidate.consent_sweep`, отчёт по воронке,
  включение флага `candidates_enabled`.
- **⟵ PR-13.**
- **Условия входа:** проверка `HANDOFF` §7.4 на стенде с посеянными кандидатами пройдена
  вручную: списки сотрудников, адресаты рассылок и счётчик активных их не содержат.
- **Условия выхода:** найм увеличивает `users_active` ровно на 1 и уменьшает
  `candidates_active` ровно на 1 в одной транзакции; тест «карточка до и после найма поле в
  поле» зелёный; флаг `candidates_enabled` можно включить тенанту.
- **Приёмка:** `28` §13 критерии 1, 3, 4, 7, 8, 12; сквозная проверка 15.

#### PR-15 · `v2-vacancy-15` · Вакансия: схема, критерии, шаблон параметров

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0071_v2_vacancies.sql`, `0072_v2_users_vacancy_fk.sql` (развязка).
> [исправлено, нумерация: `0065`–`0070` заняли параллельные PR-12, PR-13, PR-14, PR-18, PR-23
> и «Роль „Власник“»] Ранее: «`0065_v2_vacancies.sql`, `0066_v2_users_vacancy_fk.sql`».
- **Входит:** `vacancies`, `vacancy_languages`, `vacancy_criteria`, `vacancy_criterion_scores`,
  `vacancy_templates`; **вторая миграция-развязка** добавляет `users.vacancy_id` и оба
  отложенных FK цикла `28 ↔ 29` (В-13); вакансия хранит **шаблон параметров**, а применяется
  созданием обычного `assignments`.
- **Решения:** В-13. **⟵ PR-06, PR-14.**
- **Условия входа:** фильтр `params` по возможностям этапа работает (PR-06) — иначе шаблон
  вакансии станет вторым носителем правил прохождения.
- **Условия выхода:** сквозная проверка 13 (`42` §5) пуста: в `shared/schemas/vacancy*.ts`
  нет `attempts`, `pass_score`, `due_at`, `time_limit` вне `params_template`; правка вакансии
  не меняет ни одного поля уже созданного назначения; два именованных констрейнта на месте.
- **Приёмка:** `29` §13 критерии 1, 8, 11, 13, 14.
- **Сделано** (23.09, `v2-vacancy-15`): пять таблиц, развязка `users.vacancy_id`, критерии со
  свёрткой в `candidate_scores`, шаблоны, экран `/admin/vacancies`. Из к. 13 закрыта часть,
  не требующая публичного контура и площадок (прохождение не прервано, токен снят, список
  кандидатов рекрутеру); 410 публичной страницы — PR-16, снятие объявлений — PR-17.

#### PR-16 · `v2-vacancy-16` · Публичный контур и отклик (В-9, П-25.3)

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0067_v2_vacancy_apply.sql`.
- **Входит:** `vacancy_applications`, `public_apply_attempts`; `/api/v1/public/j/[token]`
  и форма отклика по образцу `server/services/mystery.ts:86` (токен → `tenant_id` →
  `withTenant`); honeypot, nonce, частотное ограничение, OTP; конверсия отклика в кандидата;
  `hitRateLimit` для существующих `mystery/*` — тем же PR.
- **Патчи:** П-25.3. **Решения:** В-9. **⟵ PR-15.**
- **Условия входа:** `candidates_enabled` управляем; ось `candidates_active` блокирует приём
  при исчерпании.
- **Условия выхода:** сквозная проверка 16 (`42` §5) зелёная — чужой токен даёт `404`;
  ни один обработчик в публичном контуре не обращается к БД мимо `withTenant()`;
  экраны проходят на 320 px (сквозная 24).
- **Приёмка:** `29` §13 критерии 2, 3, 4, 5, 6, 7; `35` §13 к.2.

#### PR-17 · `v2-vacancy-17` · Публикация и генерация текста

- **Модель:** sonnet. **Оценка:** 2 сессии. **М:** `0068_v2_vacancy_publish.sql`.
- **Входит:** `job_board_accounts`, `vacancy_publications` со статусом `manual` (обходной путь
  `44` §8: рекрутер публикует руками и вставляет ссылку), `vacancy_ai_generations`; кнопка
  «Створити з AI» с провайдером-заглушкой и отказом при исчерпанном `ai_generate_ops`.
- **⟵ PR-16.**
- **Условия входа:** реальные аккаунты площадок не заводятся (`HANDOFF` §6).
- **Условия выхода:** отозванный токен площадки переводит аккаунт в `revoked`, публикация не
  теряется; блок, сгенерированный ИИ и не тронутый человеком, не публикуется без проверки.
- **Приёмка:** `29` §13 критерии 9, 10, 12, 15.

#### PR-27 · `v2-ai-27` · Провайдер модели и учёт вызовов

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0078_v2_ai_providers.sql`.
- **Входит:** `ai_providers` (с `fallback` и `provider_retention`), `ai_calls`; абстракция
  вызова и **детерминированная заглушка для dev**; оси `ai_interview_ops`, `ai_review_ops`,
  `ai_generate_ops` подключаются к деградации.
- **⟵ PR-09, PR-14, PR-17.**
- **Условия входа:** ключ провайдера в репозиторий не попадает; `.env.example` — только имя
  переменной.
- **Условия выхода:** провайдер с `provider_retention='unknown'` не назначается на
  `transcribe` — попытка сохранить такую конфигурацию отклоняется (сквозная 18).
- **Приёмка:** `30` §13 к.7; `35` §13 к.3, к.4.

#### PR-28 · `v2-ai-28` · Сценарий, согласие, прохождение собеседования (В-12)

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0079_v2_interview.sql`.
- **Входит:** `interview_scenarios`, `interview_criteria`, `interview_consents`,
  `interview_sessions`, `interview_turns`, `interview_criterion_scores`; третье значение
  `quizzes.kind = 'interview'` (**колонка `mode` отменена** решением В-12); экран согласия,
  альтернативный путь, восстановление после обрыва.
- **Решения:** В-12. **⟵ PR-27.**
- **Условия выхода:** сквозная проверка 17 (`42` §5) пуста — `server/services/ai/` не трогает
  `candidate_state`, `candidate_status_id`, `is_correct`, `workshop_submissions`; балл без
  `evidence` и без `rationale` отклоняется, сессия уходит в `needs_human`.
- **Приёмка:** `30` §13 критерии 1, 2, 3, 4, 5, 8, 9, 12.

#### PR-29 · `v2-ai-29` · Підсумок, подсказки ментору, качество

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0080_v2_ai_summary.sql`.
- **Входит:** `candidate_summaries`, `ai_review_hints`, `ai_quality_reviews`; авто-отправка
  Підсумку с порогом и задержкой; retention аудио (`purge_after`, задача
  `interview.media_purge`).
- **⟵ PR-12, PR-28.**
- **Условия выхода:** сквозная проверка 18 зелёная (голос не живёт дольше срока);
  строку «Документ сформовано автоматично» нельзя выключить настройкой тенанта;
  подсказка ментору не содержит предзаполненных значений.
- **Приёмка:** `30` §13 критерии 6, 10, 11, 13, 14.

---

### Дорожка Т5 — проверка, делегирование, время

#### PR-18 · `v2-review-18` · `review_queue_items` как таблица (В-2, В-15)

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0066_v2_review_queue.sql`.
  > [исправлено, фактическая нумерация: в `main` на момент ветки был 0064]
  > Ранее: «`0069_v2_review_queue.sql`», затем «0065 занял параллельный PR-14»: PR-18
  > смержился раньше и занял `0066`, а PR-14 встал следом — `0068_v2_candidates_funnel.sql`.
- **Входит:** `review_queue_items` — **71-я создаваемая таблица**, источник истины о состоянии
  очереди; DDL вносится в `docs/02` (сейчас таблицы там нет вовсе); `enqueueReview()` —
  единственная точка постановки, `subject_kind` ставится из `users.kind` **один раз**, при
  постановке; `/review/queue` читает таблицу, три базовых пути (`/review/answers`,
  `/review/submissions`, `/review/workshops`) остаются узкими фильтрами;
  `/review/checklists` вычёркивается из `docs/04-api.md` §4.7.
- **Патчи:** П-13. **Решения:** В-2, В-15. **⟵ PR-01.**
- **Условия входа:** оба существующих сервиса очереди выписаны поимённо
  (`attempts.ts:653`, `:746`; `workshops.ts:230`) — переписывать придётся два, не один.
- **Условия выхода:** сквозная проверка 21 (`42` §5) пуста — ни `truncate`, ни
  `delete from review_queue_items`; ответ несёт `total` и курсор.
- **Приёмка:** `37` §13 критерии 5, 6; контрактные тесты 1–3.
- **Сделано** (23.09, ветка `v2-review-18`): таблица, `reviewQueue.ts` с `enqueueReview()` и
  `closeReview()`, оба писателя переписаны (`attempts.ts` — внутри `gradeAndFinalize`, чтобы
  точка была одна на отправку и на истечение; `workshops.ts` — сдача, захват, возврат, решение,
  SLA-скан), `/review/queue` читает таблицу и отдаёт `{items,total,cursor}`,
  `/review/checklists` вычеркнут. Проверка 21 оформлена шестой проверкой
  `scripts/v2-crosschecks.sh` с фикстурами. Принятые по ходу решения — `46-progress.md`
  (Р-18.1 одна ось `task_type`, Р-18.2 две оси таба и вида работы, Р-18.3 `attempt_no`,
  Р-18.4 исключение `sla_due_at` в контрактном тесте 5).

#### PR-19 · `v2-review-19` · Делегирование, маршрутизация, SLA

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0080_v2_review_delegation.sql`.
  > [исправлено, фактическая нумерация: к финальному rebase в `main` последней была
  > `0079_jobs_tenant_sources` (#120); `0076`–`0078` заняли геймификация #114, PR-24 #117 и
  > PR-21 #116] Ранее: «`0070_v2_review_delegation.sql`», затем «`0076`».
- **Входит:** `review_delegations`, `review_routing_rules`, `reviewer_capacity`,
  `reviewer_absences`, `review_sla_events`, `reviewer_stats_daily`; глубина делегирования,
  возврат по SLA, эскалация с фолбэком на администратора тенанта.
- **⟵ PR-18.**
- **Условия выхода:** сквозная проверка 20 (`42` §5) зелёная — делегирование не расширяет
  доступ к персональным данным кандидата; `sla_due_at` элемента не меняется при делегировании.
- **Приёмка:** `37` §13 критерии 1, 2, 3, 4, 12, 13, 14, 15.
- **Сделано** (24.09, ветка `v2-review-19`): шесть таблиц `37` §3.2–3.4 и два ключа-развязки
  В-13; пять состояний очереди и захват карточки отдельно от назначения; делегирование с
  цепочкой глубины 2, отзывом, возвратом по сроку делегата и «Делегувати обрані»;
  переназначение; правила распределения (шесть стратегий, круговое с блокировкой ёмкости,
  запасной адресат); отсутствия с перебросом очереди; пороги SLA 50/100/150 % с эскалацией на
  руководителя по `resolveManager()` и фолбэком на администратора тенанта; суточная статистика;
  пять фоновых задач `37` §11; карточка `GET /review/items/:id` без ПД; экраны «Мої перевірки»,
  «Навантаження перевіряючих», «Правила розподілу». Решения Р-19.1…Р-19.8 — `46-progress.md`.

#### PR-20 · `v2-review-20` · Снятие зеркальных колонок `workshop_submissions`

- **Модель:** sonnet. **Оценка:** 1 сессия. **М:** `0071_v2_review_mirror_drop.sql`.
- **Входит:** `reviewer_id`, `claimed_at`, `sla_due_at` удаляются из `workshop_submissions`
  (решение В-2: понижаются до зеркала в PR-18 и удаляются **через один PR**).
- **⟵ PR-19.**
- **Условия входа:** ни одного чтения этих трёх колонок в `server/` — проверяется `grep`
  и это же и есть содержание PR.
- **Условия выхода:** `rework_count` и `status` остаются; очередь — единственный источник
  истины о состоянии.
- **Приёмка:** сквозная 21; `pnpm check`.

#### PR-21 · `v2-time-21` · Учёт времени биениями

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0078_v2_learning_time.sql`.
  > [исправлено, фактическая нумерация при rebase] Ранее: «`0072_v2_learning_time.sql`»:
  > к моменту rebase в `main` были `0072`–`0076` (PR-15, PR-10, PR-16, PR-30 и геймификация #114),
  > а `0077` — PR-24 (#117, `0077_v2_content_routing`), смерженный перед этим PR.
- **Входит:** `learning_time_sessions`, `learning_time_totals`; биения вместо разницы
  «открыл — закрыл», запись пачками, агрегат фоном; `alter lesson_progress` +
  `content_seconds`, `discarded_seconds`, `sessions_count`; `alter attempts` + `net_seconds`,
  `discarded_seconds`; `alter workshop_submissions` + `attempt_seconds`, `content_seconds`;
  `alter attempt_answers` + `input_mode`.
- **⟵ PR-01.**
- **Условия выхода:** сквозная проверка 22 (`42` §5) зелёная; нагрузочная проверка на 200
  одновременных прохождениях входит в приёмку.
- **Приёмка:** `37` §13 критерии 7, 8, 9, 10.
- **Сделано** (24.09, ветка `v2-time-21`): миграция `0078_v2_learning_time` (две таблицы,
  четыре `alter`, скоуп `time.metrics.view` наставнику и руководителю), правила в
  `shared/domain/learningTime.ts`, групповая запись биений и три ручки `/learning/time/*` в
  `server/services/learningTime.ts`, свёртка `time.rollup` и `time.close_stale_sessions`
  (`learningTimeRollup.ts`, pg-boss), счётчик экрана `useLearningTime` на уроке, тесте и
  практикуме, «Ви ще тут?». Критерии 7–10 и проверка 22 — `tests/integration/v2-learning-time.spec.ts`;
  нагрузка — `v2-learning-time-load.spec.ts`: 200 потоков биений через сервис (со своими
  часами, ожидание известно заранее) и 200 человек со своими сессиями по HTTP собранного
  приложения. Принятые по ходу решения Р-21.1…Р-21.18 — `46-progress.md`; гипотезы
  Г-37.9…Г-37.14 — `37` §15.

#### PR-22 · `v2-time-22` · Нормы времени на контент

- **Модель:** opus. **Оценка:** 2 сессии. **М:** `0085_v2_time_norms.sql`.
  > [исправлено, фактическая нумерация] Ранее: «`0073_v2_time_norms.sql`»: `0073`–`0082` заняли
  > смерженные раньше PR-10, PR-16, PR-30, геймификация #114, PR-24, PR-21, починка кругов фоновых
  > задач #120, PR-19, PR-25 и PR-32, `0083` — #122 (квота хранилища), `0084` — #124 (настройки,
  > мержится раньше); номер `0085` (`when` = 1790234220000) назначен координатором волны.
- **Входит:** `content_time_norms`, флаг отклонения, уведомление автору, отчёт `37` §9.3
  без имён.
- **⟵ PR-21.**
- **Условия выхода:** флаг отклонения не меняет балл ни одного человека.
- **Приёмка:** `37` §13 к.11.
- **Сделано** (25.09, ветка `v2-time-22`): миграция `0085_v2_time_norms` (таблица с RLS
  `enable`+`force`, политикой `using`+`with check`, полным индексом по `tenant_id`, FK на
  `tenants`; два перечисления — в `docs/02` и `shared/enums.ts`), правила
  `shared/domain/timeNorms.ts`, сервис `server/services/timeNorms.ts` — единственный писатель
  нормы: форма §6.3 и «Застосувати», синхронизация «Орієнтовного часу» материала, снимок нормы в
  `review_queue_items.estimated_seconds` при сдаче и пересдаче, еженедельный `time.norms_recalc`
  с флагом отклонения и уведомлением `content_time_deviation`, обезличенный отчёт «План і факт
  часу» на `reportFrame` одной формулой с нормой. Ручки `/content/time-norms/*` и
  `/reports/time-plan-fact` (+ xlsx), экран `/admin/reports/time-plan-fact` — он же экран норм
  §5.3 — с формой нормы в строке. Условие выхода доказано тестом (снимок всех таблиц с баллами
  тенанта до и после всего, что делает модуль) и тринадцатой сквозной проверкой
  `scripts/v2-crosschecks.sh`. Критерий 11 — `tests/integration/v2-time-norms.spec.ts`. Решения
  Р-22.1…Р-22.11 — `46-progress.md`; гипотезы Г-37.15…Г-37.18 — `37` §15.

---

### Дорожка Т6 — обратная связь по контенту и библиотека

#### PR-23 · `v2-content-23` · Жалоба на материал: подача и дедупликация

> [исправлено, реализация: номер миграции в репозитории свой — 0065 и 0066 заняты
> параллельными PR; `content_issue_events` перенесена сюда из PR-24, потому что критерий
> приёмки 2 (`36` §13) требует событие `merged` в момент подачи, а не разбора; шестнадцатое
> значение `origin` (`issue_screenshot`) заведено правкой `40` §4 и этой же миграцией]
> Ранее: «**М:** `0074_v2_content_issues.sql`», состав без `content_issue_events`

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0069_v2_content_issues.sql`.
- **Входит:** `content_issues`, `content_reports`, `content_issue_events`,
  `content_reporter_stats`; форма жалобы у
  блока урока и у вопроса, дедупликация по материалу и типу, защита от злоупотребления
  (5 жалоб в сутки), скриншот через `media_assets` с `origin='issue_screenshot'`;
  жалоба не блокирует попытку и не съедает `time_limit_sec`;
  линейная шкала в типах вопросов теста и «Підказка для відповіді» в опросах.
- **Патчи:** П-12.1, П-12.2, П-12.3. **⟵ PR-12.**
- **Условия входа:** перечень `origin` закрыт констрейнтом.
- **Условия выхода:** попытка не теряет времени на форму жалобы; шестая жалоба за сутки
  блокируется текстом, а не молча.
- **Приёмка:** `36` §13 критерии 1, 2, 3, 7.

#### PR-24 · `v2-content-24` · Разбор жалобы: маршрутизация, резолюции, пересчёт

> [исправлено, PR-23: `content_issue_events` создана раньше — событие `merged` рождается
> при подаче. PR-24 добавляет к журналу переходы статусов, `assigned`, `commented`,
> `rescored` и уведомления по ним] Ранее: «Входит: `content_issue_events`, …»

> [исправлено, PR-24: в состав вошли и две колонки базовых таблиц — `course_categories.owner_id`
> (шаг (в) маршрутизации `36` §7.5 называет «владельца категории курса», которого в модели не
> было) и `attempt_results.issue_id` (`36` §7.8: «новая запись результата со ссылкой `issue_id`»);
> экраны очереди, карточки, правил маршрутизации и отчёта; уведомления §8, кроме SLA-эскалации]
> Ранее: состав без колонок и экранов

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0077_v2_content_routing.sql`.
> [исправлено, нумерация: `0075` занял PR-30 (`0075_v2_org_structure`), `0076` — геймификация
> (#114, `0076_gamification`)] Ранее: «`0075_v2_content_routing.sql`»
- **Входит:** `content_issue_routing_rules`; резолюции и переходы статусов в уже созданном
  журнале, закрытие по
  публикации новой версии, **вторая точка входа в «Перерахувати»**, отчёт «Якість контенту»
  на `reportFrame`.
- **Патчи:** П-12.4. **⟵ PR-23.**
- **Условия выхода:** пересчёт по жалобе и пересчёт по тесту — одна логика, две точки входа;
  уволенный автор не блокирует маршрутизацию.
- **Приёмка:** `36` §13 критерии 4, 5, 6, 8, 9.

#### PR-25 · `v2-library-25` · Библиотека модулей и версии (П-11)

**Статус: выполнено** (2026-09-24, ветка `v2-library-25`; миграция `0081_v2_library.sql` —
`0075`–`0079` заняли PR-30, геймификация #114, PR-24, PR-21 и починка фоновых задач #120,
`0080` — делегирование проверки PR-19 (#118). Сервер, контракты и тесты;
экраны `31` §5 — отдельным PR по правилу разрезания §6.2 п. 2, см. ниже. Подробности —
`docs/v2/46-progress.md`, запись «Фаза 3, PR-25».)

> [исправлено, PR-25: (1) у урока в репозитории нет своего `body` — тело модуля несёт урок,
> ссылающийся на материал (`resources`/`resource_versions`), `31` §3.1 исправлен; (2) урок
> курса как место использования (`lessons.library_version_id`) доведён здесь же — колонку
> вводит этот PR, и другого PR, который бы её читал, в плане нет; (3) экраны «Бібліотека
> модулів», карточка, «Де використовується» и диалоги (`31` §5.1–§5.3, §5.5) вынесены в
> следующий PR — сервер считает, клиент показывает, обратный порядок запрещён (§6.2 п. 2);
> палитра вставки §5.4 остаётся в PR-26] Ранее: «Входит» без урока курса как места и без
> оговорки об экранах.

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0081_v2_library.sql` (план называл `0076`).
- **Входит:** `library_modules` (с `embedding`, расширение `vector` уже создано),
  `library_module_versions`, `library_module_usages`, `library_module_proposals`;
  `alter lessons` + `library_module_id`, `library_version_id`; **снятие `not null` с
  `lessons.module_id`** и констрейнт `lessons_owner_ck` (ровно одно владение).
- **Патчи:** П-11. **⟵ PR-03.**
- **Условия входа:** скоупы `library.use` и `library.publish` разведены (PR-03).
- **Условия выхода:** `lessons.module_id` nullable, `lessons_owner_ck` на месте, ни одного
  урока без владельца; удаление модуля, использованного в трёх треках, даёт `409`.
- **Приёмка:** `31` §13 критерии 1, 3, 4, 5, 9.

#### PR-26 · `v2-library-26` · Библиотека в траекториях и поиск (П-17.1)

**Статус: выполнено** (2026-09-25, ветка `v2-library-26`; миграция `0088_v2_library_trajectory.sql`
— `0077`–`0083` заняли смерженные PR, `0084`–`0087` — очередь параллельных PR на мерж.
Подробности — `docs/v2/46-progress.md`, запись «Фаза 3, PR-26».)

> [исправлено, PR-26: (1) номер миграции — `0088`; (2) сверх «Входит» — то, что PR-25 оставил этому
> PR (`46`, запись PR-25, «Что осталось»): `diff` между версиями, `makeCopy` узла, копирование мест
> при дублировании траектории, `library_stale_digest`, перенос активных мест узлов из реестра в
> колонку; (3) массовое «Оновити все» — сервер с тестами, кнопка — на экране «Де
> використовується» (`31` §5.3), который приходит PR экранов вместе с остальными экранами
> библиотеки (правило разрезания §6.2 п. 2, как в PR-25)] Ранее: «**М:**
> `0077_v2_library_trajectory.sql`», без перечня унаследованного.

- **Модель:** opus. **Оценка:** 2 сессии. **М:** `0088_v2_library_trajectory.sql`.
- **Входит:** `alter trajectory_nodes` + `library_version_id`; палитра «Бібліотека модулів ▸»;
  «Оновити до останньої»; хотфикс-версии; поиск по телу последней версии.
- **Патчи:** П-17.1. **⟵ PR-25.**
- **Условия выхода:** узел трека показывает закреплённую версию, а не последнюю; ветвление
  графа (`TRAJECTORY_NODE_KINDS`, 9 значений) не сокращено.
- **Приёмка:** `31` §13 критерии 2, 6, 7, 8.

---

### Дорожка Т7 — оргструктура и карточка человека

#### PR-30 · `v2-org-30` · Дерево подчинения и единственный `resolveManager()` (П-16.4, В-7)

> [исправлено, реализация: номер миграции в репозитории свой — 0073 занял PR-10 (оплата),
> 0074 занимает параллельный PR «публічний контур вакансії», он мержится раньше.
> Колонок у `org_conflicts` добавляется **две**, а не четыре: `status` и `detected_at`
> решение `44` В-7 отвергло аргументированно (`detected_at` = `created_at`; состояние уже
> выражено парой `resolved_at` / `resolved_by`), а этот пункт плана повторял состав из
> `32` §3.3 мимо решения. Плюс: чтений `locations.manager_id` о человеке оказалось
> не три, а шестнадцать — поимённый список в описании PR]
> Ранее: «**М:** `0081_v2_org_structure.sql`», «+`severity`, `node_id`, `status`,
> `detected_at`», «три существующих источника руководителя»

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0075_v2_org_structure.sql`.
- **Входит:** `org_nodes` (`ltree`), `org_node_assignments`, `org_manager_map`,
  `org_structure_snapshots`; **`alter org_conflicts`** вместо новой таблицы: +`severity`,
  `node_id`, перечень `kind` сводится к одному списку из 10 значений
  (5 существующих сохранены, 3 пакетных отброшены как переименования) через `docs/02`
  §«Перечисления»; `resolveManager()` — единственный источник истины о руководителе;
  флаг тенанта `org_structure_is_source_of_truth` с фолбэком на `locations.manager_id`;
  `alter user_notes` не здесь (см. PR-32).
- **Патчи:** П-16.4. **Решения:** В-7. **⟵ PR-12.**
- **Условия входа:** существующие источники руководителя выписаны поимённо (шестнадцать мест,
  список — в описании PR; включая `people.ts`, `user_placements.manager_id`, `functional_chiefs`).
- **Условия выхода:** ни одного прямого чтения `locations.manager_id` вне `resolveManager()`
  (проверка 9 в `scripts/v2-crosschecks.sh`, allowlist из 16 строк с причинами);
  глубина дерева ограничена констрейнтом, циклы — триггером `org_nodes_guard`.
- **Приёмка:** `32` §13 критерии 1, 2, 3, 4, 5, 8.

#### PR-31 · `v2-org-31` · Импорт, снимки, откат реорганизации

**Статус: выполнено** (2026-09-25, ветка `v2-org-31`; миграция `0089_v2_org_import.sql` — номер
назначен координатором очереди мержа: `0082` занял PR-32, `0084`–`0088` — PR, идущие перед этим.
Подробности, решения Р-31.1…Р-31.10 и что осталось — `46-progress.md`, запись «Фаза 3, PR-31».)

> [исправлено, реализация PR-31: (1) откат идёт в запросе одной транзакцией и отвечает итогом,
> фоновой задачи `org.rollback_apply` нет — фоном применяется только импорт (`32` §10, §11);
> (2) «снимает фолбэк из PR-19» — это подъём по дереву от **любого** руководителя, а не только
> от руководителя из дерева при включённом флаге: администратор тенанта остаётся адресатом лишь
> когда выше в дереве никого нет — так требует `37` §12 («выше некуда — администратору»);
> (3) сверх «Входит» — снимок `pre_bulk_move` перед переносом ветки больше 20 узлов (`32` §7
> п. 7) и выгрузка CSV (`32` §9, формат импорта)] Ранее: «**М:** `0082_v2_org_import.sql`»

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0089_v2_org_import.sql`.
- **Входит:** импорт CSV с предварительным снимком `pre_import`, откат, экран конструктора,
  эскалация SLA по дереву (снимает фолбэк из PR-19).
- **⟵ PR-19, PR-30.**
- **Условия выхода:** откат восстанавливает дерево и активные назначения; циклы и висячие узлы
  попадают в `org_conflicts`, а не в дерево.
- **Приёмка:** `32` §13 критерии 6, 7.

#### PR-32 · `v2-people-32` · Заметки и документы человека

**Статус: выполнено** (2026-09-25, ветка `v2-people-32`; миграция `0082_v2_person_notes_docs.sql`,
а не `0083`: номер назначен координатором очереди мержа — `0080` и `0081` занимают PR, идущие перед
этим.
Сверх «Входит» — задача `documents.expiry_scan` и уведомления §8: без них не закрыть критерий 8;
`user_notes` внесена в `V2_PACKAGE_TENANT_TABLES` вместе с ключом на `tenants`, которого у неё
не было. Подробности, решения и что осталось — `46-progress.md`, запись PR-32.)

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0083_v2_person_notes_docs.sql`.
- **Входит:** **`alter user_notes`** вместо новой `person_notes`: +`visibility`, `category`,
  `is_pinned`, `flagged_at`, `flagged_terms`, `shared_at`, `archived_at`, 4 констрейнта и
  частичный индекс; `person_document_types`, `person_documents`; запись `person_note.read`
  в `audit_log` с перечнем `note_ids` и **без текста**; задача `notes.archive_scan`.
- **⟵ PR-04, PR-12.**
- **Условия выхода:** сужение видимости заметки даёт `409`; тип документа с
  `is_fact_only=true` не принимает файл.
- **Приёмка:** `38` §13 критерии 4, 5, 6, 7, 8.

#### PR-33 · `v2-people-33` · Нормы отсутствий и сдвиг дедлайнов

**Статус: выполнено** (2026-09-25, ветка `v2-people-33`; миграция `0090_v2_absences.sql`, а не
`0084`: номер и `when` назначены координатором очереди мержа — `0084` занял PR-39, `0088`–`0091` —
PR, идущие перед этим). Сверх «Входит»: блок «Відсутності» — вкладка карточки и секция своего
профиля; тишина напоминаний `due.scan` в дни отсутствия (вторая половина критерия 10); уведомление
`absence_deadline_shifted`; ночная задача `absence.deadline_guard`; «Дедлайн минув під час
відсутності» с «Перенести» (§12); `person.absence.manage` руководителю точки. Сдвиг пишется в
запись на курс — `enrollments.deadline_shifted_reason`, а не в назначение: срок у людей одного
назначения свой, а отсутствует один человек. Не сделано (не названо ни во «Входит», ни в критериях 9–10): общая тишина
уведомлений §8, `absence.balance_scan` с `absence_norm_exceeded`, отчёт «Норми і залишки
відсутностей» §9. Подробности, решения и что осталось — `46-progress.md`, запись PR-33.

> [исправлено, PR-39] `absence_norms` (DDL `38` §3.6 плюс `updated_at`, `set_by` nullable) и
> функция разрешения `resolveAbsenceNorms()` (`server/services/absenceNorms.ts`: человек → точка
> → компания → 24/5, по каждому виду отдельно) уже заведены PR-39 вместе с `PUT /absence-norms`
> (все три уровня, причина для человека) — их пишет блок «Кількість днів відпустки» настроек
> компании. PR-33 остаются `absence_records`, остаток, блок карточки с «Скоригувати» и сдвиг
> дедлайна; критерий `38` §13 к.9 закрывается им на карточке (подписи «Норма компанії» /
> «Норма точки»), разрешение под ним уже проверено тестом `v2-settings-39.spec.ts`.
> Ранее: «**Входит:** `absence_norms`, `absence_records`; резолвинг …»

- **Модель:** opus. **Оценка:** 2 сессии. **М:** `0084_v2_absences.sql`.
- **Входит:** `absence_norms`, `absence_records`; резолвинг «компания → точка →
  индивидуальная корректировка»; сдвиг дедлайна обязательного назначения на отпуск.
- **⟵ PR-06, PR-32.**
- **Условия входа:** фильтр `params` по возможностям этапа работает — сдвиг дедлайна
  применяется только там, где возможность `deadline` включена.
- **Приёмка:** `38` §13 критерии 9, 10.

#### PR-34 · `v2-people-34` · Лента и карта активности

- **Модель:** opus. **Оценка:** 3 сессии. **М:** `0085_v2_user_activity.sql`.
- **Входит:** `user_activity_events` (со снимком таймзоны и локальным днём),
  `user_activity_daily`; `alter users` + `timezone`; задача `activity.purge` (400 дней);
  контракт `GET /people/:id/activity` переписывается с «последние 100 записей `audit_log`»
  на «дни · события · уровни за год», скоуп `person.activity.view_others`.
- **⟵ PR-32.**
- **Условия выхода:** событие в 22:40 UTC у человека на UTC+4 закрашено в следующем дне;
  карта после `purge` не рассыпается.
- **Приёмка:** `38` §13 критерии 11, 12.

#### PR-35 · `v2-people-35` · Рейтинг и четыре блока карточки (П-16.2, П-16.3)

- **Модель:** opus. **Оценка:** 2 сессии. **М:** `0086_v2_person_rating.sql`.
- **Входит:** `person_rating_snapshots`; `alter users` + `rating_pct`, `rating_updated_at`
  и частичный индекс `idx_users_tenant_rating … where kind='employee'`; четыре блока
  карточки — «Етап», лента активности, документы, нормы отсутствий; запрет `rating_pct` как
  единственного условия массового действия (`422 rating_only_filter_forbidden`).
- **Патчи:** П-16.2, П-16.3. **⟵ PR-07, PR-21, PR-34.**
- **Условия входа:** `users.kind` (частичный индекс), `lifecycle_stages` (блок «Етап»),
  учёт времени (метрики «Плановий час»/«Час проходження»).
- **Условия выхода:** публичной доски лидеров нет; значение больше 100 % не обрезается.
- **Приёмка:** `38` §13 критерии 1, 2, 3.

---

### Дорожка Т8 — хвост

#### PR-37 · `v2-notify-37` · Коды уведомлений и вебхуки (П-23, В-18)

> [исправлено, по факту реализации] Ранее: «**М:** `0088_v2_notifications.sql`». Ни коды
> уведомлений (`notification_templates.code`, `notifications.code` — обычный `text`, без
> `check`/enum), ни события вебхуков (`webhook_endpoints.events` — `text[]`), ни источник
> таймзоны кандидата (цепочка существующих FK `users.vacancy_id → vacancies.location_id →
> locations.timezone`, без новой колонки) не меняют схему. Миграции нет.
>
> Область «недостающих кодов» по факту сверки (`docs/v2/46-progress.md`, запись PR-37) —
> четыре модуля, у которых уже была своя реализующая PR (`28` PR-14, `29` PR-16, `32` PR-30,
> `36` PR-23): 22 кода, а не полный реестр `41` §6 на 106. Остальные семь модулей (`30`, `31`,
> `33` сверх вебхука, `34`, `35`, `37`, `38`) регистрируются вместе с бизнес-логикой в своих PR.

- **Модель:** sonnet (реестр шаблонов и версии уже работают). **Оценка:** 2 сессии.
  **М:** нет (см. правку выше).
- **Входит:** новые коды уведомлений пакета — **без дублирования** уже существующих
  `limit_warning` и `limit_exceeded`; ровно **три** новых события вебхуков
  (`candidate.hired`, `vacancy.application_received`, `offboarding.completed`), payload без
  персональных данных; тихие часы кандидата (09:00–20:00 по его локальному времени).
- **Патчи:** П-23. **Решения:** В-18. **⟵ PR-14, PR-23.**
- **Условия выхода:** сквозная проверка 19 (`42` §5) зелёная; `WEBHOOK_EVENTS` = 10 значений,
  `docs/04` §4.18 актуален.
- **Приёмка:** `28` §13 к.3 (уведомление рекрутеру — закрыт PR-14, здесь проверен тестом
  реестра), `36` §13 к.5 (код `content_issue_rescored` зарегистрирован, пересчёт — PR-24).
> [уточнено, PR-24: отправку `content_issue_rescored` при пересчёте, пересчёт прогресса назначения
> и выдачу сертификата делает PR-24 — критерий `36` §13 к.5 проверен его тестом
> `v2-content-triage.spec.ts`; в таблице покрытия §8 он записан за PR-24]

#### PR-38 · `v2-reports-38` · Двенадцать отчётов пакета (П-22)

- **Модель:** sonnet (единый каркас `reportFrame.ts` готов, отчёты делаются по образцу).
  **Оценка:** 3 сессии. **М:** нет.
- **Входит:** двенадцать отчётов на `FRAME_COLUMNS` и конструкторе `/reports/builder`.
- **Патчи:** П-22. **⟵ PR-21, PR-24, PR-34.**
- **Условия выхода:** ни один отчёт не выгружает `rating_pct` по умолчанию; все колонки —
  из единого каркаса `docs/22` §13.
- **Приёмка:** `37` §13 к.11 (отчёт без имён).
> [исправлено, PR-24: «Якість контенту» (`36` §13 к.9) сделан PR-24 на `reportFrame`; PR-38
> регистрирует его в конструкторе выгрузок вместе с остальными] Ранее: «`36` §13 к.9; …»

#### PR-39 · `v2-settings-39` · Настройки тенанта и платформы (П-21, П-24, В-20)

> [исправлено, реализация PR-39] Номер миграции — свой (`0084_v2_settings`, назначен при rebase
> по правилу §5). Таблиц — **шесть** и три `alter`: `platform_announcements` (платформенная),
> `platform_announcement_reads`, `position_groups`, `user_totp`, `user_totp_recovery_codes` и
> `absence_norms`; `positions.group_id`, `automation_rules.position_id`/`position_group_id`,
> `sessions.two_factor_pending`. Четыре тенантные вводят патчи (не входят в 71 `40` §2 — PR-40
> прибавляет их к сверке на длину), а `absence_norms` (из 71, `38` §3.6) заводится **здесь, а не
> в PR-33**: блок норм отпуска в настройках компании пишет в неё уровни компании и точки. Модель
> фактически — opus: второй фактор входа — безопасность, а не вёрстка по образцу.
> Ранее: «**Модель:** sonnet … **М:** `0089_v2_settings.sql`».

- **Модель:** sonnet. **Оценка:** 3 сессии. **М:** `0089_v2_settings.sql`.
- **Входит:** четыре недостающих блока настроек компании (колонтитул с логотипом в
  материалах, 2FA/TOTP, подгрузка таблиц при прокрутке, нормы отпуска);
  `platform_announcements` — вторая «новость», отдельная от корпоративной ленты тенанта;
  «посада → курси за замовчуванням» как правило `automation_rules` с `dimension='position'`
  (**отдельной таблицы не заводится**); `position_groups`; флаг `sessionOnly` на скоупе
  Bearer-токена — **чёрного списка путей нет**.
- **Патчи:** П-21, П-24.1, П-24.2, П-24.3, П-24.5. **Решения:** В-20. **⟵ PR-08, PR-30.**
- **Условия выхода:** две «новости» не смешаны; `/platform/*` недоступен по Bearer со
  `sessionOnly`-скоупом.
- **Приёмка:** `32` §13 к.8 (публичная витрина оргструктуры); ручная проверка шести блоков
  настроек по `24` §13.

#### PR-40 · `v2-final-40` · Сводные проверки и закрытие пакета

- **Модель:** opus. **Оценка:** 2 сессии. **М:** нет.
- **Входит:** контрактный тест №6 — **все 71 тенантная таблица пакета под RLS**, список
  проверяется на длину; девятый контрактный тест — четыре отложенных FK по **именам
  констрейнтов** (В-13); полный прогон сквозных проверок 9–24 `42` §5;
  актуализация `docs/02-data-model.md` (70 + `review_queue_items`, шесть новых перечней) и
  `docs/04-api.md` (218 эндпоинтов); проверка `HANDOFF` §7.4 вручную на стенде;
  финальная запись в `46-progress.md`.
- **⟵ все PR.**
- **Условия выхода:** зелёные все шесть контрактных тестов + девятый; ни одной строки
  «не проверено» в таблице §8 этого документа.
- **Приёмка:** §8 — покрытие всех 123 критериев секций 13.

---

## 5. Миграции: нумерация и параллельные ветки

**Нумерация.** Пакет начинается с `0054`. Стиль имени — `NNNN_v2_<область>_<что>.sql`.
Номера в §4 — **планируемые**: они верны, если PR мержатся в порядке этого документа.

**Правило для параллельных дорожек.**

1. Номер файла и `when` в `meta/_journal.json` назначаются **при rebase перед созданием PR**,
   а не при создании ветки. До этого момента в ветке лежит миграция с временным номером.
2. Перед `gh pr create` — обязательный `git fetch && git rebase origin/main` (правило
   `CLAUDE.md`). Если в `main` появилась миграция с большим номером, своя **переименовывается**
   в `max+1`, а `when` выставляется в `max(when) + 1000`.
3. **`when` обязан строго возрастать.** На накаченной БД drizzle сравнивает `when` с
   максимальным применённым и **молча пропускает** миграцию с меньшим значением — она не
   упадёт, она не выполнится. Ловится `tests/unit/migrations-journal.spec.ts` (PR-01).
4. Нарушение уже существует в `main` и чинится в PR-01: `0053_spec_development_goal_tree`
   имеет `when` меньше, чем `0052`.
5. Параллельные ветки **не держат миграцию дольше одного рабочего дня**: чем дольше ветка
   живёт с миграцией, тем дороже перенумерация. Если PR завис — миграция выносится в
   отдельный первый PR, а логика доезжает следующим.
6. Две ветки **не трогают одну таблицу** одной миграцией. Пары, за которыми следить:
   `users` (PR-04 → PR-13 → PR-15 → PR-34 → PR-35), `media_assets` (PR-11 → PR-12),
   `tenant_limits` (PR-08), `workshop_submissions` (PR-19 → PR-20 → PR-21),
   `lessons` (PR-25). Все — внутри одной дорожки, поэтому последовательны по построению.

**Какие PR можно вести одновременно** (не пересекаются ни по файлам, ни по миграциям):

| Окно | Параллельно |
|---|---|
| после PR-01 | PR-02, PR-03, PR-04 |
| после PR-04 | PR-05 (Т1), PR-08 (Т2), PR-11 (Т3), PR-18 (Т5), PR-21 (Т5) |
| после PR-12 | PR-13 (Т4), PR-23 (Т6), PR-25 (Т6), PR-30 (Т7), PR-32 (Т7) — до **четырёх** дорожек |
| после PR-19 | PR-20, PR-22, PR-31 |
| перед PR-40 | PR-37, PR-38, PR-39 |

---

## 6. Риски и правило разрезания

### 6.1 Риски

| Риск | Где | Смягчение |
|---|---|---|
| **Забытая выборка людей** — главный риск пакета (`42` §7.1) | PR-04 и всё после | Три слоя В-8; канарейка в посеве **в том же PR**; `candidates_enabled` выключен до PR-14; ручная проверка `HANDOFF` §7.4 — условие входа PR-14; откат только вперёд |
| **Окно между миграцией и патчем** | PR-04 | Состав первого PR зафиксирован В-14; ни одной колонки `users` сверх `kind` |
| **`when` в журнале** — миграция молча не выполняется | все параллельные дорожки | §5 п. 3–5; unit-тест в PR-01; нарушение в `main` чинится там же |
| **Перечень `origin` пересобирается дважды** (Р-1) | PR-12 | Один `check`, одна миграция, контрактный тест №4 сравнивает в обе стороны и требует ровно одного констрейнта |
| **Два механизма докупки места** (Р-6) | PR-08, PR-36 | `storage_quota_addons` не создаётся; сквозная 23 проверяет это SQL-ом; одна функция эффективного лимита |
| **Публичный контур как вход для ботов** | PR-16 | Защита от ботов и `public_apply_attempts` — часть минимального объёма, не улучшение; авто-архивация освобождает лимит |
| **Heartbeat как нагрузка** | PR-21 | Запись пачками, агрегат фоном, нагрузочная проверка на 200 прохождениях — в приёмке PR |
| **Делегирование как способ не проверять** | PR-19 | Глубина ограничена, каждая передача в журнале, доля делегированного в отчёте |
| **Ошибка в биллинге видна в счёте, а не на экране** | PR-08…PR-10 | До первого счёта наружу — месяц параллельного ручного счёта и сверка; `usage_events` оператором не правится |
| **Внешние сервисы и секреты** (`HANDOFF` §6) | PR-17, PR-27, PR-10, PR-36 | Четыре обходных пути `44` §8; ни один PR не блокируется ответом владельца продукта |
| **Библиотека как свалка** | PR-25 | `library.publish` отделён от `library.use` (П-01, PR-03); чужой урок предлагается через `library_module_proposals` |

### 6.2 Правило разрезания: что делать, если PR не влезает

Признак «не влезает»: гейт не сходится за три сессии, либо `pnpm check` требует правок в
файлах, не названных в описании PR.

Резать в этом порядке — первый применимый способ:

1. **Отделить миграцию от логики.** Первый PR — только DDL + RLS + индексы + контрактные
   тесты 1–3 на новые таблицы; второй — сервисы и эндпоинты. DDL без потребителей безопасен
   и проходит гейт сам по себе. Не применимо к PR-04: там миграция и патч связаны жёстко.
2. **Отделить экран от сервера.** Сервер с тестами — один PR, вёрстка по мокапу — второй,
   sonnet. Обратный порядок запрещён: «сервер считает, клиент показывает».
3. **Отделить фоновую задачу.** `*.purge`, `*.sweep`, `*.archive`, агрегаты — отдельным PR
   после основного; до него данные копятся, ничего не теряется.
4. **Отделить отчёт.** Отчёт на `reportFrame` — всегда отдельный PR, sonnet.
5. **Отделить второй сценарий сущности.** Например, `vacancy_publications` от `vacancies`
   (так и сделано: PR-15 / PR-17).

**Чего при разрезании делать нельзя:** разрывать пары из §2; вводить колонку, которую никто
не читает, «чтобы не переделывать миграцию»; переносить проверку инварианта в следующий PR.
Если разрез требует нарушить §2 — PR не режется, а **переносится позже**, а зависимый
переупорядочивается.

**Обратное правило — если PR слишком мал** (меньше сессии): объединять разрешено только
внутри одной дорожки и только если суммарно не выходит за три сессии.

---

## 7. Сводка

| Показатель | Значение |
|---|---:|
| PR в плане | **40** |
| Из них без миграции | 6 (PR-01, 02, 03, 38, 40 и PR-06 — только индексы) |
| Миграций | **36**, номера `0054`…`0089` |
| `create table` | **71** (70 таблиц пакета + `review_queue_items` по В-2) |
| Таблиц под `alter` | 15 |
| Патчей (проверяемых пунктов) | **32**, каждый ровно в одном PR |
| Решений `44` (В-2…В-20) | 19, каждое адресовано |
| Критериев приёмки секций 13 | **123**, покрытие — §8 |
| Дорожек | 8, одновременно — до 4 |
| Оценка, сессий агента | **≈103** (диапазон 95–120) |
| Оценка по критическому пути | **≈27 сессий** дорожки Т1→Т4 при четырёх дорожках |
| Исполнитель opus | 28 PR |
| Исполнитель sonnet | 12 PR |

Первый видимый результат — PR-05 (этапы жизненного цикла). Первый результат, ради которого
пакет написан, — PR-14 (воронка кандидата от отклика до найма).

---

## 8. PR → критерии приёмки пакета

Столбец «критерии» — номера пунктов секции 13 соответствующего документа. Каждый критерий
встречается **ровно один раз**.

| Документ | Критериев | PR и номера |
|---|---:|---|
| `28` кандидаты | 12 | PR-13: 2, 5, 6, 9, 10, 11 · PR-14: 1, 4, 7, 8, 12 · PR-37: 3 |
| `29` вакансии | 15 | PR-15: 1, 8, 11, 13, 14 · PR-16: 2, 3, 4, 5, 6, 7 · PR-17: 9, 10, 12, 15 |
| `30` ИИ-собеседование | 14 | PR-27: 7 · PR-28: 1, 2, 3, 4, 5, 8, 9, 12 · PR-29: 6, 10, 11, 13, 14 |
| `31` библиотека модулей | 9 | PR-25: 1, 3, 4, 5, 9 · PR-26: 2, 6, 7, 8 |
| `32` оргструктура | 8 | PR-30: 1, 2, 3, 4, 5 · PR-31: 6, 7 · PR-39: 8 |
| `33` жизненный цикл | 12 | PR-05: 6, 11, 12 · PR-06: 1, 2, 3, 4, 8 · PR-07: 5, 7, 9, 10 |
| `34` хранилище | 7 | PR-11: 2, 6 · PR-12: 5 · PR-36: 1, 3, 4, 7 |
| `35` тариф и лимиты | 10 | PR-08: 7, 9, 10 · PR-09: 1, 3, 5, 8 · PR-10: 4, 6 · PR-16: 2 |
| `36` обратная связь | 9 | PR-23: 1, 2, 3, 7 · PR-24: 4, 5, 6, 8, 9 |
| `37` проверка и время | 15 | PR-18: 5, 6 · PR-19: 1, 2, 3, 4, 12, 13, 14, 15 · PR-21: 7, 8, 9, 10 · PR-22: 11 |
| `38` карточка человека | 12 | PR-32: 4, 5, 6, 7, 8 · PR-33: 9, 10 · PR-34: 11, 12 · PR-35: 1, 2, 3 |
| **Итого** | **123** | покрыто 123, не покрыто 0 |

> [исправлено, PR-24: критерии `36` §13 п. 5 (`content_issue_rescored` со старым и новым баллом)
> и п. 9 (отчёт «Якість контенту») закрыты PR-24 — уведомления по резолюциям и отчёт на
> `reportFrame` вошли в его постановку, а сам раздел PR-24 уже перечислял 4, 5, 6, 8, 9]
> Ранее: «PR-24: 4, 6, 8 · PR-37: 5 · PR-38: 9»

### 8.1 Патчи → PR

| Патч | PR | Патч | PR | Патч | PR |
|---|---|---|---|---|---|
| П-01 | 03 | П-13 | 18 | П-22 | 38 |
| П-02 | 02 | П-14 | 07 | П-23 | 37 |
| П-03 | 02 | П-15 | 06 | П-24.1 | 39 |
| П-04 | 02 | П-16.1 | **04** | П-24.2 | 39 |
| П-05 | 02 | П-16.2 | 35 | П-24.3 | 39 |
| П-07 | 01 | П-16.3 | 35 | П-24.4 | 10 |
| П-11 | 25 | П-16.4 | 30 | П-24.5 | 39 |
| П-12.1 | 23 | П-17.1 | 26 | П-25.1 | 08 |
| П-12.2 | 23 | П-17.2 | 02 | П-25.2 | 09 |
| П-12.3 | 23 | П-20 | 02 | П-25.3 | 16 |
| П-12.4 | 24 | П-21 | 39 | | |

> [исправлено, пересчёт по таблице вердиктов `43` §2.3] Ранее в `43` §2.1 и §2.4:
> «29 проверяемых пунктов», «не применён — 19». В таблице вердиктов `43` §2.3 **32 строки**
> (номера П-17 и П-25 в ней тоже разбиты на подпункты), из них «применён частично» — 10,
> значит «не применён» — **22**, а всего пунктов — **32**. Пометка-исправление внесена и в
> `43-reconciliation.md`.

### 8.2 Решения `44` → PR

| Решение | PR | Решение | PR |
|---|---|---|---|
| **В-2** очередь таблицей | 18 (+20) | В-11 мягкие ссылки | 12, 18, 34 (общее правило — PR-01) |
| В-3 гибрид категорий | 05 | В-12 `kind='interview'` | 28 |
| **В-4** переименование `media_assets` | 11 | В-13 отложенные FK | 15 (+тест в 40) |
| **В-5** лимиты колонками | 08 | В-14 состав первой миграции | 04 |
| В-6 перечень `origin` | 12 | В-15 `/review/queue` | 18 |
| В-7 перечень `org_conflicts.kind` | 30 | В-16 `details.axis` | 09 |
| **В-8** три слоя фильтра `kind` | 04 | В-17 `DELETE /media/:id` | 11 |
| В-9 публичный контур | 16 | В-18 вебхуки | 37 |
| В-10 девять ключей разбивки | 12 (+36) | В-19 журнал скачивания | 11 |
| | | В-20 Bearer `sessionOnly` | 39 |

Жирным — четыре решения, дорогих для отката. Все четыре закрываются в **первой трети
плана** (PR-04, 08, 11, 18) — раньше, чем откат начнёт стоить миграции данных.

---

## 9. Самопроверка фазы

| Требование `HANDOFF` §4 и задания | Итог |
|---|---|
| У каждого PR есть входные и выходные условия | **40 из 40**. У PR-01 вход пуст по определению — это первый PR |
| Ни один PR не зависит от идущего позже | Проверено по всем 40: каждая ссылка «⟵» указывает на меньший номер. Максимальная «дальность» зависимости — PR-35 ⟵ PR-07 |
| Каждая из 70 таблиц — ровно в одном PR | **70 из 70**, плюс `review_queue_items` (PR-18) как 71-я. Разбивка: `28` → 4 (PR-13) · `29` → 5+2+3 (PR-15/16/17) · `30` → 2+6+3 (PR-27/28/29) · `31` → 4 (PR-25) · `32` → 4 (PR-30) · `33` → 1+2 (PR-05/07) · `34` → 5 (PR-36) · `35` → 3+3+2 (PR-08/09/10) · `36` → 3+2 (PR-23/24) · `37` → 6+1+2 (PR-19/22/21) · `38` → 2+2+2+1 (PR-32/33/34/35) |
| Каждый патч — ровно в одном PR | **32 из 32**, §8.1. Номера с подпунктами разложены по подпунктам; подпункты одного номера могут жить в соседних PR одной дорожки (П-12, П-16, П-24) |
| Сумма PR покрывает все критерии приёмки секций 13 | **123 из 123**, §8. Ни одного критерия дважды |
| Обязательные связки `HANDOFF` §4 соблюдены | три из трёх, §2: `33` раньше назначений (PR-05/06 → PR-13/15/33); `28` раньше `29` и `30` (PR-13/14 → PR-15…17 → PR-27…29); `users.kind` и П-16.1 в одном PR (PR-04) |
| Контрактные тесты и сквозные проверки — отдельным PR в начале | PR-01, до первой миграции пакета |
| Размер PR — такой, чтобы `pnpm check` проходил на каждом | Ориентир 1–3 сессии соблюдён у всех 40; правило разрезания — §6.2 |
| Параллельные PR помечены, правило нумерации миграций есть | §3 (дорожки), §5 (нумерация и окна параллельности) |
| Риски и правило разрезания | §6 |
| Кода не написано | да |

**Что найдено сверх задания.**

1. **Тест `tests/unit/migrations-journal.spec.ts` в репозитории отсутствует.** Проверено
   `ls tests/unit` — там пять файлов, этого среди них нет.
2. **Нарушение, которое он должен был поймать, уже в `main`:**
   `0053_spec_development_goal_tree` имеет `when = 1790116110662`, а `0052` —
   `when = 1790120023875`. На накаченной БД `0053` пропускается молча. Починка и сам тест —
   в PR-01.
3. **Арифметическая ошибка в `43` §2.4:** проверяемых пунктов патчей не 29, а 32 (§8.1).

---

## 10. Что дальше

**Фаза 3 — внедрение** по `HANDOFF` §5: PR за PR в порядке §4, цикл из семи шагов на каждый,
гейт `pnpm check` перед открытием PR, запись в `46-progress.md` после каждого.

Первый ход фазы 3 — **PR-01 `v2-guard-01`**: контрактные тесты, сквозные проверки и починка
журнала миграций. Ни одной миграции пакета до его мержа.
