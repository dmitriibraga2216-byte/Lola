// Алиас docs/04 §4.13: «PATCH /scales/:id». Код заводил только PUT (шкала пересобирается целиком,
// см. `[id].put.ts`) — то же тело и семантика полной замены, отличие только в имени метода
// (docs/33 D-064, `debts-final-b`). :id совпадает по имени с исходным маршрутом — реэкспорт без изменений.
export { default } from './[id].put'
