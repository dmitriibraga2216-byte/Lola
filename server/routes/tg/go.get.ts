/**
 * Кнопка «Пройти» из бота (docs/23 §7 п. 7). Временно ведёт на обычную страницу входа:
 * вход из бота переводится на одноразовый токен, выданный вместе с кнопкой, — до этого
 * маршрут сессий не создаёт.
 */
export default defineEventHandler(event => sendRedirect(event, '/login'))
